---
phase: 13-scaffolding-skills
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/add-provider/SKILL.md
autonomous: true
requirements: [SKILL-02]

must_haves:
  truths:
    - "/add-provider scaffolds a complete provider integration: ProviderSpec in registry.py, ModelSpec entries, Settings field, .env.example update, and a test file"
    - "Generated provider code follows the frozen dataclass pattern from providers/registry.py"
    - "Generated config field follows the os.getenv() pattern from core/config.py"
  artifacts:
    - path: ".claude/skills/add-provider/SKILL.md"
      provides: "Slash command skill for scaffolding new LLM providers"
      contains: "name: add-provider"
  key_links:
    - from: ".claude/skills/add-provider/SKILL.md"
      to: "providers/registry.py"
      via: "Inline template references ProviderType enum, ProviderSpec, ModelSpec, PROVIDERS dict, MODELS dict"
      pattern: "ProviderType|ProviderSpec|ModelSpec|PROVIDERS|MODELS"
    - from: ".claude/skills/add-provider/SKILL.md"
      to: "core/config.py"
      via: "Inline template references Settings frozen dataclass and from_env() pattern"
      pattern: "Settings|from_env|os\\.getenv"
    - from: ".claude/skills/add-provider/SKILL.md"
      to: ".env.example"
      via: "Instructions to add new API key env var"
      pattern: "env\\.example|API_KEY"
---

<objective>
Create the `/add-provider` Claude Code skill as a SKILL.md file with inline templates covering all five touchpoints of provider integration.

Purpose: Enable one-command scaffolding of a complete LLM provider (enum, spec, models, config, env, tests) instead of manually editing 4+ files.
Output: One skill file in `.claude/skills/add-provider/` that Claude Code discovers as a slash command.
</objective>

<execution_context>
@C:/Users/rickw/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rickw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/claude-code-automation-enhancements/ROADMAP.md
@.planning/workstreams/claude-code-automation-enhancements/REQUIREMENTS.md
@.planning/workstreams/claude-code-automation-enhancements/STATE.md
@.planning/workstreams/claude-code-automation-enhancements/phases/13-scaffolding-skills/13-CONTEXT.md

<interfaces>
<!-- Key types and patterns the executor needs to embed in the skill template -->

From providers/registry.py:
```python
class ProviderType(str, Enum):
    OPENAI = "openai"
    CEREBRAS = "cerebras"
    GROQ = "groq"
    # ... add new enum value here

class ModelTier(str, Enum):
    FREE = "free"
    CHEAP = "cheap"
    PREMIUM = "premium"

@dataclass(frozen=True)
class ModelSpec:
    model_id: str
    provider: ProviderType
    max_tokens: int = 4096
    temperature: float = 0.3
    display_name: str = ""
    tier: ModelTier = ModelTier.FREE
    max_context_tokens: int = 128000

@dataclass(frozen=True)
class ProviderSpec:
    provider_type: ProviderType
    base_url: str
    api_key_env: str
    display_name: str
    default_model: str = ""
    requires_model_prefix: bool = False
    supports_function_calling: bool = True

PROVIDERS: dict[ProviderType, ProviderSpec] = {
    ProviderType.OPENAI: ProviderSpec(
        provider_type=ProviderType.OPENAI,
        base_url="https://api.openai.com/v1",
        api_key_env="OPENAI_API_KEY",
        display_name="OpenAI",
        default_model="gpt-4o-mini",
    ),
    # ...
}

MODELS: dict[str, ModelSpec] = {
    "gpt-4o-mini": ModelSpec(
        model_id="gpt-4o-mini",
        provider=ProviderType.OPENAI,
        display_name="GPT-4o Mini",
        tier=ModelTier.CHEAP,
    ),
    # ...
}
```

From core/config.py Settings pattern:
```python
@dataclass(frozen=True)
class Settings:
    cerebras_api_key: str = ""
    groq_api_key: str = ""
    # ... add new field here

    @classmethod
    def from_env(cls):
        return cls(
            cerebras_api_key=os.getenv("CEREBRAS_API_KEY", ""),
            groq_api_key=os.getenv("GROQ_API_KEY", ""),
            # ... add new getenv here
        )
```

From .env.example pattern:
```
# Provider API Keys
CEREBRAS_API_KEY=        # Free tier: https://cloud.cerebras.ai
GROQ_API_KEY=            # Free tier: https://console.groq.com
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /add-provider skill</name>
  <files>.claude/skills/add-provider/SKILL.md</files>
  <action>
Create `.claude/skills/add-provider/` directory and `SKILL.md` file.

YAML frontmatter:
- name: add-provider
- description: Scaffold a complete LLM provider integration (registry, config, env, tests)
- always: false
- task_types: [coding]

Skill instructions must cover:

1. **Input gathering**: Instruct Claude to ask the developer for:
   - Provider name (e.g., "fireworks", "replicate")
   - Base URL (e.g., "https://api.fireworks.ai/inference/v1")
   - API key environment variable name (e.g., "FIREWORKS_API_KEY")
   - Display name (e.g., "Fireworks AI")
   - Model IDs to register (at least one, with display names, max tokens, tier)
   - Default model ID
   - Whether it supports function calling (default: true)
   - Tier (free, cheap, premium)
   - Sign-up URL for .env.example comment

2. **Pre-generation step**: Read `providers/registry.py` to see current ProviderType enum values, PROVIDERS dict entries, and MODELS dict entries. Read `core/config.py` to see current Settings fields and from_env() method. Read `.env.example` to see current env var layout.

3. **Step-by-step modifications** (the skill must instruct Claude to make these edits in order):

   **Step A — `providers/registry.py` ProviderType enum**: Add new enum value. Use UPPER_SNAKE_CASE for the enum name, lowercase string value. Place alphabetically among existing entries.

   **Step B — `providers/registry.py` PROVIDERS dict**: Add new ProviderSpec entry:
   ```python
   ProviderType.<NAME>: ProviderSpec(
       provider_type=ProviderType.<NAME>,
       base_url="<base_url>",
       api_key_env="<API_KEY_ENV>",
       display_name="<Display Name>",
       default_model="<default_model_id>",
       supports_function_calling=<bool>,
   ),
   ```

   **Step C — `providers/registry.py` MODELS dict**: Add ModelSpec entries for each model:
   ```python
   "<model_id>": ModelSpec(
       model_id="<model_id>",
       provider=ProviderType.<NAME>,
       display_name="<Display Name>",
       tier=ModelTier.<TIER>,
       max_tokens=<N>,
       max_context_tokens=<N>,
   ),
   ```

   **Step D — `core/config.py` Settings class**: Add `<name>_api_key: str = ""` field among other API key fields (alphabetical). Add `<name>_api_key=os.getenv("<API_KEY_ENV>", "")` to `from_env()`.

   **Step E — `.env.example`**: Add `<API_KEY_ENV>=` with comment showing tier and sign-up URL, placed in the Provider API Keys section.

   **Step F — `tests/test_<name>_provider.py`**: Create test file with:
   - `class TestProviderRegistration:` verifying ProviderType enum, ProviderSpec in PROVIDERS, ModelSpec in MODELS
   - `class TestProviderConfig:` verifying Settings has the API key field
   - Test that `get_client()` returns an AsyncOpenAI instance when key is set (mock the key via monkeypatch)

4. **Post-generation**: Run `python -m pytest tests/test_<name>_provider.py -x -q` to verify.

5. **What NOT to do**: Do NOT hardcode premium models as defaults (per CLAUDE.md). Do NOT add the provider to `FREE_ROUTING` or `model_router.py` -- that's dynamic routing territory. Do NOT forget `supports_function_calling` field if the provider doesn't support it.

6. **Checklist** (instruct Claude to verify before finishing):
   - [ ] ProviderType enum value added
   - [ ] ProviderSpec in PROVIDERS dict
   - [ ] ModelSpec(s) in MODELS dict
   - [ ] Settings field + from_env() entry in config.py
   - [ ] .env.example updated
   - [ ] Test file created and passing
  </action>
  <verify>
    <automated>test -f C:/Users/rickw/projects/agent42/.claude/skills/add-provider/SKILL.md && grep -q "name: add-provider" C:/Users/rickw/projects/agent42/.claude/skills/add-provider/SKILL.md && grep -q "always: false" C:/Users/rickw/projects/agent42/.claude/skills/add-provider/SKILL.md && grep -q "ProviderSpec" C:/Users/rickw/projects/agent42/.claude/skills/add-provider/SKILL.md && grep -q "ModelSpec" C:/Users/rickw/projects/agent42/.claude/skills/add-provider/SKILL.md && grep -q "config.py" C:/Users/rickw/projects/agent42/.claude/skills/add-provider/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
  - `.claude/skills/add-provider/SKILL.md` exists with valid YAML frontmatter (name, description, always: false, task_types)
  - Instructions cover all 6 modification steps: ProviderType enum, ProviderSpec, ModelSpec, Settings field, .env.example, test file
  - Inline templates show exact code patterns matching existing registry.py and config.py structure
  - Instructions include input gathering (provider name, base URL, API key env var, models, tier)
  - Instructions include post-generation pytest verification
  - Instructions include completion checklist
  </done>
</task>

</tasks>

<verification>
Skill file exists and has valid structure:
```bash
echo "=== add-provider ==="
head -10 .claude/skills/add-provider/SKILL.md
echo "---"
# Verify all five touchpoints are referenced
grep -c "registry.py\|config.py\|env.example\|test_.*provider\|ProviderType\|ProviderSpec\|ModelSpec\|Settings" .claude/skills/add-provider/SKILL.md
```
</verification>

<success_criteria>
- `.claude/skills/add-provider/SKILL.md` exists with `always: false` and `task_types: [coding]`
- Skill instructions reference all 5 files that need modification (registry.py, config.py, .env.example) + test file creation
- Inline templates show ProviderSpec, ModelSpec, and Settings field patterns matching existing codebase
- Input gathering prompts developer for provider name, base URL, API key env var, model IDs, tier
- Instructions warn against hardcoding premium defaults and adding to FREE_ROUTING
</success_criteria>

<output>
After completion, create `.planning/workstreams/claude-code-automation-enhancements/phases/13-scaffolding-skills/13-02-SUMMARY.md`
</output>
