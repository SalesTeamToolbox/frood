---
phase: 11-mcp-server-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .mcp.json
  - .gitignore
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
must_haves:
  truths:
    - "Developer can query context7 for any npm/pip library and receive current API docs"
    - "Developer can list, create, and comment on GitHub issues and PRs from within Claude Code"
    - "Developer can check CI/workflow status for the agent42 repo from within Claude Code"
    - "Developer can use Playwright MCP for browser automation in Claude Code"
    - ".mcp.json persists all server configs across session restarts"
  artifacts:
    - path: ".mcp.json"
      provides: "MCP server configuration for context7, GitHub, Playwright, and jcodemunch"
      contains: "context7"
    - path: ".mcp.json"
      provides: "GitHub MCP server entry referencing GITHUB_PERSONAL_ACCESS_TOKEN env var"
      contains: "github"
  key_links:
    - from: ".mcp.json"
      to: "GITHUB_PERSONAL_ACCESS_TOKEN"
      via: "env var reference in mcpServers.github.env"
      pattern: "GITHUB_PERSONAL_ACCESS_TOKEN"
---

<objective>
Configure context7, GitHub, and Playwright MCP servers in `.mcp.json` so developers can query live library docs, manage GitHub resources, and run browser automation from within Claude Code sessions.

Purpose: Eliminate context-switching between Claude Code and external tools (docs sites, GitHub web UI, browser DevTools) during development.
Output: Updated `.mcp.json` with four servers (existing jcodemunch + three new), cleaned up stale `.playwright-mcp/` directory.
</objective>

<execution_context>
@C:/Users/rickw/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rickw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mcp-server-integration/11-CONTEXT.md

<interfaces>
<!-- Existing .mcp.json structure to extend -->
From .mcp.json:
```json
{
  "mcpServers": {
    "jcodemunch": {
      "command": "uvx",
      "args": ["jcodemunch-mcp"],
      "env": {}
    }
  }
}
```

MCP server entry format (Claude Code convention):
- `command`: executable to run (npx, uvx, node, etc.)
- `args`: array of CLI arguments
- `env`: object of environment variables (can reference system env vars)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add context7, GitHub, and Playwright MCP servers to .mcp.json</name>
  <files>.mcp.json</files>
  <action>
Read the existing `.mcp.json` and add three new server entries to `mcpServers`, preserving the existing `jcodemunch` entry:

1. **context7** (MCP-01): Add entry using npx to run `@upstash/context7-mcp@latest`. No auth needed. This enables `resolve-library-id` and `get-library-docs` tools for querying any library documentation.
   ```json
   "context7": {
     "command": "npx",
     "args": ["-y", "@upstash/context7-mcp@latest"],
     "env": {}
   }
   ```

2. **github** (MCP-02): Add entry using npx to run `@modelcontextprotocol/server-github`. The GitHub token MUST NOT be embedded in `.mcp.json` -- reference it via env var. Per user decision: token stored as system env var `GITHUB_PERSONAL_ACCESS_TOKEN` with `repo` + `workflow` scopes. The server needs the token as `GITHUB_PERSONAL_ACCESS_TOKEN` in its env block.
   ```json
   "github": {
     "command": "npx",
     "args": ["-y", "@modelcontextprotocol/server-github"],
     "env": {
       "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
     }
   }
   ```
   Note: Claude Code's `.mcp.json` env block passes environment variables to the spawned process. Using the `${VAR}` syntax or simply listing the var name causes the system env var to be forwarded. Check the actual Claude Code MCP convention -- if the convention is to just list the var name as key with empty string value to forward from system env, use that pattern. The goal is: token comes from the developer's system environment, not from the file.

3. **playwright** (MCP-03): Add entry using npx to run `@anthropic/mcp-server-playwright`. No auth needed. This provides browser automation tools (navigate, screenshot, click, fill, etc.).
   ```json
   "playwright": {
     "command": "npx",
     "args": ["-y", "@anthropic/mcp-server-playwright"],
     "env": {}
   }
   ```

The final `.mcp.json` should have exactly 4 servers: jcodemunch, context7, github, playwright. Write valid JSON with consistent formatting.
  </action>
  <verify>
    <automated>node -e "const cfg = require('./.mcp.json'); const s = Object.keys(cfg.mcpServers); console.log('Servers:', s); if (!s.includes('context7')) throw 'Missing context7'; if (!s.includes('github')) throw 'Missing github'; if (!s.includes('playwright')) throw 'Missing playwright'; if (!s.includes('jcodemunch')) throw 'Missing jcodemunch'; if (JSON.stringify(cfg).includes('ghp_') || JSON.stringify(cfg).includes('github_pat_')) throw 'Token embedded in file!'; console.log('PASS: 4 servers configured, no embedded tokens');"</automated>
  </verify>
  <done>`.mcp.json` contains 4 MCP server entries (jcodemunch, context7, github, playwright). GitHub token is referenced via env var, not embedded. File is valid JSON and committed to repo.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up .playwright-mcp directory and update .gitignore</name>
  <files>.gitignore</files>
  <action>
1. **Remove `.playwright-mcp/` directory**: This directory only contains stale log files and screenshots from previous browser testing sessions (console logs, PNG screenshots). It is NOT a Playwright MCP configuration directory. Per user decision, Playwright config is now in `.mcp.json`. Delete the entire `.playwright-mcp/` directory.

2. **Update `.gitignore`**: Add a pattern to prevent Playwright MCP artifacts from accumulating again. Add the following line (in a logical section, near other tool/generated-file patterns):
   ```
   # Playwright MCP artifacts (logs, screenshots)
   .playwright-mcp/
   ```

3. **Verify no other references**: Check if any other files reference `.playwright-mcp/` path. If found, they should be updated to reflect the new `.mcp.json`-based config, but based on analysis this directory was only used for artifact storage.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); if (fs.existsSync('.playwright-mcp')) throw '.playwright-mcp still exists'; const gi = fs.readFileSync('.gitignore','utf8'); if (!gi.includes('.playwright-mcp')) throw '.gitignore missing .playwright-mcp pattern'; console.log('PASS: .playwright-mcp removed, .gitignore updated');"</automated>
  </verify>
  <done>`.playwright-mcp/` directory is deleted. `.gitignore` includes pattern to prevent re-accumulation. No stale references remain.</done>
</task>

</tasks>

<verification>
1. `.mcp.json` is valid JSON with 4 server entries
2. No secrets/tokens embedded in `.mcp.json`
3. `.playwright-mcp/` directory no longer exists
4. `.gitignore` prevents `.playwright-mcp/` re-creation
5. `git diff` shows only intended changes
</verification>

<success_criteria>
- `.mcp.json` has context7, github, playwright, and jcodemunch servers configured
- GitHub MCP references `GITHUB_PERSONAL_ACCESS_TOKEN` via env var (not hardcoded)
- `.playwright-mcp/` directory is removed
- `.gitignore` blocks `.playwright-mcp/` from being tracked
- All changes committed to the repo
</success_criteria>

<output>
After completion, create `.planning/phases/11-mcp-server-integration/11-01-SUMMARY.md`
</output>
