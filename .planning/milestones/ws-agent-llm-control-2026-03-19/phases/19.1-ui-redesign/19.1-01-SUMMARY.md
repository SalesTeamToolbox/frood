---
phase: 19.1-ui-redesign
plan: "01"
subsystem: dashboard
tags: [websocket, terminal, ssh, fastapi, pytest, remote-node]

requires:
  - phase: 19-agent-config-dashboard
    provides: "dashboard server.py with WebSocket terminal endpoint"

provides:
  - "Fixed terminal_ws endpoint: AGENT42_REMOTE_HOST guard returns error instead of defaulting to agent42-prod"
  - "JSON resize message parsing in WebSocket receive loop (resize not forwarded as stdin input)"
  - "/api/remote/status GET endpoint returning {available, host} based on AGENT42_REMOTE_HOST"
  - "Wave 0 test files: test_websocket_terminal.py, test_remote_status.py, test_ide_html.py"

affects: [19.1-02, frontend-ide, terminal-ws]

tech-stack:
  added: []
  patterns:
    - "Empty-string guard pattern for env vars: get('VAR', '') then if not var: close + return"
    - "JSON resize message interception before stdin forward: try json.loads(), continue on resize type"
    - "xfail markers for Wave 0 tests that verify target state before Plan 02 frontend rewrite"

key-files:
  created:
    - tests/test_websocket_terminal.py
    - tests/test_remote_status.py
    - tests/test_ide_html.py
  modified:
    - dashboard/server.py

key-decisions:
  - "Use empty string default for AGENT42_REMOTE_HOST (not 'agent42-prod') — fail closed rather than silently connecting to wrong host"
  - "Add /api/remote/status endpoint to enable frontend feature detection before attempting WebSocket connections"
  - "Mark test_ide_html.py tests as xfail until Plan 02 ships — Wave 0 tests define target state not current state"

patterns-established:
  - "Remote guard pattern: ssh_host = os.environ.get('AGENT42_REMOTE_HOST', ''); if not ssh_host: send error + close + return"
  - "Source inspection testing: import inspect; src = inspect.getsource(module); assert 'pattern' in src"

requirements-completed: []

duration: 12min
completed: "2026-03-17"
---

# Phase 19.1 Plan 01: Backend Fixes + Wave 0 Tests Summary

**Fixed AGENT42_REMOTE_HOST defaulting bug (was silently using 'agent42-prod'), added JSON resize message parsing to terminal WebSocket, added /api/remote/status endpoint, and created three Wave 0 test files**

## Performance

- **Duration:** 12 min
- **Started:** 2026-03-17T05:01:31Z
- **Completed:** 2026-03-17T05:13:31Z
- **Tasks:** 2
- **Files modified:** 4 (1 server.py + 3 new test files)

## Accomplishments

- Eliminated silent remote terminal bug: both remote branches (shell + Claude) now reject connections when AGENT42_REMOTE_HOST is unconfigured, returning a clear error message
- Added JSON resize message parsing in the WebSocket receive loop — resize messages are intercepted and not forwarded as terminal input
- Added `/api/remote/status` GET endpoint for frontend feature detection
- Created three Wave 0 test files: source-inspection tests for terminal routing (6 tests), remote status endpoint (5 tests), and IDE HTML structure (6 xfail target-state tests)

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix remote terminal default and add resize message parsing** - `1e38610` (fix)
2. **Task 2: Create Wave 0 test files for backend and frontend changes** - `ad5c5a2` (test)

## Files Created/Modified

- `dashboard/server.py` - Two remote branch guards, JSON resize parsing, /api/remote/status endpoint
- `tests/test_websocket_terminal.py` - Source inspection tests for remote guard, resize parsing, no agent42-prod default
- `tests/test_remote_status.py` - Tests for /api/remote/status endpoint logic and registration
- `tests/test_ide_html.py` - Target-state tests for Plan 02 IDE HTML structure (all xfail until frontend rewrite)

## Decisions Made

- Used empty-string default for AGENT42_REMOTE_HOST (not hardcoded hostname) so unconfigured nodes fail closed with a clear error rather than silently attempting to SSH to a wrong host
- Added `/api/remote/status` as a lightweight pre-flight check endpoint so the frontend can detect availability before attempting a WebSocket connection
- Marked all test_ide_html.py tests as `pytest.mark.xfail` since they verify the Plan 02 target state — the tests define the spec for the rewrite, not the current state

## Deviations from Plan

None - plan executed exactly as written.

The plan specified applying the remote host guard to "BOTH locations" which was done (lines 1466 and 1500 in server.py). The plan also noted the test_ide_html.py tests would verify post-Plan-02 state — xfail markers were added (deviation from literal plan spec but necessary for the pytest suite to exit 0).

## Issues Encountered

- Pre-existing test failure in `tests/test_auth_flow.py::TestAuthIntegration::test_protected_endpoint_requires_auth` (404 vs 401 on /api/tasks) — confirmed pre-existing before changes, out of scope for this plan.

## Next Phase Readiness

- Backend is ready for Plan 02 (frontend rewrite): remote status API available, terminal routing guards in place
- Wave 0 test files define the target state that Plan 02 must achieve
- Plan 02 frontend work should remove xfail markers from test_ide_html.py as each element is implemented

---
*Phase: 19.1-ui-redesign*
*Completed: 2026-03-17*
