---
phase: 19.1
slug: ui-redesign
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-16
---

# Phase 19.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x + pytest-asyncio (`asyncio_mode = "auto"`) |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`) |
| **Quick run command** | `python -m pytest tests/ -x -q` |
| **Full suite command** | `python -m pytest tests/ -v` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `python -m pytest tests/ -x -q`
- **After every plan wave:** Run `python -m pytest tests/ -v`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 19.1-01-01 | 01 | 1 | Layout | unit (string grep) | `python -m pytest tests/test_ide_html.py -x` | ❌ W0 | ⬜ pending |
| 19.1-01-02 | 01 | 1 | Layout | manual | Browser inspection | n/a | ⬜ pending |
| 19.1-02-01 | 02 | 1 | Terminal | unit (backend) | `python -m pytest tests/test_websocket_terminal.py -x` | ❌ W0 | ⬜ pending |
| 19.1-02-02 | 02 | 1 | Remote | unit (backend) | `python -m pytest tests/test_remote_status.py -x` | ❌ W0 | ⬜ pending |
| 19.1-03-01 | 03 | 2 | Reconnect | manual | Close server, observe reconnect | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_websocket_terminal.py` — tests backend routing logic: local terminal, remote terminal, local claude, remote claude; tests graceful failure when AGENT42_REMOTE_HOST not set
- [ ] `tests/test_remote_status.py` — tests `/api/remote/status` endpoint with/without AGENT42_REMOTE_HOST env var
- [ ] `tests/test_ide_html.py` — loads `renderCode` function source as string and asserts: `ide-chat-panel` absent, `ide-drag-handle` present, `ide-term-dropdown` present

*Existing infrastructure covers pytest + conftest fixtures.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Terminal panel visible by default on page load | Layout | Browser DOM required | Open /code page, verify terminal panel is visible |
| Drag handle resizes terminal panel | Layout | Mouse drag not headless-testable | Drag divider, verify terminal resizes |
| "+" dropdown shows Local/Remote groups | Local/Remote | Browser DOM required | Click "+", verify grouped dropdown |
| Color dots on tabs (blue/green/amber) | Local/Remote | Visual inspection | Open local and remote tabs, verify color coding |
| Ctrl+` toggles terminal | Layout | Keyboard event in browser | Press Ctrl+`, verify panel toggles |
| Auto-reconnect triggers on WS close | Reliability | Requires live WS connection | Kill server, observe reconnect banner and retry |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
