---
phase: 19.1-ui-redesign
verified: 2026-03-17T06:00:00Z
status: human_needed
score: 14/14 must-haves verified
re_verification: null
gaps: []
human_verification:
  - test: "Navigate to Code page in browser and verify VS Code-style layout renders correctly"
    expected: "Editor occupies top portion, drag handle divider visible, terminal panel visible at bottom (~30% height) with one open local terminal tab showing a blue dot"
    why_human: "Layout rendering and visual proportions cannot be verified by static code analysis"
  - test: "Drag the divider between editor and terminal"
    expected: "Terminal panel resizes smoothly as divider is dragged; terminal text refits without overflow"
    why_human: "Mouse interaction and smooth resize behavior requires browser execution"
  - test: "Click the '+' button in the terminal header"
    expected: "Dropdown opens upward with 'Local' group (Terminal, Claude Code) and 'Remote' group. Remote options grayed out with 'No remote node configured' tooltip when AGENT42_REMOTE_HOST is unset"
    why_human: "Dropdown positioning, upward opening direction, and grayed-out tooltip state require visual verification"
  - test: "Open a second terminal tab and observe both tab dots"
    expected: "Both tabs show blue dots. Active tab is highlighted. Both tabs are independently usable."
    why_human: "Tab color rendering and independent session state require browser execution"
  - test: "Press Ctrl+backtick to toggle the terminal panel, then press again"
    expected: "Terminal panel hides on first press, restores on second press. Navigating away and back to Code page does not duplicate the keydown listener."
    why_human: "Keyboard shortcut and SPA listener deduplication require interactive browser testing"
  - test: "Kill the WebSocket connection (stop server briefly or block ws:// in DevTools)"
    expected: "Terminal shows yellow 'Disconnected - reconnecting in 1s...' with amber dot, retries at 1s/2s/4s intervals, then shows red 'Disconnected (max retries)' after 3 failures"
    why_human: "WebSocket reconnect timing and ANSI color output require live browser observation"
  - test: "Verify status bar and absence of Chat panel"
    expected: "Status bar at very bottom shows backtick 'Terminal' button only — no 'Chat' button. No AI Chat side panel anywhere on the right side of the page."
    why_human: "Visual presence/absence of UI elements requires browser rendering"
---

# Phase 19.1: IDE UI Redesign Verification Report

**Phase Goal:** Redesign the IDE/coding page with VS Code-style layout — editor top, resizable terminal bottom, drag handle, terminal dropdown, auto-reconnect, remove AI Chat panel

**Verified:** 2026-03-17T06:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths — Plan 01 (Backend)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Remote terminal/CC WebSocket returns error when AGENT42_REMOTE_HOST is not configured | VERIFIED | `grep -c "if not ssh_host:" server.py` returns 2; both remote branches have guard at lines 1465 and 1499 |
| 2 | /api/remote/status returns {available: true, host: hostname} when AGENT42_REMOTE_HOST is set | VERIFIED | `async def remote_status` at line 1570; returns `{"available": bool(host), "host": host if host else None}` |
| 3 | /api/remote/status returns {available: false, host: null} when AGENT42_REMOTE_HOST is not set | VERIFIED | Same implementation handles empty string correctly |
| 4 | Backend parses JSON resize messages and does not forward them as terminal input | VERIFIED | `parsed.get("type") == "resize"` at line 1547; `continue` skips stdin forward |
| 5 | Tests for terminal routing and remote status endpoint pass | VERIFIED | 11 tests across test_websocket_terminal.py and test_remote_status.py — all 17 phase tests pass |

**Score:** 5/5 Plan 01 truths verified

### Observable Truths — Plan 02 (Frontend)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Terminal panel visible by default when Code page loads | VERIFIED | renderCode() sets `_termVisible = true` and calls `termNew("local")` if no sessions |
| 2 | Editor top, terminal bottom ~30%, draggable divider between them | VERIFIED | ide-top-row → ide-drag-handle → ide-terminal-wrapper order confirmed in renderCode(); CSS `.ide-terminal-wrapper { height: 30% }` |
| 3 | Single + button opens dropdown with Local/Remote groups | VERIFIED | `ide-term-dropdown` div with `dropdown-group-label` elements "Local" and "Remote" present at lines 3240/3243 |
| 4 | Remote options grayed out with tooltip when no remote node configured | VERIFIED | `checkRemoteAvailable` sets `opacity: 0.4`, `pointerEvents: none`, `title: "No remote node configured"` on `.remote-item` elements |
| 5 | Color-coded dots: blue local, green remote, amber reconnecting | VERIFIED | `dotColor = s.reconnecting ? "#f59e0b" : (s.node === "remote" ? "#34d399" : "#38bdf8")` at line 3753; `&#9679;` circle at line 3755 |
| 6 | AI Chat side panel completely removed | VERIFIED | `grep -c "ideChatToggle\|ideChatSend\|_ideChatHistory\|ide-chat-panel" app.js` returns 0 |
| 7 | Ctrl+backtick toggles terminal without duplicating listeners on revisit | VERIFIED | Guard at line 3280: `if (!window._ideKeyListenerAttached)`; key handler checks `e.key === "\`"` at line 3284 |
| 8 | Terminal auto-reconnects with 1s/2s/4s exponential backoff | VERIFIED | `Math.pow(2, retryCount) * 1000` at line 3788; capped at 3 retries |
| 9 | Dragging divider resizes terminal and refits all xterm.js terminals | VERIFIED | `initDragHandle()` at line 3868; calls `termFitAll()` on mousemove |
| 10 | Status bar shows Terminal toggle button (no Chat button) | VERIFIED | Status bar HTML contains only backtick Terminal button; no Chat reference in statusbar HTML |

**Score:** 10/10 Plan 02 truths verified (automated) + 7 items flagged for human verification

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `dashboard/server.py` | Fixed terminal_ws with remote guard, resize parsing, /api/remote/status | VERIFIED | 2 guards at lines 1465/1499; resize parsing at line 1547; remote_status at line 1570 |
| `tests/test_websocket_terminal.py` | Terminal routing tests (min 30 lines) | VERIFIED | 55 lines, 6 tests, all pass |
| `tests/test_remote_status.py` | Remote status endpoint tests (min 20 lines) | VERIFIED | 63 lines, 5 tests, all pass |
| `tests/test_ide_html.py` | IDE HTML structure tests (min 25 lines) | VERIFIED | 45 lines, 6 tests, all pass (xfail markers removed as planned) |
| `dashboard/frontend/dist/app.js` | Restructured renderCode(), new functions, chat removed | VERIFIED | Contains ide-drag-handle (5), ide-term-dropdown (3), termConnectWs (4), termFitAll (4); ideChatToggle/ideChatSend/ide-chat-panel count = 0 |
| `dashboard/frontend/dist/style.css` | Drag handle, dropdown, updated terminal wrapper CSS | VERIFIED | `.ide-drag-handle` at line 1787; `.ide-term-dropdown` at line 1791; `.ide-terminal-wrapper { height: 30% }` at line 1768 |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `dashboard/server.py` | AGENT42_REMOTE_HOST env var | `os.environ.get` with empty-string guard | WIRED | `_os.environ.get("AGENT42_REMOTE_HOST", "")` + `if not ssh_host:` at both remote branches |
| `dashboard/frontend/dist/app.js` | `/api/remote/status` | `checkRemoteAvailable()` fetch call | WIRED | `apiFetch("/api/remote/status")` at line 3852 inside `checkRemoteAvailable()` |
| `dashboard/frontend/dist/app.js` | `dashboard/frontend/dist/style.css` | CSS classes ide-drag-handle, ide-term-dropdown | WIRED | Classes used in renderCode() HTML; `.ide-drag-handle` and `.ide-term-dropdown` rules exist in style.css |

---

## Requirements Coverage

No formal requirement IDs declared in either plan's `requirements` field (both set `requirements: []`). This is an inserted phase with no corresponding REQUIREMENTS.md entries. No orphaned requirements to flag.

---

## Anti-Patterns Found

No blocker anti-patterns detected:

- No TODO/FIXME/PLACEHOLDER comments in modified files
- No stub implementations (return null / return {} / empty handlers)
- No per-session `window.addEventListener("resize", ...)` accumulation — single shared `termFitAll` registered once with guard
- No `agent42-prod` default remaining in server.py (grep returns 0)
- HTML `placeholder=` attributes in app.js are legitimate form input placeholders, not stub code

---

## Pre-Existing Test Failure (Out of Scope)

`tests/test_auth_flow.py::TestAuthIntegration::test_protected_endpoint_requires_auth` fails with 404 vs expected 401 on `/api/tasks`. Confirmed pre-existing: the file was not modified in phase 19.1 (`git diff cfa2b48..HEAD -- tests/test_auth_flow.py` returns no output). Both plan summaries also documented this as a known pre-existing issue.

---

## Human Verification Required

All automated checks pass. The following 7 items require human browser testing to confirm the goal is fully achieved:

### 1. VS Code-Style Layout Renders Correctly

**Test:** Navigate to Code page in a running Agent42 instance
**Expected:** Editor visible at top, drag handle divider visible as thin horizontal bar, terminal panel visible at bottom (~30% height) with one open local terminal tab showing a blue dot
**Why human:** Layout rendering and visual proportions cannot be verified by static code analysis

### 2. Drag Handle Resize

**Test:** Click and drag the divider between editor and terminal up and down
**Expected:** Terminal panel resizes smoothly; terminal text content refits without overflow or clipping; drag is responsive
**Why human:** Mouse interaction and smooth resize behavior requires browser execution

### 3. Dropdown Opens Upward with Correct Groups

**Test:** Click the '+' button in the terminal header bar
**Expected:** Dropdown opens upward (above the button), shows "Local" group with Terminal and Claude Code items, then "Remote" group with same items grayed out and showing "No remote node configured" tooltip (when AGENT42_REMOTE_HOST unset)
**Why human:** Dropdown direction, positioning relative to screen bottom, and tooltip state require visual verification

### 4. Color-Coded Tab Dots

**Test:** Open multiple terminal tabs (local terminal, local Claude Code)
**Expected:** Each tab shows a colored circle before the label — blue for local shell, blue for local Claude, labels like "bash 1", "Claude (local)". All dots visible and correctly colored.
**Why human:** Tab color rendering and label formatting require browser rendering

### 5. Ctrl+Backtick Toggle (SPA-safe)

**Test:** Press Ctrl+backtick; navigate away to another page; navigate back to Code page; press Ctrl+backtick again
**Expected:** First press hides terminal, second press restores it. After SPA navigation away and back, Ctrl+backtick still works exactly once per press (no listener accumulation)
**Why human:** Keyboard shortcut and SPA listener deduplication require interactive browser testing

### 6. Auto-Reconnect with Exponential Backoff

**Test:** Open a terminal session, then stop the Agent42 server process (or block WebSocket in DevTools Network tab)
**Expected:** Terminal shows yellow "Disconnected — reconnecting in 1s..." with amber dot on tab. After 1s retries, shows "reconnecting in 2s...", then "4s...". After 3 failures, shows red "Disconnected (max retries)" and tab dot returns to blue (no longer amber).
**Why human:** WebSocket reconnect timing, ANSI color output rendering, and dot color transitions require live browser observation

### 7. Status Bar and Chat Panel Absence

**Test:** Look at the full Code page layout
**Expected:** Status bar at very bottom shows only the backtick "Terminal" toggle button — no "Chat" button. No AI Chat side panel appears anywhere on the right side of the page.
**Why human:** Visual presence/absence of UI elements requires browser rendering

---

## Summary

Phase 19.1 achieved its goal in the codebase. All 14 must-haves (5 from Plan 01, 9 from Plan 02 that are statically verifiable) pass full three-level verification (exists, substantive, wired). All 17 automated tests pass. No anti-patterns or stub implementations found.

The phase is blocked only on human visual verification — the 7 items above cannot be confirmed without running the server and inspecting browser output. The automated test suite (including the 6 test_ide_html.py structural tests) confirms the HTML structure, function presence, and logic are correct, but visual layout, mouse drag behavior, and WebSocket timing require a human to sign off.

---

_Verified: 2026-03-17T06:00:00Z_
_Verifier: Claude (gsd-verifier)_
