---
phase: 17-tier-routing-architecture
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - agents/model_catalog.py
  - tests/test_model_router.py
  - tests/test_dynamic_routing.py
  - tests/test_model_catalog.py
  - tests/test_openclaw_features.py
  - tests/test_project_interview.py
autonomous: true
requirements: [TIER-01, TIER-02, TIER-03, TIER-04, TIER-05, ROUTE-02, ROUTE-03]

must_haves:
  truths:
    - "All test files import FALLBACK_ROUTING instead of FREE_ROUTING"
    - "model_catalog.py references FALLBACK_ROUTING for validation"
    - "New test class TestL1Routing verifies L1 resolution, health checks, and fallback"
    - "New test class TestL2RoutingUpdates verifies L2 self-critique and OR paid availability"
    - "New test class TestFallbackChain verifies StrongWall -> Free -> L2 chain"
    - "TestFreeRoutingUpdates renamed to TestFallbackRoutingEntries and uses FALLBACK_ROUTING"
    - "Full test suite passes"
  artifacts:
    - path: "tests/test_model_router.py"
      provides: "L1/L2 tier routing tests and renamed FALLBACK_ROUTING references"
      contains: "TestL1Routing"
    - path: "tests/test_model_router.py"
      provides: "L2 self-critique and OR paid tests"
      contains: "TestL2RoutingUpdates"
    - path: "tests/test_model_router.py"
      provides: "Full fallback chain test"
      contains: "TestFallbackChain"
    - path: "agents/model_catalog.py"
      provides: "Updated FALLBACK_ROUTING import"
      contains: "FALLBACK_ROUTING"
  key_links:
    - from: "tests/test_model_router.py"
      to: "agents/model_router.py::FALLBACK_ROUTING"
      via: "import statement"
      pattern: "from agents\\.model_router import FALLBACK_ROUTING"
    - from: "tests/test_model_router.py::TestL1Routing"
      to: "agents/model_router.py::_get_l1_routing"
      via: "test exercises L1 routing path"
      pattern: "get_routing.*CODING"
    - from: "agents/model_catalog.py"
      to: "agents/model_router.py::FALLBACK_ROUTING"
      via: "import for model validation"
      pattern: "from agents\\.model_router import FALLBACK_ROUTING"
---

<objective>
Update all consumer files to use FALLBACK_ROUTING instead of FREE_ROUTING, and add comprehensive test coverage for L1 routing, L2 self-critique, L2 OR paid availability, and the full fallback chain (StrongWall -> Free -> L2).

Purpose: Complete the rename across the codebase and ensure all new tier routing behavior is tested
Output: Updated test files and model_catalog.py with FALLBACK_ROUTING references, new test classes for L1/L2/fallback
</objective>

<execution_context>
@C:/Users/rickw/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rickw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/agent-llm-control/ROADMAP.md
@.planning/workstreams/agent-llm-control/STATE.md
@.planning/workstreams/agent-llm-control/phases/17-tier-routing-architecture/17-CONTEXT.md
@.planning/workstreams/agent-llm-control/phases/17-tier-routing-architecture/17-RESEARCH.md
@.planning/workstreams/agent-llm-control/phases/17-tier-routing-architecture/17-01-SUMMARY.md

<interfaces>
<!-- Contracts created by Plan 01 that this plan depends on. -->

From agents/model_router.py (Plan 01 output):
```python
# Canonical dict name (was FREE_ROUTING)
FALLBACK_ROUTING: dict[TaskType, dict] = { ... }

# Backward-compatible alias (Plan 02 will remove need for this)
FREE_ROUTING = FALLBACK_ROUTING

# L2_ROUTING now has self-critique:
L2_ROUTING: dict[TaskType, dict] = {
    TaskType.CODING: {"primary": "claude-sonnet", "critic": "claude-sonnet", "max_iterations": 3},
    # ... all entries have critic == primary
}

class ModelRouter:
    def _resolve_l1_model(self) -> str: ...
    def _is_l1_available(self, l1_model: str) -> bool: ...
    def _get_l1_routing(self, task_type: TaskType) -> dict | None: ...
    def authorize_l2(self, task_type: str | None = None, task_id: str | None = None): ...
    def revoke_l2(self, task_type: str | None = None, task_id: str | None = None): ...
    def is_l2_authorized(self, task_type: TaskType, task_id: str = "") -> bool: ...
```

From agents/model_catalog.py (lines referencing FREE_ROUTING):
```python
# Line 293-306: validate_primary_models() imports FREE_ROUTING
from agents.model_router import FREE_ROUTING  # <-- change to FALLBACK_ROUTING
for _task_type, routing in FREE_ROUTING.items():  # <-- change to FALLBACK_ROUTING
```

From tests/test_model_router.py (existing test classes):
```python
from agents.model_router import FREE_ROUTING, ...  # <-- change to FALLBACK_ROUTING
class TestFreeRoutingUpdates:  # <-- rename to TestFallbackRoutingEntries
    # 12 assertions using FREE_ROUTING[TaskType.X]  # <-- change to FALLBACK_ROUTING
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename FREE_ROUTING references in all consumer files</name>
  <files>agents/model_catalog.py, tests/test_model_router.py, tests/test_dynamic_routing.py, tests/test_model_catalog.py, tests/test_openclaw_features.py, tests/test_project_interview.py</files>
  <behavior>
    - All files import FALLBACK_ROUTING from agents.model_router (not FREE_ROUTING)
    - All references to FREE_ROUTING within these files changed to FALLBACK_ROUTING
    - TestFreeRoutingUpdates class renamed to TestFallbackRoutingEntries
    - TestFreeRoutingUpdates docstring updated to reference FALLBACK_ROUTING
    - All existing test assertions still pass with the new name
  </behavior>
  <action>
Update all 6 files that import or reference `FREE_ROUTING`:

**agents/model_catalog.py** (3 references at lines ~293, 299, 306):
- Change import from `from agents.model_router import FREE_ROUTING` to `from agents.model_router import FALLBACK_ROUTING`
- Change all iteration references from `FREE_ROUTING.items()` to `FALLBACK_ROUTING.items()`

**tests/test_model_router.py** (~20 references):
- Change import line 9 from `FREE_ROUTING` to `FALLBACK_ROUTING`
- Lines 154, 168-169: Change `FREE_ROUTING[TaskType.CODING]` to `FALLBACK_ROUTING[TaskType.CODING]`
- Rename class `TestFreeRoutingUpdates` (line 201) to `TestFallbackRoutingEntries`
- Update docstring from "Verify FREE_ROUTING dict" to "Verify FALLBACK_ROUTING dict"
- Lines 205-235: Change all `FREE_ROUTING[TaskType.X]` to `FALLBACK_ROUTING[TaskType.X]` (12 assertions)
- Line 434 comment: Update reference

**tests/test_dynamic_routing.py** (~10 references):
- Change import line 6 from `FREE_ROUTING` to `FALLBACK_ROUTING`
- Lines 14-15, 22, 105, 118, 128, 177: Change all `FREE_ROUTING` references to `FALLBACK_ROUTING`

**tests/test_model_catalog.py** (~5 references):
- Change import and monkeypatch references from `FREE_ROUTING` to `FALLBACK_ROUTING` (around lines 318-334)

**tests/test_openclaw_features.py** (2 references):
- Change import line 625 from `FREE_ROUTING` to `FALLBACK_ROUTING`
- Change assertion line 649 from `FREE_ROUTING[TaskType.CODING]` to `FALLBACK_ROUTING[TaskType.CODING]`

**tests/test_project_interview.py** (3 references):
- Change import (line ~585) from `FREE_ROUTING` to `FALLBACK_ROUTING`
- Change assertion (line ~588-589) from `FREE_ROUTING` to `FALLBACK_ROUTING`

**Total: ~50 references across 6 files.** Use search-and-replace for accuracy. Keep the import alias in model_router.py (`FREE_ROUTING = FALLBACK_ROUTING`) but these consumer files should use the canonical name directly.
  </action>
  <verify>
    <automated>cd C:/Users/rickw/projects/agent42 && python -m pytest tests/test_model_router.py tests/test_dynamic_routing.py tests/test_model_catalog.py tests/test_openclaw_features.py tests/test_project_interview.py -x -q</automated>
  </verify>
  <done>
- All 6 consumer files import and use FALLBACK_ROUTING (not FREE_ROUTING)
- TestFreeRoutingUpdates renamed to TestFallbackRoutingEntries
- All existing tests pass with the new import name
- No remaining references to FREE_ROUTING except the backward-compatible alias in model_router.py
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add L1/L2/fallback chain test classes</name>
  <files>tests/test_model_router.py</files>
  <behavior>
    - TestL1Routing: L1 defaults to strongwall-kimi-k2.5 when STRONGWALL_API_KEY set, L1 uses explicit L1_MODEL env var, L1 returns None when no key set, L1 returns None when provider unhealthy, L1 routing has self-critique (critic == primary), L1 max_iterations comes from FALLBACK_ROUTING, coding uses L1 not OR free when configured (ROUTE-02)
    - TestL2RoutingUpdates: L2 entries have self-critique (critic == primary for all task types), OR paid models available when balance present
    - TestFallbackChain: full chain StrongWall -> Free -> L2 when each layer fails, backward compat when no StrongWall key
  </behavior>
  <action>
Add three new test classes to tests/test_model_router.py. Add them after the existing `TestFallbackRoutingEntries` class (renamed from TestFreeRoutingUpdates in Task 1).

**Class TestL1Routing** (covers TIER-01, TIER-02, ROUTE-02):
```python
class TestL1Routing:
    """TIER-01/02: Verify L1 model resolution and routing."""

    def setup_method(self):
        self.router = ModelRouter()

    def test_l1_defaults_to_strongwall(self, monkeypatch):
        """TIER-02: StrongWall is L1 when API key is set."""
        monkeypatch.setenv("STRONGWALL_API_KEY", "test-key")
        monkeypatch.delenv("L1_MODEL", raising=False)
        monkeypatch.delenv("L1_DEFAULT_MODEL", raising=False)
        assert self.router._resolve_l1_model() == "strongwall-kimi-k2.5"

    def test_l1_explicit_model_override(self, monkeypatch):
        """L1_MODEL env var takes priority over auto-detection."""
        monkeypatch.setenv("L1_MODEL", "custom-model-v2")
        monkeypatch.setenv("STRONGWALL_API_KEY", "test-key")
        assert self.router._resolve_l1_model() == "custom-model-v2"

    def test_l1_not_configured_returns_empty(self, monkeypatch):
        """No L1 when neither L1_MODEL nor STRONGWALL_API_KEY set."""
        monkeypatch.delenv("L1_MODEL", raising=False)
        monkeypatch.delenv("L1_DEFAULT_MODEL", raising=False)
        monkeypatch.delenv("STRONGWALL_API_KEY", raising=False)
        assert self.router._resolve_l1_model() == ""

    def test_l1_routing_has_self_critique(self, monkeypatch):
        """L1 self-critique: critic == primary."""
        monkeypatch.setenv("STRONGWALL_API_KEY", "test-key")
        monkeypatch.delenv("L1_MODEL", raising=False)
        # Mock health checks to return healthy
        # Use monkeypatch to make _is_l1_available return True
        monkeypatch.setattr(self.router, "_is_l1_available", lambda m: True)
        routing = self.router._get_l1_routing(TaskType.CODING)
        assert routing is not None
        assert routing["critic"] == routing["primary"]
        assert routing["primary"] == "strongwall-kimi-k2.5"

    def test_l1_max_iterations_from_fallback(self, monkeypatch):
        """L1 reuses max_iterations from FALLBACK_ROUTING per task type."""
        monkeypatch.setenv("STRONGWALL_API_KEY", "test-key")
        monkeypatch.delenv("L1_MODEL", raising=False)
        monkeypatch.setattr(self.router, "_is_l1_available", lambda m: True)
        routing = self.router._get_l1_routing(TaskType.APP_CREATE)
        assert routing["max_iterations"] == FALLBACK_ROUTING[TaskType.APP_CREATE]["max_iterations"]

    def test_l1_unavailable_returns_none(self, monkeypatch):
        """L1 returns None when provider is unhealthy."""
        monkeypatch.setenv("STRONGWALL_API_KEY", "test-key")
        monkeypatch.delenv("L1_MODEL", raising=False)
        monkeypatch.setattr(self.router, "_is_l1_available", lambda m: False)
        assert self.router._get_l1_routing(TaskType.CODING) is None

    def test_coding_uses_l1_not_or_free(self, monkeypatch):
        """ROUTE-02: Critical tasks use L1 when configured, not OR free models."""
        monkeypatch.setenv("STRONGWALL_API_KEY", "test-key")
        monkeypatch.delenv("L1_MODEL", raising=False)
        monkeypatch.delenv("GEMINI_PRO_FOR_COMPLEX", raising=False)
        monkeypatch.setenv("GEMINI_PRO_FOR_COMPLEX", "false")  # Disable Gemini Pro upgrade
        monkeypatch.setattr(self.router, "_is_l1_available", lambda m: True)
        monkeypatch.setattr(self.router, "_get_l1_routing", lambda tt: {
            "primary": "strongwall-kimi-k2.5",
            "critic": "strongwall-kimi-k2.5",
            "max_iterations": FALLBACK_ROUTING[tt]["max_iterations"],
        })
        routing = self.router.get_routing(TaskType.CODING)
        assert routing["primary"] == "strongwall-kimi-k2.5"
```

**Class TestL2RoutingUpdates** (covers TIER-03, TIER-04):
```python
class TestL2RoutingUpdates:
    """TIER-03/04: Verify L2 routing with self-critique and OR paid availability."""

    def test_l2_all_entries_have_self_critique(self):
        """TIER-03: All L2_ROUTING entries have critic == primary (self-critique)."""
        for task_type, routing in L2_ROUTING.items():
            assert routing["critic"] == routing["primary"], (
                f"L2_ROUTING[{task_type}] critic should equal primary for self-critique"
            )

    def test_l2_coding_uses_claude_sonnet(self):
        """TIER-03: Coding L2 defaults to claude-sonnet."""
        assert L2_ROUTING[TaskType.CODING]["primary"] == "claude-sonnet"

    def test_l2_research_uses_gpt4o(self):
        """TIER-03: Research L2 defaults to gpt-4o."""
        assert L2_ROUTING[TaskType.RESEARCH]["primary"] == "gpt-4o"
```

**Class TestFallbackChain** (covers TIER-05):
```python
class TestFallbackChain:
    """TIER-05: Verify full fallback chain StrongWall -> Free -> L2."""

    def setup_method(self):
        self.router = ModelRouter()

    def test_fallback_chain_l1_to_fallback(self, monkeypatch):
        """When L1 is unavailable, get_routing falls to FALLBACK_ROUTING."""
        monkeypatch.delenv("STRONGWALL_API_KEY", raising=False)
        monkeypatch.delenv("L1_MODEL", raising=False)
        monkeypatch.setenv("CEREBRAS_API_KEY", "test-key")
        monkeypatch.setenv("GEMINI_PRO_FOR_COMPLEX", "false")
        routing = self.router.get_routing(TaskType.CODING)
        # Should use FALLBACK_ROUTING default (Cerebras for coding)
        assert routing["primary"] == FALLBACK_ROUTING[TaskType.CODING]["primary"]

    def test_backward_compat_no_strongwall(self, monkeypatch):
        """ROUTE-03: Existing behavior preserved when no StrongWall key."""
        monkeypatch.delenv("STRONGWALL_API_KEY", raising=False)
        monkeypatch.delenv("L1_MODEL", raising=False)
        monkeypatch.setenv("CEREBRAS_API_KEY", "test-key")
        monkeypatch.setenv("GEMINI_PRO_FOR_COMPLEX", "false")
        routing = self.router.get_routing(TaskType.CODING)
        expected = FALLBACK_ROUTING[TaskType.CODING]
        assert routing["primary"] == expected["primary"]
        assert routing["critic"] == expected["critic"]
```

Adapt the exact test implementations to match the actual method signatures and behavior from Plan 01. Use `monkeypatch` for env vars (not `os.environ` directly — pytest monkeypatch handles cleanup). Import `FALLBACK_ROUTING, L2_ROUTING, ModelRouter` from `agents.model_router`. Use existing test patterns from the file (class-based, `setup_method`, monkeypatch fixtures).

**Important:** Some tests may need to mock `self.router._catalog` to `None` (no health catalog) for simpler assertions, or mock `_is_l1_available` to control the L1 availability path. Follow existing patterns in the test file for how catalog mocking is done.
  </action>
  <verify>
    <automated>cd C:/Users/rickw/projects/agent42 && python -m pytest tests/test_model_router.py::TestL1Routing tests/test_model_router.py::TestL2RoutingUpdates tests/test_model_router.py::TestFallbackChain -x -v</automated>
  </verify>
  <done>
- TestL1Routing class exists with 7+ tests covering L1 resolution, health, self-critique, max_iterations, and ROUTE-02
- TestL2RoutingUpdates class exists with 3+ tests covering L2 self-critique and defaults
- TestFallbackChain class exists with 2+ tests covering chain behavior and backward compatibility
- All new tests pass
- All existing tests still pass
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `python -m pytest tests/test_model_router.py -x -v` -- all router tests pass including new L1/L2/fallback classes
2. `python -m pytest tests/test_dynamic_routing.py tests/test_model_catalog.py tests/test_openclaw_features.py tests/test_project_interview.py -x -q` -- all renamed imports work
3. `python -m pytest tests/ -x -q` -- full suite green
4. `python -c "import ast; tree = ast.parse(open('agents/model_catalog.py').read()); [print('OK') for node in ast.walk(tree) if isinstance(node, ast.Name) and node.id == 'FREE_ROUTING']"` -- should print nothing (no FREE_ROUTING references in model_catalog.py)
5. Verify no remaining FREE_ROUTING references outside of model_router.py alias: `grep -rn "FREE_ROUTING" agents/ tests/ --include="*.py" | grep -v "model_router.py"` should return empty
</verification>

<success_criteria>
- Zero references to FREE_ROUTING outside of model_router.py (only the alias remains)
- TestFreeRoutingUpdates renamed to TestFallbackRoutingEntries
- TestL1Routing covers: resolution, auto-detect, explicit override, health check, self-critique, max_iterations, ROUTE-02
- TestL2RoutingUpdates covers: self-critique for all entries, default models
- TestFallbackChain covers: L1-to-fallback degradation, backward compat without StrongWall
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/workstreams/agent-llm-control/phases/17-tier-routing-architecture/17-02-SUMMARY.md`
</output>
