---
phase: 04-layout-diff-viewer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_cc_layout.py
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04

must_haves:
  truths:
    - "Test file exists with xfail stubs for all LAYOUT requirements"
    - "Tests run and produce expected xfail results (no import errors)"
  artifacts:
    - path: "tests/test_cc_layout.py"
      provides: "Source-inspection tests for LAYOUT-01 through LAYOUT-04"
      min_lines: 80
  key_links:
    - from: "tests/test_cc_layout.py"
      to: "dashboard/frontend/dist/app.js"
      via: "Path(__file__).resolve().parent.parent / 'dashboard' / 'frontend' / 'dist' / 'app.js'"
      pattern: "APP_JS.*read_text"
---

<objective>
Create the Wave 0 test scaffold for Phase 4 Layout + Diff Viewer requirements.

Purpose: Establish RED tests for all LAYOUT-01 through LAYOUT-04 requirements using the established source-inspection pattern from Phases 2 and 3. These tests read app.js and style.css as text and assert required patterns are present.

Output: `tests/test_cc_layout.py` with xfail stubs that will flip to XPASS as Plans 04-02 through 04-04 implement the features.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/custom-claude-code-ui/ROADMAP.md
@.planning/workstreams/custom-claude-code-ui/STATE.md
@.planning/workstreams/custom-claude-code-ui/phases/04-layout-diff-viewer/04-CONTEXT.md
@.planning/workstreams/custom-claude-code-ui/phases/04-layout-diff-viewer/04-RESEARCH.md
@.planning/workstreams/custom-claude-code-ui/phases/04-layout-diff-viewer/04-VALIDATION.md

<interfaces>
<!-- Existing test pattern from test_cc_tool_use.py — replicate exactly -->

From tests/test_cc_tool_use.py:
```python
import json
from pathlib import Path
import pytest

APP_JS = Path(__file__).resolve().parent.parent / "dashboard" / "frontend" / "dist" / "app.js"
STYLE_CSS = Path(__file__).resolve().parent.parent / "dashboard" / "frontend" / "dist" / "style.css"

class TestToolCards:
    def setup_method(self):
        self.src = APP_JS.read_text(encoding="utf-8")

    @pytest.mark.xfail(raises=AssertionError, strict=False, reason="Unimplemented -- Plan 03-03 GREEN flips")
    def test_tool_card_created_on_tool_start(self):
        assert "ccCreateToolCard" in self.src, "app.js must define ccCreateToolCard..."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_cc_layout.py with xfail stubs for LAYOUT-01 through LAYOUT-04</name>
  <files>tests/test_cc_layout.py</files>
  <read_first>
    - tests/test_cc_tool_use.py (source-inspection test pattern to replicate)
    - tests/test_cc_chat_ui.py (additional source-inspection pattern reference)
    - .planning/workstreams/custom-claude-code-ui/phases/04-layout-diff-viewer/04-VALIDATION.md (test map)
    - .planning/workstreams/custom-claude-code-ui/phases/04-layout-diff-viewer/04-RESEARCH.md (function names and patterns to assert)
  </read_first>
  <action>
Create `tests/test_cc_layout.py` with the following structure:

**File header:**
```python
"""Tests for Phase 4 Layout + Diff Viewer -- source inspection of app.js and style.css.

Strategy: identical to tests/test_cc_tool_use.py -- read source files as text and assert
required patterns are present. Tests run RED until implementation plans 04-02 through 04-04
complete.

Requirements covered: LAYOUT-01 through LAYOUT-04.
"""
from pathlib import Path
import pytest

APP_JS = Path(__file__).resolve().parent.parent / "dashboard" / "frontend" / "dist" / "app.js"
STYLE_CSS = Path(__file__).resolve().parent.parent / "dashboard" / "frontend" / "dist" / "style.css"
```

**Class 1: TestCCPanelLayout** (LAYOUT-01, LAYOUT-02, LAYOUT-03) — 6 tests, all xfail:

1. `test_cc_tab_default_path` (LAYOUT-01): Assert `ideOpenCCChat` is in self.src (already exists — will XPASS immediately). This confirms the tab entry point is preserved.

2. `test_panel_container_present` (LAYOUT-02): Assert `"ide-cc-panel"` is in self.src AND `"ide-panel-drag-handle"` is in self.src. These are the new HTML elements in the template.

3. `test_panel_width_persistence` (LAYOUT-02): Assert `"cc_panel_width"` is in self.src (localStorage key for panel width).

4. `test_panel_drag_handle_function` (LAYOUT-02): Assert `"initPanelDragHandle"` is in self.src (the new drag handle init function).

5. `test_toggle_function_defined` (LAYOUT-03): Assert `"ideToggleCCPanel"` is in self.src (the toggle function).

6. `test_panel_mode_flag` (LAYOUT-03): Assert `"_ccPanelMode"` is in self.src (the state flag tracking current mode).

**Class 2: TestCCPanelCSS** (LAYOUT-02) — 2 tests, all xfail:

1. `test_panel_css_classes` (LAYOUT-02): Read STYLE_CSS, assert `.ide-cc-panel` and `.ide-panel-drag-handle` appear in the CSS source.

2. `test_panel_editor_area_wrapper` (LAYOUT-02): Read STYLE_CSS, assert `.ide-main-editor-area` appears in the CSS source (the wrapper div for tabs+editor).

**Class 3: TestDiffViewer** (LAYOUT-04) — 4 tests, all xfail:

1. `test_open_diff_tab_defined` (LAYOUT-04): Assert `"ideOpenDiffTab"` is in self.src.

2. `test_create_diff_editor_used` (LAYOUT-04): Assert `"createDiffEditor"` is in self.src.

3. `test_view_diff_button_on_file_tools` (LAYOUT-04): Assert `"View Diff"` is in self.src (button text on tool cards).

4. `test_diff_uses_agent42_dark_theme` (LAYOUT-04): Assert `"agent42-dark"` appears in self.src at least twice (once for existing Monaco init, once for diff editor). Use `self.src.count('"agent42-dark"') >= 2` or assert `createDiffEditor` and `agent42-dark` both appear near each other. Simpler: assert `"agent42-dark"` in self.src (already true) AND assert `"createDiffEditor"` in self.src (tests diff editor exists with theme available).

**All xfail markers** use: `@pytest.mark.xfail(raises=AssertionError, strict=False, reason="Unimplemented -- Plan 04-NN GREEN flips")` where NN matches the plan that implements it (02, 03, or 04).

**Exception:** `test_cc_tab_default_path` should use `reason="LAYOUT-01: ideOpenCCChat already exists -- expected XPASS"` since the function already exists and this test validates the tab path remains intact.

Each test should have a 1-line docstring referencing the requirement ID (e.g., `"""LAYOUT-02: Panel container div and drag handle present in HTML template."""`).

Each assert should have a failure message string (e.g., `"app.js must contain ide-cc-panel element for panel container"`).
  </action>
  <verify>
    <automated>python -m pytest tests/test_cc_layout.py -x -q</automated>
  </verify>
  <done>
    - tests/test_cc_layout.py exists with 12 tests across 3 classes
    - `python -m pytest tests/test_cc_layout.py -x -q` runs without import errors
    - test_cc_tab_default_path XPASSes (ideOpenCCChat already exists in app.js)
    - Remaining 11 tests xfail (expected — features not yet implemented)
  </done>
  <acceptance_criteria>
    - File `tests/test_cc_layout.py` exists and contains `class TestCCPanelLayout`
    - File contains `class TestCCPanelCSS`
    - File contains `class TestDiffViewer`
    - File contains `"ide-cc-panel"` (panel container assertion)
    - File contains `"ideToggleCCPanel"` (toggle function assertion)
    - File contains `"ideOpenDiffTab"` (diff tab function assertion)
    - File contains `"createDiffEditor"` (Monaco diff editor assertion)
    - File contains `"View Diff"` (tool card button assertion)
    - File contains `"cc_panel_width"` (localStorage persistence assertion)
    - File contains `"_ccPanelMode"` (panel mode flag assertion)
    - `python -m pytest tests/test_cc_layout.py -x -q` exits 0 (all xfail + xpass)
    - `python -m pytest tests/test_cc_layout.py -v` shows 12 test items
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_cc_layout.py -v` shows 12 tests (1 XPASS, 11 xfail)
- `python -m pytest tests/ -x -q` still passes (no regressions)
- `grep -c "def test_" tests/test_cc_layout.py` returns 12
</verification>

<success_criteria>
Wave 0 test scaffold complete. All 12 source-inspection tests exist and run. Implementation plans 04-02 through 04-04 will flip them GREEN.
</success_criteria>

<output>
After completion, create `.planning/workstreams/custom-claude-code-ui/phases/04-layout-diff-viewer/04-01-SUMMARY.md`
</output>
