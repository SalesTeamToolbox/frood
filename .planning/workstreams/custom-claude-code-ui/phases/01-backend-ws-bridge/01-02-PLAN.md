---
phase: 01-backend-ws-bridge
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - dashboard/server.py
autonomous: true
requirements: [BRIDGE-01, BRIDGE-02, BRIDGE-03, BRIDGE-04]

must_haves:
  truths:
    - "A WebSocket client connecting to /ws/cc-chat receives text_delta, tool_start, tool_delta, turn_complete events"
    - "Sending a second message passes --resume cc_session_id to the subprocess"
    - "Disconnecting mid-generation terminates the CC subprocess immediately"
    - "Session metadata written to .agent42/cc-sessions after each turn"
  artifacts:
    - path: "dashboard/server.py"
      provides: "CC WebSocket bridge + NDJSON parser + session helpers"
      contains: "/ws/cc-chat"
    - path: "dashboard/server.py"
      provides: "_parse_cc_event pure function"
      contains: "_parse_cc_event"
    - path: "dashboard/server.py"
      provides: "_save_session and _load_session async helpers"
      contains: "_save_session"
  key_links:
    - from: "dashboard/server.py _parse_cc_event"
      to: "CC subprocess stdout"
      via: "async for line in proc.stdout"
      pattern: "async for proc.stdout"
    - from: "dashboard/server.py cc_chat_ws"
      to: "_parse_cc_event"
      via: "called per NDJSON line in stdout read loop"
      pattern: "_parse_cc_event"
    - from: "dashboard/server.py"
      to: ".agent42/cc-sessions/"
      via: "_save_session called after turn_complete"
      pattern: "_save_session"
---

<objective>
Implement /ws/cc-chat WebSocket endpoint, NDJSON event parser, and session persistence -- addressing BRIDGE-01 through BRIDGE-04.

Purpose: Central Phase 1 deliverable. Spawns CC subprocesses, translates NDJSON to typed WS messages, tracks session state for multi-turn conversations.
Output: /ws/cc-chat endpoint; _parse_cc_event function; _save_session/_load_session helpers; session directory.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/custom-claude-code-ui/REQUIREMENTS.md
@.planning/workstreams/custom-claude-code-ui/phases/01-backend-ws-bridge/01-CONTEXT.md
@.planning/workstreams/custom-claude-code-ui/phases/01-backend-ws-bridge/01-RESEARCH.md
@.planning/workstreams/custom-claude-code-ui/phases/01-backend-ws-bridge/01-01-SUMMARY.md

<interfaces>
From dashboard/server.py -- patterns executor replicates verbatim.

JWT auth (lines 1430-1457):
  After websocket.accept(), get token = websocket.query_params.get("token", "").
  Close code=4001 if missing. _get_user_from_token(token) in try/except; close 4001 on exception.

Disconnect cleanup (lines 1519-1522):
  finally: read_task.cancel(); if proc.returncode is None: proc.terminate()

Subprocess spawn (lines 1661-1667):
  proc = await _asyncio.create_subprocess_exec(*args, stdout=PIPE, stderr=PIPE, cwd=str(workspace))

Existing aliases (do not redefine): _asyncio, _shutil, _json, workspace (Path).
aiofiles installed. SECURITY: args is a Python list. No shell=True.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add _parse_cc_event and session helpers</name>
  <files>dashboard/server.py</files>
  <behavior>
    - See RESEARCH.md Pattern 2 for complete NDJSON routing table
    - _parse_cc_event(event, tool_id_map, session_state) -> list[dict]
    - text_delta from stream_event/content_block_delta
    - tool_start from content_block_start/tool_use
    - tool_delta from input_json_delta
    - turn_complete from result; sets session_state[cc_session_id]
    - empty list for system/init/unknown
    - _save_session: aiofiles JSON write to sessions_dir/{ws_session_id}.json
    - _load_session: aiofiles JSON read; empty dict if missing
  </behavior>
  <action>
Insert into dashboard/server.py BEFORE:
    "    # -- Remote Node Status ----------------------------------------------------"
(~line 1703, after terminal WS finally block)

CODE TO INSERT (4-space indented, inside create_app closure):

    # -- Claude Code Chat WebSocket Bridge -------------------------------------

    import aiofiles as _aiofiles
    from pathlib import Path as _pathlib_Path

    _CC_SESSIONS_DIR = workspace / ".agent42" / "cc-sessions"
    _CC_SESSIONS_DIR.mkdir(parents=True, exist_ok=True)

    def _parse_cc_event(event: dict, tool_id_map: dict, session_state: dict) -> list:
        """Translate one CC NDJSON event into WS envelope dicts.
        Returns [] for events with no WS output (system/init, message_start, etc.).
        Mutates tool_id_map and session_state["cc_session_id"] on result event.
        """
        etype = event.get("type")
        envelopes = []
        if etype == "stream_event":
            raw = event.get("event", {})
            raw_type = raw.get("type", "")
            index = raw.get("index")
            if raw_type == "content_block_start":
                cb = raw.get("content_block", {})
                if cb.get("type") == "tool_use":
                    tool_id_map[index] = {"id": cb["id"], "name": cb["name"]}
                    envelopes.append({"type": "tool_start", "data": {"id": cb["id"], "name": cb["name"], "input": {}}})
            elif raw_type == "content_block_delta":
                delta = raw.get("delta", {})
                if delta.get("type") == "text_delta":
                    envelopes.append({"type": "text_delta", "data": {"text": delta.get("text", "")}})
                elif delta.get("type") == "input_json_delta":
                    t = tool_id_map.get(index, {})
                    envelopes.append({"type": "tool_delta", "data": {"id": t.get("id"), "partial": delta.get("partial_json", "")}})
            elif raw_type == "content_block_stop":
                if index in tool_id_map:
                    t = tool_id_map.pop(index)
                    envelopes.append({"type": "tool_complete", "data": {"id": t.get("id"), "name": t.get("name"), "output": "", "is_error": False}})
        elif etype == "result":
            cc_sid = event.get("session_id")
            session_state["cc_session_id"] = cc_sid
            usage = event.get("usage", {})
            envelopes.append({"type": "turn_complete", "data": {"session_id": cc_sid, "cost_usd": event.get("cost_usd"), "input_tokens": usage.get("input_tokens", 0), "output_tokens": usage.get("output_tokens", 0)}})
        return envelopes

    async def _save_session(ws_session_id: str, data: dict, sessions_dir=None) -> None:
        d = _pathlib_Path(sessions_dir) if sessions_dir is not None else _CC_SESSIONS_DIR
        async with _aiofiles.open(d / f"{ws_session_id}.json", "w") as fh:
            await fh.write(_json.dumps(data, indent=2))

    async def _load_session(ws_session_id: str, sessions_dir=None) -> dict:
        d = _pathlib_Path(sessions_dir) if sessions_dir is not None else _CC_SESSIONS_DIR
        path = d / f"{ws_session_id}.json"
        if not path.exists():
            return {}
        try:
            async with _aiofiles.open(path, "r") as fh:
                return _json.loads(await fh.read())
        except Exception:
            return {}

NOTE: _json alias already defined in terminal block (~line 1468). Do NOT redefine.
  </action>
  <read_first>
    - dashboard/server.py lines 1700-1715 (insertion point before Remote Node Status)
    - dashboard/server.py lines 1425-1430 (existing aliases; _json already defined -- do not redefine)
    - .planning/workstreams/custom-claude-code-ui/phases/01-backend-ws-bridge/01-RESEARCH.md (Pattern 2 NDJSON routing, Pattern 3 session I/O)
    - tests/test_cc_bridge.py (_parse_cc_event signature the tests call)
  </read_first>
  <verify>
    <automated>python -m pytest tests/test_cc_bridge.py::TestNDJSONParser tests/test_cc_bridge.py::TestSessionRegistry -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "_parse_cc_event" dashboard/server.py finds "def _parse_cc_event"
    - grep -n "_save_session" dashboard/server.py finds "async def _save_session"
    - grep -n "_load_session" dashboard/server.py finds "async def _load_session"
    - python -m pytest tests/test_cc_bridge.py::TestNDJSONParser -x -q all green (not xfail)
    - python -m pytest tests/test_cc_bridge.py::TestSessionRegistry -x -q all green
  </acceptance_criteria>
  <done>_parse_cc_event, _save_session, _load_session defined; TestNDJSONParser and TestSessionRegistry green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement /ws/cc-chat WebSocket endpoint</name>
  <files>dashboard/server.py</files>
  <behavior>
    - Route /ws/cc-chat with JWT auth (close 4001 on invalid/missing token)
    - ws_session_id from query param or uuid4
    - Loads prior session for cc_session_id via _load_session
    - Per message: args list = [claude_bin, "-p", user_message, "--output-format", "stream-json", "--verbose", "--include-partial-messages"] + optional ["--resume", cc_session_id]
    - Spawn via create_subprocess_exec PIPE stdout/stderr; never shell=True
    - Read stdout via async-for line-by-line; call _parse_cc_event; send envelopes via send_json
    - After turn: save session via _save_session
    - Disconnect: cancel read task + proc.terminate() in finally block
    - When claude CLI absent: emit status event then text_delta + turn_complete fallback
  </behavior>
  <action>
Insert cc_chat_ws endpoint in dashboard/server.py AFTER _load_session (after its "return {}" line)
and BEFORE "    # -- Remote Node Status" comment.

ENDPOINT TO INSERT:

    @app.websocket("/ws/cc-chat")
    async def cc_chat_ws(websocket: WebSocket):
        """CC Chat WebSocket: per-turn spawn, NDJSON relay, multi-turn via --resume.
        Security: subprocess args is a Python list. No shell=True. user_message is a
        single positional CC argument (not shell-interpolated).
        """
        import uuid as _uuid
        import datetime as _datetime

        await websocket.accept()
        token = websocket.query_params.get("token", "")
        if not token:
            await websocket.close(code=4001, reason="Missing token")
            return
        try:
            _get_user_from_token(token)
        except Exception:
            await websocket.close(code=4001, reason="Invalid token")
            return

        ws_session_id = websocket.query_params.get("session_id") or str(_uuid.uuid4())
        session_data = await _load_session(ws_session_id)
        session_state: dict = {"cc_session_id": session_data.get("cc_session_id")}
        session_title: str = session_data.get("title", "")
        created_at: str = session_data.get("created_at", _datetime.datetime.utcnow().isoformat())

        try:
            while True:
                raw_msg = await websocket.receive_text()
                try:
                    msg_data = _json.loads(raw_msg)
                    user_message = msg_data.get("message", raw_msg)
                except (_json.JSONDecodeError, AttributeError):
                    user_message = raw_msg

                if not user_message.strip():
                    continue
                if not session_title:
                    session_title = user_message[:80]

                claude_bin = _shutil.which("claude")
                if not claude_bin:
                    # BRIDGE-05: CLI not installed -- notify and emit fallback response
                    await websocket.send_json({"type": "status", "data": {"message": "CC subscription not available \u2014 using API mode"}})
                    await websocket.send_json({"type": "text_delta", "data": {"text": "Claude Code CLI not installed. Run: npm install -g @anthropic-ai/claude-code"}})
                    await websocket.send_json({"type": "turn_complete", "data": {"session_id": session_state.get("cc_session_id"), "cost_usd": None, "input_tokens": 0, "output_tokens": 0}})
                    continue

                # Build subprocess args as a Python list (prevents shell injection)
                args = [claude_bin, "-p", user_message, "--output-format", "stream-json", "--verbose", "--include-partial-messages"]
                cc_session_id = session_state.get("cc_session_id")
                if cc_session_id:
                    args += ["--resume", cc_session_id]

                try:
                    proc = await _asyncio.create_subprocess_exec(*args, stdout=_asyncio.subprocess.PIPE, stderr=_asyncio.subprocess.PIPE, cwd=str(workspace))
                except Exception as spawn_err:
                    await websocket.send_json({"type": "error", "data": {"message": f"Failed to start CC: {spawn_err}", "code": "spawn_failed"}})
                    continue

                tool_id_map: dict = {}

                async def _read_stdout():
                    try:
                        async for raw_line in proc.stdout:
                            line = raw_line.decode("utf-8", errors="replace").strip()
                            if not line:
                                continue
                            try:
                                event = _json.loads(line)
                            except _json.JSONDecodeError:
                                continue
                            for envelope in _parse_cc_event(event, tool_id_map, session_state):
                                try:
                                    await websocket.send_json(envelope)
                                except Exception:
                                    return
                    except Exception:
                        pass

                read_task = _asyncio.create_task(_read_stdout())
                try:
                    await read_task
                except Exception:
                    pass
                finally:
                    read_task.cancel()
                    if proc.returncode is None:
                        proc.terminate()

                await _save_session(ws_session_id, {"ws_session_id": ws_session_id, "cc_session_id": session_state.get("cc_session_id"), "created_at": created_at, "last_active_at": _datetime.datetime.utcnow().isoformat(), "title": session_title})

        except Exception:
            pass

KEY POINTS: args is a list (no shell=True). user_message is positional CC arg.
async for raw_line in proc.stdout reads line by line (NDJSON safe). outer while True enables multi-turn.
await read_task blocks until CC exits before next message.
  </action>
  <read_first>
    - dashboard/server.py lines 1700-1720 (insertion after _load_session, before Remote Node Status)
    - dashboard/server.py lines 1447-1457 (JWT auth pattern to replicate)
    - dashboard/server.py lines 1519-1522 (finally cleanup pattern)
    - .planning/workstreams/custom-claude-code-ui/phases/01-backend-ws-bridge/01-CONTEXT.md (locked: per-turn spawn, no keep-alive)
    - .planning/workstreams/custom-claude-code-ui/phases/01-backend-ws-bridge/01-RESEARCH.md (Pitfalls 1-7, Pattern 1 subprocess spawn)
  </read_first>
  <verify>
    <automated>python -m pytest tests/test_cc_bridge.py::TestCCBridgeRouting tests/test_cc_bridge.py::TestMultiTurn tests/test_cc_bridge.py::TestFallback -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "/ws/cc-chat" dashboard/server.py finds the websocket route decorator
    - grep -n "--resume" dashboard/server.py finds resume flag usage in cc_chat_ws
    - grep -n "stream-json" dashboard/server.py finds stream-json arg
    - grep -n "--verbose" dashboard/server.py finds verbose flag
    - grep -n "--include-partial-messages" dashboard/server.py finds flag
    - grep -c shell=True dashboard/server.py returns 0
    - python -m pytest tests/test_cc_bridge.py::TestCCBridgeRouting -x -q all green
    - python -m pytest tests/test_cc_bridge.py::TestMultiTurn -x -q all green
    - python -m pytest tests/test_cc_bridge.py::TestFallback -x -q all green
    - python -m pytest tests/ -x -q full suite passes
  </acceptance_criteria>
  <done>cc_chat_ws defined; TestCCBridgeRouting/TestMultiTurn/TestFallback green; shell=True absent; full suite passes</done>
</task>

</tasks>

<verification>
After both tasks:
1. python -m pytest tests/test_cc_bridge.py -x -q -- all 6 test classes green
2. grep -c "def _parse_cc_event" dashboard/server.py returns 1
3. grep -c "async def cc_chat_ws" dashboard/server.py returns 1
4. grep -c "shell=True" dashboard/server.py returns 0
5. python -m pytest tests/ -x -q full suite passes
</verification>

<success_criteria>
- /ws/cc-chat implemented: args list, JWT auth, per-turn spawn, --resume, disconnect cleanup
- _parse_cc_event handles all CC event types; returns correct WS envelopes
- _save_session/_load_session write/read .agent42/cc-sessions JSON files
- BRIDGE-01, BRIDGE-02, BRIDGE-03, BRIDGE-04 test classes all green
- shell=True absent from dashboard/server.py
</success_criteria>

<output>
After completion, create .planning/workstreams/custom-claude-code-ui/phases/01-backend-ws-bridge/01-02-SUMMARY.md
</output>
