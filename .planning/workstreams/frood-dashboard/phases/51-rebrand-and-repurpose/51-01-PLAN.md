---
phase: 51-rebrand-and-repurpose
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_rebrand_phase51.py
  - dashboard/frontend/dist/app.js
  - dashboard/server.py
  - dashboard/frontend/dist/index.html
  - dashboard/frontend/dist/assets/frood-logo-light.svg
  - dashboard/frontend/dist/assets/frood-avatar.svg
  - dashboard/frontend/dist/assets/frood-favicon.svg
  - dashboard/frontend/dist/assets/frood-logo.svg
autonomous: true
requirements: [BRAND-01, BRAND-03, SET-01, SET-02, SET-03, SET-04]

must_haves:
  truths:
    - "No user-visible 'Sandboxed Apps' text exists — all renamed to 'Agent Apps'"
    - "No user-visible 'Agent42' text exists in app.js (excluding deferred internal keys)"
    - "Server FastAPI title is 'Frood Dashboard'"
    - "SVG assets are renamed from agent42-* to frood-*"
    - "Settings 'Channels' tab is removed"
    - "Settings 'Orchestrator' tab is renamed to 'Routing'"
    - "MAX_CONCURRENT_AGENTS setting is removed from Routing panel"
    - "loadChannels() is removed from loadAll()"
  artifacts:
    - path: "tests/test_rebrand_phase51.py"
      provides: "Automated verification for all Phase 51 requirements"
      contains: "test_agent_apps_renamed"
    - path: "dashboard/frontend/dist/app.js"
      provides: "Rebranded frontend with Agent Apps, Frood branding, cleaned Settings"
    - path: "dashboard/server.py"
      provides: "Frood-branded FastAPI server"
      contains: 'title="Frood Dashboard"'
    - path: "dashboard/frontend/dist/assets/frood-logo-light.svg"
      provides: "Renamed logo asset"
    - path: "dashboard/frontend/dist/assets/frood-favicon.svg"
      provides: "Renamed favicon asset"
  key_links:
    - from: "dashboard/frontend/dist/app.js"
      to: "dashboard/frontend/dist/assets/frood-logo-light.svg"
      via: "img src reference"
      pattern: "frood-logo-light\\.svg"
    - from: "dashboard/frontend/dist/index.html"
      to: "dashboard/frontend/dist/assets/frood-favicon.svg"
      via: "link href reference"
      pattern: "frood-favicon\\.svg"
---

<objective>
Rebrand the dashboard from Agent42 to Frood and clean up Settings tabs.

Purpose: Establish Frood identity across all user-visible surfaces, remove dead Channels tab, rename Orchestrator to Routing, and create the test scaffold that validates all Phase 51 requirements.
Output: Rebranded app.js + server.py, renamed SVG assets, cleaned Settings, test file for all Phase 51 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frood-dashboard/ROADMAP.md
@.planning/workstreams/frood-dashboard/REQUIREMENTS.md
@.planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-CONTEXT.md
@.planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 51 test scaffold</name>
  <files>tests/test_rebrand_phase51.py</files>
  <read_first>
    - tests/test_settings_ui.py (pattern: module-level file read, string assertions)
    - dashboard/frontend/dist/app.js (verify current strings to assert against)
    - dashboard/server.py (verify current title string)
  </read_first>
  <action>
Create `tests/test_rebrand_phase51.py` following the exact pattern from `tests/test_settings_ui.py` — read files at module level using `pathlib.Path(...).read_text(encoding="utf-8")`, then assert string presence/absence.

Read these files at module level:
```python
_APP_JS = pathlib.Path("dashboard/frontend/dist/app.js").read_text(encoding="utf-8")
_SERVER_PY = pathlib.Path("dashboard/server.py").read_text(encoding="utf-8")
_INDEX_HTML = pathlib.Path("dashboard/frontend/dist/index.html").read_text(encoding="utf-8")
_README = pathlib.Path("README.md").read_text(encoding="utf-8")
```

Create these test classes and methods:

**class TestBranding:**
- `test_agent_apps_renamed`: assert `"Agent Apps"` in `_APP_JS`, assert `"Sandboxed Apps"` not in `_APP_JS`
- `test_no_agent42_visible`: Count occurrences of `"Agent42"` in `_APP_JS` excluding lines with `agent42_token`, `agent42_auth`, `.agent42/` — assert count == 0. Implementation: split `_APP_JS` by newlines, filter out lines containing `agent42_token` or `agent42_auth` or `.agent42/`, rejoin and assert `"Agent42"` not in filtered text.
- `test_server_title`: assert `'title="Frood Dashboard"'` in `_SERVER_PY`
- `test_frood_logo_references`: assert `"frood-logo-light.svg"` in `_APP_JS`, assert `"agent42-logo-light.svg"` not in `_APP_JS`
- `test_frood_favicon`: assert `"frood-favicon.svg"` in `_INDEX_HTML`, assert `"agent42-favicon.svg"` not in `_INDEX_HTML`
- `test_frood_avatar`: assert `"frood-avatar.svg"` in `_APP_JS`, assert `"agent42-avatar.svg"` not in `_APP_JS`

**class TestSidebarNav:**
- `test_sidebar_has_activity`: assert `'data-page="activity"'` in `_APP_JS` (this will fail until Plan 03 — mark with `pytest.mark.xfail(reason="Activity page added in Plan 03")`)
- `test_sidebar_no_channels`: assert `'data-page="channels"'` not in `_APP_JS`

**class TestSettingsCleanup:**
- `test_channels_tab_removed`: assert `'"channels"'` not in settings tabs section. Use: assert `'id: "channels"'` not in `_APP_JS`
- `test_routing_tab`: assert `'"routing"'` in `_APP_JS` and `'label: "Routing"'` in `_APP_JS`
- `test_orchestrator_label_gone`: assert `'label: "Orchestrator"'` not in `_APP_JS`
- `test_max_concurrent_removed`: assert `"MAX_CONCURRENT_AGENTS"` not in `_APP_JS`
- `test_load_channels_removed`: Find the `loadAll()` function text. Assert `"loadChannels"` not in `_APP_JS`.

**class TestReportsTabs:**
- `test_tasks_tab_removed`: assert `'"Tasks & Projects"'` not in `_APP_JS` (xfail until Plan 02)
- `test_tasks_renderer_removed`: assert `"_renderReportsTasks"` not in `_APP_JS` (xfail until Plan 02)
- `test_health_tab_present`: assert `'"System Health"'` in `_APP_JS` or `'"health"'` in `_APP_JS`
- `test_intelligence_overview`: assert `"memory_recall"` in `_APP_JS` or `"memoryStats"` in `_APP_JS` (xfail until Plan 02)

**class TestActivityFeed:**
- `test_activity_renderer`: assert `"renderActivity"` in `_APP_JS` (xfail until Plan 03)
- `test_intelligence_event_types`: assert `"intelligence_event"` in `_APP_JS` (xfail until Plan 03)
- `test_activity_endpoint`: assert `"/api/activity"` in `_SERVER_PY` (xfail until Plan 03)

**class TestSetupWizard:**
- `test_setup_wizard_no_mission_control`: assert `"Mission Control"` not in `_APP_JS` (xfail until Plan 04)

**class TestReadme:**
- `test_readme_no_harness_terms`: for term in `["Mission Control", "Agent Teams", "Agents page"]`: assert term not in `_README` (xfail until Plan 04)

Mark tests that depend on later plans with `@pytest.mark.xfail(reason="Implemented in Plan NN")` so they do not block earlier plans. Remove xfail marks as each plan completes.
  </action>
  <verify>
    <automated>python -m pytest tests/test_rebrand_phase51.py -x -q 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_rebrand_phase51.py exists and has classes TestBranding, TestSettingsCleanup, TestReportsTabs, TestActivityFeed, TestSetupWizard, TestReadme
    - `python -m pytest tests/test_rebrand_phase51.py --co -q` lists at least 20 test items
    - Branding tests initially fail (Agent42 text still present), xfail tests show xfail
  </acceptance_criteria>
  <done>Test scaffold exists with all Phase 51 requirement checks. Branding/Settings tests fail (RED), later-plan tests xfail.</done>
</task>

<task type="auto">
  <name>Task 2: Branding sweep + SVG rename + Settings cleanup</name>
  <files>
    dashboard/frontend/dist/app.js
    dashboard/server.py
    dashboard/frontend/dist/index.html
    dashboard/frontend/dist/assets/frood-logo-light.svg
    dashboard/frontend/dist/assets/frood-avatar.svg
    dashboard/frontend/dist/assets/frood-favicon.svg
    dashboard/frontend/dist/assets/frood-logo.svg
  </files>
  <read_first>
    - dashboard/frontend/dist/app.js (full file — understand every Agent42/Sandboxed occurrence)
    - dashboard/server.py (line 273 for title, line 309 for disabled message)
    - dashboard/frontend/dist/index.html (line 16 for favicon)
    - .planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-RESEARCH.md (File-by-File Change Map section for exact line locations)
  </read_first>
  <action>
**Branding sweep in app.js (per D-11, D-13):**

Replace these exact strings in app.js (approximately 20 occurrences):
1. Line 0 comment: `/* Agent42 Dashboard` to `/* Frood Dashboard`
2. Line 71: `agent42-avatar.svg` to `frood-avatar.svg`
3. Line 335: `agent42-logo-light.svg` to `frood-logo-light.svg`
4. Line 624: `"Restart Agent42"` to `"Restart Frood"`
5. Line 870: `"Internal (Agent42 system tool)"` to `"Internal (Frood system tool)"`
6. Line 1103: `"What do sandboxed apps get from Agent42?"` to `"What do Agent Apps get from Frood?"`
7. Line 1280: `"Model routing is handled by Agent42's tiered routing."` to `"Model routing is handled by Frood's tiered routing."`
8. Line 1320: All `"Agent42 operates as..."` to `"Frood operates as..."`
9. Line 1486: `"Agent42 routes tasks..."` to `"Frood routes LLM requests..."`
10. Lines 1660, 1672, 1726, 1729, 1735, 1739-1743, 1775: All `"Agent42"` to `"Frood"` in storage/memory descriptions
11. Lines 2023, 2050: `agent42-logo-light.svg` to `frood-logo-light.svg`
12. Line 2052: `"Sandboxed Apps"` to `"Agent Apps"` in sidebar nav link text
13. Line 2071: `apps: "Sandboxed Apps"` to `apps: "Agent Apps"` in topbar title map

**DO NOT change:** `agent42_token` (localStorage key), `agent42_auth` (BroadcastChannel), `.agent42/` paths, Python logger names. These are deferred per D-15.

**Strategy:** Use find-and-replace for `Agent42` -> `Frood` on user-visible strings only. Then separately replace `Sandboxed Apps` -> `Agent Apps`. Then replace all `agent42-logo-light.svg` -> `frood-logo-light.svg`, `agent42-avatar.svg` -> `frood-avatar.svg`, `agent42-favicon.svg` -> `frood-favicon.svg`. After all replacements, manually verify that `agent42_token`, `agent42_auth`, and `.agent42/` occurrences are untouched.

**Branding sweep in server.py (per D-12):**
- Line 273: Change `title="Agent42 Dashboard"` to `title="Frood Dashboard"`
- Line 309: Change `"Agent42 dashboard UI is disabled"` to `"Frood dashboard UI is disabled"`
- Do NOT change logger names like `logging.getLogger("agent42.server")`

**Favicon in index.html:**
- Line 16: Change `agent42-favicon.svg` to `frood-favicon.svg`

**SVG file renames (per D-14):**
```bash
cd dashboard/frontend/dist/assets/
mv agent42-logo-light.svg frood-logo-light.svg
mv agent42-avatar.svg frood-avatar.svg
mv agent42-favicon.svg frood-favicon.svg
mv agent42-logo.svg frood-logo.svg
```

**Settings cleanup in app.js (per D-16, D-17, D-18, D-19, D-20):**

1. **Remove Channels tab** (line 1449): Delete `{ id: "channels", label: "Channels" },` from the tabs array
2. **Rename Orchestrator tab** (line 1451): Change `{ id: "orchestrator", label: "Orchestrator" }` to `{ id: "routing", label: "Routing" }`
3. **Update panels object**: Find the `orchestrator:` key in the panels object (around line 1658-1682) and rename to `routing:`. Update the h3 heading from `"Orchestrator"` to `"Routing"`. Update description text from `"Controls how Agent42 processes tasks"` to `"Controls how Frood routes LLM requests"`.
4. **Remove MAX_CONCURRENT_AGENTS**: Find and delete the `settingReadonly("MAX_CONCURRENT_AGENTS", ...)` line from inside the routing panel (around line 1662).
5. **Remove loadChannels()**: In `loadAll()` at line 2007, remove `loadChannels(),` from the Promise.all array. The resulting line should be: `loadTools(), loadSkills(), loadProviders(),`
6. **Delete loadChannels function**: Find the `async function loadChannels()` function (around lines 551-554) and delete it entirely.
7. **Delete the channels panel rendering code**: Find the channels case in the panels object (around lines 1589-1612) and delete it.

**CAUTION per Pitfall 1:** When renaming `orchestrator` to `routing`, update BOTH the tabs array entry AND the panels object key. These are at different locations in the file.

**CAUTION per Pitfall 4:** Do NOT modify `tests/test_settings_ui.py` Paperclip assertions. Only app.js changes happen this task.
  </action>
  <verify>
    <automated>python -m pytest tests/test_rebrand_phase51.py::TestBranding tests/test_rebrand_phase51.py::TestSettingsCleanup -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Sandboxed Apps" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "Agent Apps" dashboard/frontend/dist/app.js` returns at least 2 (sidebar + title map)
    - `grep "title=" dashboard/server.py | grep -c "Frood Dashboard"` returns 1
    - `ls dashboard/frontend/dist/assets/frood-logo-light.svg` succeeds
    - `ls dashboard/frontend/dist/assets/frood-avatar.svg` succeeds
    - `ls dashboard/frontend/dist/assets/frood-favicon.svg` succeeds
    - `ls dashboard/frontend/dist/assets/frood-logo.svg` succeeds
    - `ls dashboard/frontend/dist/assets/agent42-logo-light.svg` fails (file no longer exists)
    - `grep -c "loadChannels" dashboard/frontend/dist/app.js` returns 0
    - `grep 'id: "channels"' dashboard/frontend/dist/app.js` returns no matches
    - `grep 'id: "routing"' dashboard/frontend/dist/app.js` returns a match
    - `grep "MAX_CONCURRENT_AGENTS" dashboard/frontend/dist/app.js` returns no matches
    - `python -m pytest tests/ -x -q` passes (full suite green)
  </acceptance_criteria>
  <done>All user-visible "Agent42" text replaced with "Frood", "Sandboxed Apps" replaced with "Agent Apps", SVG files renamed, Settings Channels tab removed, Orchestrator renamed to Routing, MAX_CONCURRENT_AGENTS removed, loadChannels removed. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_rebrand_phase51.py::TestBranding -v` — all pass
- `python -m pytest tests/test_rebrand_phase51.py::TestSettingsCleanup -v` — all pass
- `python -m pytest tests/ -x -q` — full suite green
- `grep -rn "Agent42" dashboard/frontend/dist/app.js | grep -v "agent42_token\|agent42_auth\|\.agent42/"` — no matches
- `grep -rn "Sandboxed Apps" dashboard/frontend/dist/app.js` — no matches
- `ls dashboard/frontend/dist/assets/frood-*.svg` — 4 files exist
- `ls dashboard/frontend/dist/assets/agent42-*.svg` — no files exist
</verification>

<success_criteria>
1. Zero user-visible "Agent42" text in app.js (deferred internal keys excluded)
2. Zero "Sandboxed Apps" text anywhere in app.js
3. SVG assets renamed from agent42-* to frood-*
4. Settings has no Channels tab, Orchestrator renamed to Routing, no MAX_CONCURRENT_AGENTS
5. loadChannels() removed from codebase
6. Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-01-SUMMARY.md`
</output>
