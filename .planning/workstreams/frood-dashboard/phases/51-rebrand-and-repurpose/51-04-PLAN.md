---
phase: 51-rebrand-and-repurpose
plan: "04"
type: execute
wave: 4
depends_on: ["51-03"]
files_modified:
  - dashboard/frontend/dist/app.js
  - README.md
  - tests/test_rebrand_phase51.py
autonomous: true
requirements: [BRAND-04, CLEAN-05]

must_haves:
  truths:
    - "Setup wizard text reflects Frood-as-service intelligence layer identity"
    - "No 'Mission Control' text in setup wizard"
    - "README describes Frood Dashboard as intelligence layer admin panel"
    - "No harness terms (Mission Control, Agent Teams, orchestrator) in README"
  artifacts:
    - path: "dashboard/frontend/dist/app.js"
      provides: "Updated setup wizard copy"
    - path: "README.md"
      provides: "Frood Dashboard README"
      contains: "Frood Dashboard"
    - path: "tests/test_rebrand_phase51.py"
      provides: "All xfail markers removed, all tests pass"
  key_links:
    - from: "README.md"
      to: "dashboard/frontend/dist/assets/frood-logo.svg"
      via: "image reference"
      pattern: "frood-logo"
---

<objective>
Update setup wizard copy for Frood identity and rewrite README for intelligence layer admin panel.

Purpose: Complete the branding work by updating the last two user-visible surfaces: the first-run setup wizard and the project README. Remove all harness/orchestrator language.
Output: Updated setup wizard text in app.js, rewritten README.md, all Phase 51 tests passing (no xfails remaining).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frood-dashboard/ROADMAP.md
@.planning/workstreams/frood-dashboard/REQUIREMENTS.md
@.planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-CONTEXT.md
@.planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-RESEARCH.md
@.planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-01-SUMMARY.md
@.planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-02-SUMMARY.md
@.planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update setup wizard copy for Frood identity</name>
  <files>dashboard/frontend/dist/app.js</files>
  <read_first>
    - dashboard/frontend/dist/app.js (lines 320-421 for renderSetupWizard function — read the full function to understand all steps and text)
    - .planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-RESEARCH.md (Pitfall 8 for Mission Control reference; D-21 for requirements)
  </read_first>
  <action>
**Update setup wizard (per D-21, BRAND-04):**

Find the `renderSetupWizard()` function (around lines 320-421). Make these specific text changes:

1. **Step 1 subtitle/tagline** (around line 335): The current tagline is "The answer to life, the universe, and all your tasks." Change to something like: "The answer to life, the universe, and intelligent tools." (Keep the H2G2 flavor but remove "tasks" which implies orchestration.)

2. **Step 1 description** (around line 336-338): If there is a paragraph describing what Frood does, update it to emphasize the intelligence layer: memory system, tool effectiveness tracking, provider routing, and learning extraction. Example: "Frood is your intelligence layer — memory recall, learning extraction, tool effectiveness tracking, and smart provider routing for your AI workflows."

3. **Step 4 completion text** (around line 411-418): Change "Loading Mission Control..." to "Loading Frood Dashboard..." or "Launching Frood Dashboard...". Remove any references to tasks, agents, or orchestration in the completion screen.

4. **Any other "Agent42" references** in the wizard that were not caught by Plan 01's branding sweep: search the entire function for "Agent42" and replace with "Frood".

5. **Logo reference**: If the wizard references `agent42-logo-light.svg` (line 335), it should already be `frood-logo-light.svg` from Plan 01. Verify this is the case.

**Claude's discretion on exact wording** — keep the Hitchhiker's Guide tone (towels, "Don't Panic", "mostly harmless") but remove orchestrator/harness language. The wizard should position Frood as an intelligence layer service, not a task runner.
  </action>
  <verify>
    <automated>python -m pytest tests/test_rebrand_phase51.py::TestSetupWizard -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "Mission Control" dashboard/frontend/dist/app.js returns 0 matches
    - grep -i "Loading.*Dashboard\|Launching.*Dashboard" dashboard/frontend/dist/app.js returns at least 1 match (the step 4 completion text)
    - The setup wizard function contains no "Agent42" text (verify: grep "Agent42" in the renderSetupWizard function only)
    - python -m pytest tests/ -x -q passes
  </acceptance_criteria>
  <done>Setup wizard text updated for Frood-as-service intelligence layer identity. No "Mission Control" or "Agent42" references in wizard.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README for Frood Dashboard intelligence layer</name>
  <files>README.md, tests/test_rebrand_phase51.py</files>
  <read_first>
    - README.md (full file — understand current structure and content to identify what to keep vs remove)
    - .planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-RESEARCH.md (CLEAN-05 section, Pitfall 6 for agent42-logo.svg rename)
    - .planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-CONTEXT.md (D-22 for requirements)
  </read_first>
  <action>
**Rewrite README.md (per D-22, CLEAN-05):**

The current README describes Agent42 as an orchestrator with teams, mission control, agent profiles, chat, multi-node orchestration. Rewrite it to describe Frood Dashboard as an intelligence layer admin panel.

**Structure for new README:**

```markdown
# Frood Dashboard

> Don't Panic. Your towel knows what it's doing.

Frood is the intelligence layer for AI workflows — an admin and observability dashboard for memory, learning, tool effectiveness, and provider routing.

![Frood Dashboard](dashboard/frontend/dist/assets/frood-logo.svg)

## What Frood Does

- **Memory System** — ONNX + Qdrant semantic memory with auto-sync from Claude Code. Recall relevant context across sessions.
- **Learning Extraction** — Tracks what works and what doesn't. Captures task outcomes and tool effectiveness for continuous improvement.
- **Provider Routing** — Tiered LLM routing (L1 workhorse, free fallback, L2 premium) across 9 providers. No single outage stops your workflows.
- **Tool & Skill Registry** — MCP tools and skills with effectiveness scoring. See what's loaded, what's working, and what's not.
- **Agent Apps** — Sandboxed application containers with Frood intelligence access.
- **Activity Feed** — Real-time observability of memory recalls, routing decisions, learning extractions, and effectiveness scores.

## Quick Start

```bash
# Clone and set up
git clone <repo-url>
cd agent42
python -m venv .venv
source .venv/bin/activate  # or .venv\Scripts\activate on Windows
pip install -r requirements.txt

# Configure
cp .env.example .env
# Edit .env with your API keys and settings

# Run
python agent42.py
# Dashboard at http://localhost:8000
```

## Dashboard Pages

| Page | Purpose |
|------|---------|
| **Agent Apps** | Launch and manage sandboxed applications with Frood intelligence |
| **Tools** | View registered MCP tools, status, and effectiveness |
| **Skills** | Browse loaded skills and their capabilities |
| **Reports** | Intelligence metrics (memory, effectiveness, routing) and system health |
| **Activity** | Real-time feed of intelligence events |
| **Settings** | API keys, security, routing config, storage, memory & learning |

## Architecture

Frood runs as a FastAPI server with a hand-written vanilla JS SPA dashboard. Key components:

- **Server** (`dashboard/server.py`) — FastAPI with JWT auth, REST API, WebSocket
- **Frontend** (`dashboard/frontend/dist/`) — Single-page app, no build step
- **Memory** (`core/memory/`) — ONNX embeddings + Qdrant vector store
- **Providers** (`core/providers/`) — Multi-provider LLM routing
- **Effectiveness** (`core/effectiveness/`) — Tool and model outcome tracking

## Configuration

See `.env.example` for all configuration options. Key settings:

- `DASHBOARD_PASSWORD_HASH` — bcrypt hash for dashboard login
- `JWT_SECRET` — Secret for JWT token signing
- `QDRANT_*` — Vector database connection
- Provider API keys (StrongWall, OpenRouter, Anthropic, OpenAI, etc.)

## Development

```bash
python -m pytest tests/ -x -q    # Run tests
make lint                        # Lint with ruff
make format                      # Auto-format
make check                       # Lint + tests
```

## License

MIT
```

**Key changes from current README:**
- Remove ALL references to: "Mission Control", "Agent Teams", "Agents page", "orchestrator", "harness", "Workspace/IDE", "chat", "multi-node", "device gateway", "approvals"
- Replace all "Agent42" with "Frood" (in user-visible headings and text)
- Update image reference from `agent42-logo.svg` to `frood-logo.svg` (file was renamed in Plan 01)
- Keep technical accuracy — describe what actually exists post-Phase 50/51

**Claude's discretion on:** Section depth, exact wording, whether to include API documentation section, whether to add Contributing section.

**Update tests:**

In tests/test_rebrand_phase51.py, remove the @pytest.mark.xfail decorators from:
- TestSetupWizard::test_setup_wizard_no_mission_control
- TestReadme::test_readme_no_harness_terms

After removing all xfail markers, verify that NO xfail markers remain anywhere in the test file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_rebrand_phase51.py -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "Mission Control" README.md returns 0 matches
    - grep "Agent Teams" README.md returns 0 matches
    - grep "Agents page" README.md returns 0 matches
    - grep "orchestrator" README.md returns 0 matches (case-insensitive check: grep -i "orchestrator" README.md)
    - grep "Frood Dashboard" README.md returns at least 1 match
    - grep "frood-logo" README.md returns at least 1 match
    - grep "xfail" tests/test_rebrand_phase51.py returns 0 matches (all xfails removed)
    - python -m pytest tests/test_rebrand_phase51.py -v -- all tests pass, no xfail, no skip
    - python -m pytest tests/ -x -q -- full suite green
  </acceptance_criteria>
  <done>README rewritten for Frood Dashboard intelligence layer. Setup wizard updated. All Phase 51 tests pass with no xfail markers remaining. Phase 51 complete.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/test_rebrand_phase51.py -v -- ALL tests pass (no xfail, no skip)
- python -m pytest tests/ -x -q -- full suite green
- grep "Mission Control" README.md dashboard/frontend/dist/app.js -- 0 matches in both
- grep "Agent Teams" README.md -- 0 matches
- grep "Frood Dashboard" README.md -- at least 1 match
- grep "xfail" tests/test_rebrand_phase51.py -- 0 matches
</verification>

<success_criteria>
1. Setup wizard uses Frood identity language (intelligence layer, memory, tools, routing)
2. No "Mission Control" text anywhere in app.js or README
3. README describes Frood Dashboard as intelligence layer admin panel
4. No harness terms in README (orchestrator, Agent Teams, Agents page)
5. All Phase 51 tests pass with zero xfails
6. Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/workstreams/frood-dashboard/phases/51-rebrand-and-repurpose/51-04-SUMMARY.md`
</output>
