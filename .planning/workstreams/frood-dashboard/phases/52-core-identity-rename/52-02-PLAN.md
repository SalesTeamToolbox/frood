---
phase: 52-core-identity-rename
plan: 02
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - mcp_server.py
  - scripts/setup_helpers.py
  - .mcp.json
  - .mcp.available.json
  - tools/video_gen.py
  - tools/image_gen.py
  - tools/browser_tool.py
  - core/portability.py
  - core/worktree_manager.py
  - core/task_context.py
  - memory/embeddings.py
  - dashboard/server.py
  - dashboard/server.py
  - core/agent_runtime.py
  - core/key_store.py
  - core/repo_manager.py
  - core/reward_system.py
  - core/rewards_config.py
  - core/url_policy.py
  - core/github_accounts.py
  - core/approval_gate.py
  - memory/effectiveness.py
  - memory/qdrant_store.py
  - memory/search_service.py
  - memory/consolidation_worker.py
  - tools/node_sync.py
  - tools/context_assembler.py
  - tools/project_interview.py
  - tools/app_test_tool.py
  - tools/security_audit.py
  - tools/behaviour_tool.py
  - scripts/check_openrouter_key.py
  - migrate.py
  - tests/e2e/config.py
  - tests/e2e/discovery.py
  - tests/e2e/runner.py
autonomous: true
requirements: [ENTRY-03, PY-03, PY-04, DATA-03]

must_haves:
  truths:
    - "No AGENT42_* env var reads remain in any Python source file"
    - "MCP server uses _FROOD_ROOT variable and SERVER_NAME = 'frood'"
    - "CLAUDE.md marker injection uses FROOD_MEMORY markers"
    - "No .agent42/ hardcoded path references remain in source files (excluding hooks and unit tests)"
    - ".mcp.json and .mcp.available.json use FROOD_WORKSPACE"
  artifacts:
    - path: "mcp_server.py"
      provides: "MCP server with frood references"
      contains: "_FROOD_ROOT"
    - path: "scripts/setup_helpers.py"
      provides: "CLAUDE.md injection with FROOD MEMORY markers"
      contains: "FROOD MEMORY"
    - path: ".mcp.json"
      provides: "Active MCP config with FROOD_WORKSPACE"
      contains: "FROOD_WORKSPACE"
    - path: ".mcp.available.json"
      provides: "Full MCP catalog with frood server names and FROOD_WORKSPACE"
      contains: "FROOD_WORKSPACE"
  key_links:
    - from: "mcp_server.py"
      to: "FROOD_WORKSPACE env var"
      via: "os.environ.get"
      pattern: "FROOD_WORKSPACE"
    - from: "scripts/setup_helpers.py"
      to: "CLAUDE.md files"
      via: "marker injection constants"
      pattern: "BEGIN FROOD MEMORY"
    - from: ".mcp.available.json"
      to: "mcp_server.py"
      via: "env vars passed to MCP server process"
      pattern: "FROOD_WORKSPACE"
---

<objective>
Rename all AGENT42_* environment variable reads to FROOD_* across all Python source files (clean break, no fallback per D-01). Fully rename mcp_server.py internals. Update CLAUDE.md marker injection. Update MCP config files. Replace all remaining .agent42/ hardcoded path references in source files.

Purpose: Complete the env var identity rename and eliminate all agent42 references from production source code (except hooks/tests handled by Plan 03).
Output: All source files use FROOD_* env vars, .frood/ paths, and frood identifiers.
</objective>

<execution_context>
@C:/Users/rickw/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rickw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frood-dashboard/ROADMAP.md
@.planning/workstreams/frood-dashboard/REQUIREMENTS.md
@.planning/workstreams/frood-dashboard/phases/52-core-identity-rename/52-CONTEXT.md
@.planning/workstreams/frood-dashboard/phases/52-core-identity-rename/52-RESEARCH.md

@mcp_server.py
@scripts/setup_helpers.py
@.mcp.json
@.mcp.available.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename AGENT42_* env vars in all source files + .agent42/ paths in source files</name>
  <files>
    tools/video_gen.py, tools/image_gen.py, tools/browser_tool.py,
    core/portability.py, core/worktree_manager.py, core/task_context.py,
    memory/embeddings.py, dashboard/server.py,
    core/agent_runtime.py, core/key_store.py, core/repo_manager.py,
    core/reward_system.py, core/rewards_config.py, core/url_policy.py,
    core/github_accounts.py, core/approval_gate.py,
    memory/effectiveness.py, memory/qdrant_store.py, memory/search_service.py,
    memory/consolidation_worker.py, tools/node_sync.py, tools/context_assembler.py,
    tools/project_interview.py, tools/app_test_tool.py, tools/security_audit.py,
    tools/behaviour_tool.py, scripts/check_openrouter_key.py, migrate.py,
    tests/e2e/config.py, tests/e2e/discovery.py, tests/e2e/runner.py,
    .mcp.json, .mcp.available.json
  </files>
  <read_first>
    - tools/video_gen.py (find AGENT42_VIDEO_MODEL string constant)
    - tools/image_gen.py (find AGENT42_IMAGE_MODEL string constant)
    - tools/browser_tool.py (find _AGENT42_BROWSER_TOKEN)
    - core/portability.py (find AGENT42_WORKTREE_DIR and .agent42/ paths -- 14 refs)
    - core/worktree_manager.py (find AGENT42_WORKTREE_DIR)
    - core/task_context.py (find AGENT42_DATA_DIR and .agent42/)
    - memory/embeddings.py (find AGENT42_WORKSPACE and .agent42/ paths)
    - dashboard/server.py (find .agent42/ path refs -- 6 occurrences)
    - scripts/setup_helpers.py (find AGENT42_WORKSPACE)
    - migrate.py (find .agent42/ refs)
    - tests/e2e/config.py (find AGENT42_ROOT)
    - tests/e2e/discovery.py (find AGENT42_ROOT)
    - tests/e2e/runner.py (find AGENT42_ROOT)
    - .mcp.json (find AGENT42_WORKSPACE)
    - .mcp.available.json (find AGENT42_WORKSPACE, agent42 server key names)
  </read_first>
  <action>
**Per D-01: Clean break. No fallback. Straight rename of AGENT42_* to FROOD_*.**

**Env var renames in Python source files (ENTRY-03):**

1. `tools/video_gen.py`: Find `ADMIN_OVERRIDE_ENV` constant or `"AGENT42_VIDEO_MODEL"` string. Change to `"FROOD_VIDEO_MODEL"`.

2. `tools/image_gen.py`: Find `ADMIN_OVERRIDE_ENV` constant or `"AGENT42_IMAGE_MODEL"` string. Change to `"FROOD_IMAGE_MODEL"`.

3. `tools/browser_tool.py`: Find `_AGENT42_BROWSER_TOKEN` (sets env var). Change to `_FROOD_BROWSER_TOKEN`.

4. `core/portability.py`: Change `"AGENT42_WORKTREE_DIR"` to `"FROOD_WORKTREE_DIR"`. Also change all `.agent42/` path strings to `.frood/` (~14 occurrences -- backup/restore paths like `.agent42/memory`, `.agent42/sessions`, `.agent42/settings.json`, `.agent42/approvals.jsonl`, `.agent42/outputs`, `.agent42/templates`, `.agent42/images`).

5. `core/worktree_manager.py`: Change `"AGENT42_WORKTREE_DIR"` to `"FROOD_WORKTREE_DIR"`. Change any `.agent42/` to `.frood/`.

6. `core/task_context.py`: Change `"AGENT42_DATA_DIR"` to `"FROOD_DATA_DIR"`. Change `.agent42/` to `.frood/`.

7. `memory/embeddings.py`: Change `"AGENT42_WORKSPACE"` to `"FROOD_WORKSPACE"`. Change `.agent42/` paths to `.frood/`.

8. `scripts/setup_helpers.py`: Change `"AGENT42_WORKSPACE"` to `"FROOD_WORKSPACE"` (in MCP config generation). Change any `.agent42/` to `.frood/`.

9. `scripts/check_openrouter_key.py`: Change any `.agent42/` to `.frood/`.

**.agent42/ path renames in remaining source files (DATA-03):**

10. `dashboard/server.py`: Change all `.agent42/` to `.frood/` — specifically:
    - `".agent42/qdrant"` default in QDRANT_LOCAL_PATH
    - `workspace / ".agent42" / "MEMORY.md"` 
    - `workspace / ".agent42" / "cc-sync-status.json"`
    - `workspace / ".agent42" / "consolidation-status.json"`
    - Comment about `.agent42/` paths
    - `"passed from agent42.py"` comment becomes `"passed from frood.py"`

11. `core/agent_runtime.py`: Change `.agent42/` to `.frood/`.
12. `core/key_store.py`: Change `.agent42/` to `.frood/` (~2 refs).
13. `core/repo_manager.py`: Change `.agent42/` to `.frood/` (~2 refs).
14. `core/reward_system.py`: Change `.agent42/` to `.frood/`.
15. `core/rewards_config.py`: Change `.agent42/` to `.frood/` (~2 refs).
16. `core/url_policy.py`: Change `.agent42/` to `.frood/`.
17. `core/github_accounts.py`: Change `.agent42/` to `.frood/` (~2 refs).
18. `core/approval_gate.py`: Change `.agent42/` to `.frood/`.
19. `memory/effectiveness.py`: Change `.agent42/` to `.frood/`.
20. `memory/qdrant_store.py`: Change `.agent42/` to `.frood/`.
21. `memory/search_service.py`: Change `.agent42/` to `.frood/` (~2 refs).
22. `memory/consolidation_worker.py`: Change `.agent42/` to `.frood/`.
23. `tools/node_sync.py`: Change `.agent42/` to `.frood/` (~4 refs).
24. `tools/context_assembler.py`: Change `.agent42/` to `.frood/`.
25. `tools/project_interview.py`: Change `.agent42/` to `.frood/`.
26. `tools/app_test_tool.py`: Change `.agent42/` to `.frood/`.
27. `tools/security_audit.py`: Change `.agent42/` to `.frood/` (~5 refs).
28. `tools/behaviour_tool.py`: Change `.agent42/` to `.frood/`.
29. `migrate.py`: Change `.agent42/` to `.frood/` (~6 refs). Also rename any `agent42_db` argument names if present.

**E2E test config files (AGENT42_ROOT env var):**

30. `tests/e2e/config.py`: Change `AGENT42_ROOT` to `FROOD_ROOT`. Change `.agent42/` to `.frood/` (~1 ref + 4 AGENT42_ refs).
31. `tests/e2e/discovery.py`: Change all `AGENT42_ROOT` to `FROOD_ROOT` (~11 refs).
32. `tests/e2e/runner.py`: Change `AGENT42_ROOT` to `FROOD_ROOT`.

**MCP config files:**

33. `.mcp.json`: Change `"AGENT42_WORKSPACE"` to `"FROOD_WORKSPACE"`. Change server key `"agent42"` to `"frood"`.

34. `.mcp.available.json`: Change `"AGENT42_WORKSPACE"` to `"FROOD_WORKSPACE"` in both `agent42` and `agent42-remote` entries. Change server key names: `"agent42"` to `"frood"`, `"agent42-remote"` to `"frood-remote"`. Also update the SSH args string: `AGENT42_WORKSPACE=~/agent42` to `FROOD_WORKSPACE=~/agent42` (note: the PATH ~/agent42 stays the same -- it's the git repo path, not a data dir).

**CRITICAL — Do NOT rename:**
- Qdrant collection name strings (`"agent42_memory"`, `"agent42_history"`) -- Phase 55
- The git repo path in SSH args (`~/agent42`) -- that's the repo name, not the identity
  </action>
  <verify>
    <automated>python -c "
import subprocess
# Check no AGENT42_ env var reads remain in source files (excluding hooks and comments)
r = subprocess.run(['grep', '-rn', 'AGENT42_', '--include=*.py', '.'], capture_output=True, text=True, cwd='.')
lines = [l for l in r.stdout.strip().split('\n') if l and not l.startswith('.claude/hooks/') and '# ' not in l.split('AGENT42_')[0][-10:]]
# Filter out test files that Plan 03 handles
lines = [l for l in lines if not l.startswith('tests/test_')]
if lines:
    for l in lines[:10]:
        print(f'REMAINING: {l}')
    raise AssertionError(f'{len(lines)} AGENT42_ refs remain in source files')
print('No AGENT42_ env var refs in source files (outside hooks/tests)')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "AGENT42_" --include="*.py" tools/ core/ memory/ dashboard/ scripts/ migrate.py returns 0 matches
    - grep "FROOD_VIDEO_MODEL" tools/video_gen.py returns a match
    - grep "FROOD_IMAGE_MODEL" tools/image_gen.py returns a match
    - grep "_FROOD_BROWSER_TOKEN" tools/browser_tool.py returns a match
    - grep "FROOD_WORKTREE_DIR" core/portability.py returns a match
    - grep "FROOD_WORKSPACE" memory/embeddings.py returns a match
    - grep -c "\.agent42" dashboard/server.py returns 0
    - grep -c "\.agent42" core/portability.py returns 0
    - grep -c "\.agent42" tools/node_sync.py returns 0
    - grep -c "\.agent42" tools/security_audit.py returns 0
    - grep "FROOD_WORKSPACE" .mcp.json returns a match
    - grep "FROOD_WORKSPACE" .mcp.available.json returns a match
    - grep -c "AGENT42" .mcp.json returns 0
    - grep -c "AGENT42" .mcp.available.json returns 0
    - grep "FROOD_ROOT" tests/e2e/config.py returns a match
  </acceptance_criteria>
  <done>All AGENT42_* env var reads renamed to FROOD_* across all source files. All .agent42/ hardcoded paths renamed to .frood/ in source files. MCP config files use frood naming.</done>
</task>

<task type="auto">
  <name>Task 2: Full mcp_server.py rename (variable + SERVER_NAME + env vars + logger)</name>
  <files>mcp_server.py</files>
  <read_first>
    - mcp_server.py (full read -- need _AGENT42_ROOT variable, SERVER_NAME constant, all AGENT42_* env reads, logger name, any .agent42/ paths)
  </read_first>
  <action>
Apply ALL renames in `mcp_server.py` in a single pass (this file is owned exclusively by this task):

1. **Variable rename (PY-03):** `_AGENT42_ROOT = Path(__file__).resolve().parent` becomes `_FROOD_ROOT = Path(__file__).resolve().parent`. Update all usages of `_AGENT42_ROOT` to `_FROOD_ROOT` (~3 occurrences: declaration + 2 uses in sys.path insertion).

2. **SERVER_NAME constant (PY-03):** Find `SERVER_NAME = "agent42"` and change to `SERVER_NAME = "frood"`.

3. **Env var reads (ENTRY-03):** Change all `AGENT42_*` env var reads:
   - `"AGENT42_WORKSPACE"` becomes `"FROOD_WORKSPACE"` 
   - `"AGENT42_SEARCH_URL"` becomes `"FROOD_SEARCH_URL"`
   - `"AGENT42_API_URL"` becomes `"FROOD_API_URL"`
   - Any other `AGENT42_*` reads in this file

4. **Logger name (PY-01):** `logging.getLogger("agent42.mcp.server")` becomes `logging.getLogger("frood.mcp.server")`.

5. **.agent42/ paths (DATA-03):** Change any `.agent42/` path references to `.frood/` (~7 refs found by grep).

6. **Local variable names:** Rename `agent42_root` local variable (if present, ~line 752) to `frood_root`. Rename `try_agent42_api_search()` function to `try_frood_api_search()`.

**CRITICAL — Do NOT rename:**
- Qdrant collection name strings (`"agent42_memory"`, `"agent42_history"`) -- Phase 55 scope
- The `mcp_server.py` filename itself -- PY-03 says "references", not filename
  </action>
  <verify>
    <automated>python -c "
import ast
tree = ast.parse(open('mcp_server.py').read())
src = open('mcp_server.py').read()
assert '_FROOD_ROOT' in src, '_FROOD_ROOT not found'
assert '_AGENT42_ROOT' not in src, '_AGENT42_ROOT still present'
assert 'AGENT42_WORKSPACE' not in src, 'AGENT42_WORKSPACE still present'
assert 'AGENT42_SEARCH_URL' not in src, 'AGENT42_SEARCH_URL still present'
assert 'AGENT42_API_URL' not in src, 'AGENT42_API_URL still present'
assert 'frood.mcp.server' in src, 'logger not renamed'
print('mcp_server.py fully renamed')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep "_FROOD_ROOT" mcp_server.py returns matches
    - grep -c "_AGENT42_ROOT" mcp_server.py returns 0
    - grep 'SERVER_NAME = "frood"' mcp_server.py returns a match
    - grep -c "AGENT42_" mcp_server.py returns 0
    - grep 'getLogger("frood.mcp.server")' mcp_server.py returns a match
    - grep -c "\.agent42" mcp_server.py returns 0 (excluding Qdrant collection names which use underscore not dot)
    - python -c "import ast; ast.parse(open('mcp_server.py').read())" exits 0
  </acceptance_criteria>
  <done>mcp_server.py fully renamed: _FROOD_ROOT variable, SERVER_NAME = "frood", all FROOD_* env vars, frood.mcp.server logger, .frood/ paths.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md marker injection in setup_helpers.py</name>
  <files>scripts/setup_helpers.py</files>
  <read_first>
    - scripts/setup_helpers.py (find _CLAUDE_MD_BEGIN, _CLAUDE_MD_END, CLAUDE_MD_TEMPLATE constants, and any agent42_memory tool name references)
  </read_first>
  <action>
Update the CLAUDE.md marker injection system in `scripts/setup_helpers.py` (PY-04):

1. **Marker constants:**
   - `_CLAUDE_MD_BEGIN = "<!-- BEGIN AGENT42 MEMORY -->"` becomes `_CLAUDE_MD_BEGIN = "<!-- BEGIN FROOD MEMORY -->"`
   - `_CLAUDE_MD_END = "<!-- END AGENT42 MEMORY -->"` becomes `_CLAUDE_MD_END = "<!-- END FROOD MEMORY -->"`

2. **Template body:** Within the `CLAUDE_MD_TEMPLATE` multi-line string:
   - Change `<!-- BEGIN AGENT42 MEMORY -->` to `<!-- BEGIN FROOD MEMORY -->`
   - Change `<!-- END AGENT42 MEMORY -->` to `<!-- END FROOD MEMORY -->`
   - Change `agent42_memory` MCP tool name references to `frood_memory` (since SERVER_NAME changed to "frood" in Task 2, the tool becomes `frood_memory`)
   - Change any `agent42` branding text to `frood`

3. **Any remaining .agent42/ or AGENT42_ references** in setup_helpers.py (the AGENT42_WORKSPACE env var read was already renamed in Task 1, but verify no others remain).

4. **Migration concern:** Add a one-time migration check in `generate_claude_md_section()` — if the target CLAUDE.md contains `<!-- BEGIN AGENT42 MEMORY -->` (old marker), replace it with `<!-- BEGIN FROOD MEMORY -->` before proceeding with the normal injection logic. Same for the END marker. This prevents the function from appending a second block when old markers exist.
  </action>
  <verify>
    <automated>python -c "
src = open('scripts/setup_helpers.py').read()
assert 'BEGIN FROOD MEMORY' in src, 'BEGIN marker not renamed'
assert 'END FROOD MEMORY' in src, 'END marker not renamed'
assert 'BEGIN AGENT42 MEMORY' not in src.split('AGENT42 MEMORY')[0] if 'AGENT42 MEMORY' in src else True, 'Old marker constant remains'
assert 'agent42_memory' not in src.lower() or 'agent42_memory' in src.lower().split('migration')[1] if 'migration' in src.lower() else 'agent42_memory' not in src, 'Old tool name remains'
print('setup_helpers.py markers updated')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep "BEGIN FROOD MEMORY" scripts/setup_helpers.py returns a match
    - grep "END FROOD MEMORY" scripts/setup_helpers.py returns a match
    - grep -c "BEGIN AGENT42 MEMORY" scripts/setup_helpers.py returns 0 (outside migration logic)
    - grep "frood_memory" scripts/setup_helpers.py returns a match (tool name in template)
    - grep -c "agent42_memory" scripts/setup_helpers.py returns 0 (outside migration/comment context)
    - python -c "import ast; ast.parse(open('scripts/setup_helpers.py').read())" exits 0
  </acceptance_criteria>
  <done>CLAUDE.md marker injection fully updated to FROOD MEMORY markers and frood_memory tool name. Old marker migration logic added.</done>
</task>

</tasks>

<verification>
- `grep -rn "AGENT42_" --include="*.py" tools/ core/ memory/ dashboard/ scripts/ migrate.py mcp_server.py` -- returns 0 matches
- `grep -rn "\.agent42" --include="*.py" tools/ core/ memory/ dashboard/ scripts/ migrate.py mcp_server.py` -- returns 0 matches
- `grep -c "AGENT42" .mcp.json .mcp.available.json` -- both return 0
- `python -c "from core.config import Settings"` -- config still imports
- `python -c "import ast; ast.parse(open('mcp_server.py').read())"` -- valid syntax
- `python -m pytest tests/test_setup.py -x -q` -- setup helpers tests (may need Plan 03 test updates first)
</verification>

<success_criteria>
1. Zero AGENT42_* env var reads in any source Python file (outside hooks handled by Plan 03)
2. mcp_server.py uses _FROOD_ROOT, SERVER_NAME = "frood", FROOD_* env vars
3. CLAUDE.md markers use FROOD MEMORY
4. .mcp.json and .mcp.available.json fully use frood naming
5. Zero .agent42/ hardcoded paths in source files (outside hooks/tests)
</success_criteria>

<output>
After completion, create `.planning/workstreams/frood-dashboard/phases/52-core-identity-rename/52-02-SUMMARY.md`
</output>
