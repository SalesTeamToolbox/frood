---
phase: 52-core-identity-rename
plan: 03
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - "# Logger rename: ~106 Python files (all except mcp_server.py, agent42.py, frood.py, config.py)"
  - dashboard/auth.py
  - dashboard/sidecar.py
  - dashboard/websocket_manager.py
  - dashboard/server.py
  - commands.py
  - mcp_registry.py
  - skills/loader.py
  - agents/profile_loader.py
  - agents/agent_routing_store.py
  - "# All files in core/ (except config.py)"
  - "# All files in tools/"
  - "# All files in memory/"
  - "# All files in channels/"
  - "# All files in providers/"
  - "# Hook files (.claude/hooks/)"
  - .claude/hooks/memory-recall.py
  - .claude/hooks/memory-learn.py
  - .claude/hooks/proactive-inject.py
  - .claude/hooks/cc-memory-sync.py
  - .claude/hooks/context-loader.py
  - .claude/hooks/effectiveness-learn.py
  - .claude/hooks/knowledge-learn-worker.py
  - .claude/hooks/credential-sync.py
  - .claude/hooks/test-validator.py
  - .claude/hooks/session-handoff.py
  - .claude/hooks/conversation-accumulator.py
  - .claude/hooks/knowledge-learn.py
  - .claude/hooks/cc-memory-sync-worker.py
  - "# Test files"
  - tests/test_memory_hooks.py
  - tests/test_proactive_injection.py
  - tests/test_setup.py
  - tests/test_portability.py
  - tests/test_mcp_server.py
  - tests/test_device_auth.py
  - tests/test_consolidation_worker.py
  - tests/test_cc_memory_sync.py
  - tests/test_learning_extraction.py
  - tests/test_migrate.py
  - tests/test_rebrand_phase51.py
autonomous: true
requirements: [PY-01, PY-02, DATA-03]

must_haves:
  truths:
    - "No getLogger('agent42.*') calls remain in any Python source file"
    - "No [agent42-*] print prefixes remain in any hook or source file"
    - "All hook env var reads use FROOD_* not AGENT42_*"
    - "All test assertions reference [frood-*] prefixes and .frood/ paths"
    - "test-validator.py GLOBAL_IMPACT_FILES includes frood.py"
  artifacts:
    - path: ".claude/hooks/memory-recall.py"
      provides: "Hook with FROOD_* env vars, [frood-*] prefixes"
      contains: "FROOD_SEARCH_URL"
    - path: ".claude/hooks/proactive-inject.py"
      provides: "Hook with FROOD_* env vars, [frood-*] prefixes"
      contains: "[frood-learnings]"
    - path: "tests/test_memory_hooks.py"
      provides: "Tests asserting [frood-memory] prefix"
      contains: "[frood-memory]"
    - path: "tests/test_proactive_injection.py"
      provides: "Tests asserting [frood-*] prefixes and .frood/ paths"
      contains: "[frood-recommendations]"
    - path: ".claude/hooks/test-validator.py"
      provides: "Test validator with frood.py in GLOBAL_IMPACT_FILES"
      contains: "frood.py"
  key_links:
    - from: "tests/test_memory_hooks.py"
      to: ".claude/hooks/memory-recall.py"
      via: "assert prefix strings match hook output"
      pattern: "\\[frood-memory\\]"
    - from: "tests/test_proactive_injection.py"
      to: ".claude/hooks/proactive-inject.py"
      via: "assert prefix strings match hook output"
      pattern: "\\[frood-recommendations\\]"
---

<objective>
Batch rename all logger names from agent42.* to frood.* across ~106 Python files. Rename all hook files (env vars, print prefixes, .agent42/ paths). Update all test files to match new naming. Add frood.py to test-validator GLOBAL_IMPACT_FILES.

Purpose: Complete the Python internal identity rename -- every logger, every print prefix, every hook env var, and every test assertion speaks "frood".
Output: Zero agent42 references remain in Python source (logger names, print prefixes, env vars, path defaults).
</objective>

<execution_context>
@C:/Users/rickw/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rickw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frood-dashboard/ROADMAP.md
@.planning/workstreams/frood-dashboard/REQUIREMENTS.md
@.planning/workstreams/frood-dashboard/phases/52-core-identity-rename/52-CONTEXT.md
@.planning/workstreams/frood-dashboard/phases/52-core-identity-rename/52-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch rename all logger names from agent42.* to frood.*</name>
  <files>~106 Python files across core/, tools/, memory/, agents/, channels/, providers/, dashboard/, skills/, commands.py, mcp_registry.py</files>
  <read_first>
    - core/sandbox.py (representative file to confirm pattern: single getLogger line)
  </read_first>
  <action>
Batch rename all `getLogger("agent42.` to `getLogger("frood.` across the entire Python codebase (PY-01).

**Method:** Use a single find-and-replace command across all Python files. Every file has exactly one occurrence of `getLogger("agent42.something")` that needs to become `getLogger("frood.something")`.

**Bash command approach (preferred for speed):**
```bash
# Find all Python files with agent42 logger and replace
grep -rl 'getLogger("agent42' --include="*.py" . | while read f; do
  sed -i 's/getLogger("agent42\./getLogger("frood./g' "$f"
done
```

**Files to SKIP (already handled or will be handled by other plans/tasks):**
- `agent42.py` -- now a shim with no logger (Plan 01)
- `frood.py` -- already uses `getLogger("frood")` (Plan 01)  
- `core/config.py` -- already renamed to `frood.config` (Plan 01)
- `mcp_server.py` -- renamed to `frood.mcp.server` by Plan 02

So the command should exclude those 4 files, OR run on all files and let the idempotent replacement handle them (since they already say "frood", the sed will be a no-op).

**Expected scope:** ~103 remaining files across these directories:
- `core/` (~24 files): sandbox.py, heartbeat.py, rate_limiter.py, command_filter.py, encryption.py, device_auth.py, app_manager.py, approval_gate.py, agent_runtime.py, agent_manager.py, key_store.py, github_oauth.py, github_accounts.py, git_auth.py, notification_service.py, memory_bridge.py, portability.py, project_manager.py, rewards_config.py, reward_system.py, repo_manager.py, security_scanner.py, sidecar_orchestrator.py, tiered_routing_bridge.py, url_policy.py, workspace_registry.py, worktree_manager.py
- `tools/` (~35 files): all tool modules
- `memory/` (~9 files): store.py, session.py, redis_session.py, qdrant_store.py, project_memory.py, embeddings.py, effectiveness.py, consolidation_worker.py, consolidation.py, search_service.py
- `agents/` (~2 files): profile_loader.py, agent_routing_store.py
- `channels/` (~5 files): base.py, manager.py, telegram_channel.py, slack_channel.py, discord_channel.py, email_channel.py
- `providers/` (~2 files): zen_proxy.py, zen_api.py
- `dashboard/` (~3 files): auth.py, sidecar.py, websocket_manager.py (server.py logger was NOT already done by Plan 02 -- Plan 02 only does .agent42/ paths and env vars, so include server.py logger here too)
- `skills/` (~1 file): loader.py
- Root: `commands.py`, `mcp_registry.py`

NOTE: `dashboard/server.py` logger rename IS in scope for this task (Plan 02 handles .agent42/ paths and env vars in server.py, but the logger rename `getLogger("agent42.server")` to `getLogger("frood.server")` happens here). The batch sed command handles it cleanly since it only touches the getLogger line.

After the batch rename, verify with: `grep -rn 'getLogger("agent42' --include="*.py" .` should return 0 matches.
  </action>
  <verify>
    <automated>python -c "
import subprocess
r = subprocess.run(['grep', '-rn', 'getLogger(\"agent42', '--include=*.py', '.'], capture_output=True, text=True)
count = len([l for l in r.stdout.strip().split('\n') if l])
if count > 0:
    print(r.stdout[:500])
    raise AssertionError(f'{count} agent42 logger refs remain')
print('All logger names renamed to frood.*')
# Verify some known files
for f, expected in [('core/sandbox.py', 'frood.sandbox'), ('tools/shell.py', 'frood.tools.shell'), ('dashboard/auth.py', 'frood.auth')]:
    src = open(f).read()
    assert expected in src or 'frood.' in src, f'{f} logger not renamed'
print('Spot checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn 'getLogger("agent42' --include="*.py" . returns 0 matches
    - grep 'getLogger("frood.sandbox")' core/sandbox.py returns a match
    - grep 'getLogger("frood.auth")' dashboard/auth.py returns a match
    - grep 'getLogger("frood.server")' dashboard/server.py returns a match
    - grep 'getLogger("frood.websocket")' dashboard/websocket_manager.py returns a match
    - grep 'getLogger("frood.sidecar")' dashboard/sidecar.py returns a match
    - grep -rn 'getLogger("frood' --include="*.py" . | wc -l returns at least 103
  </acceptance_criteria>
  <done>All ~107 getLogger("agent42.*") calls renamed to getLogger("frood.*") across the entire Python codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Rename all hook files -- env vars, print prefixes, and .agent42/ paths</name>
  <files>
    .claude/hooks/memory-recall.py,
    .claude/hooks/memory-learn.py,
    .claude/hooks/proactive-inject.py,
    .claude/hooks/cc-memory-sync.py,
    .claude/hooks/context-loader.py,
    .claude/hooks/effectiveness-learn.py,
    .claude/hooks/knowledge-learn-worker.py,
    .claude/hooks/credential-sync.py,
    .claude/hooks/session-handoff.py,
    .claude/hooks/conversation-accumulator.py,
    .claude/hooks/knowledge-learn.py,
    .claude/hooks/cc-memory-sync-worker.py
  </files>
  <read_first>
    - .claude/hooks/memory-recall.py (largest hook -- AGENT42_SEARCH_URL, AGENT42_API_URL, AGENT42_ROOT, [agent42-memory], [agent42-session-context], .agent42/ paths, agent42_root variable, try_agent42_api_search function)
    - .claude/hooks/proactive-inject.py (AGENT42_DASHBOARD_URL, AGENT42_DATA_DIR, [agent42-learnings], [agent42-recommendations], .agent42/ path)
    - .claude/hooks/memory-learn.py (AGENT42_SEARCH_URL, [agent42-memory])
    - .claude/hooks/cc-memory-sync.py ([agent42-memory] prefix)
    - .claude/hooks/context-loader.py ([agent42] prefix)
    - .claude/hooks/effectiveness-learn.py (AGENT42_DASHBOARD_URL)
    - .claude/hooks/knowledge-learn-worker.py (AGENT42_DASHBOARD_URL, .agent42/ path)
    - .claude/hooks/credential-sync.py (AGENT42_SSH_ALIAS, parses .env file for AGENT42_SSH_ALIAS)
    - .claude/hooks/session-handoff.py (.agent42/ path)
    - .claude/hooks/conversation-accumulator.py (.agent42/ path)
    - .claude/hooks/knowledge-learn.py (.agent42/ path)
    - .claude/hooks/cc-memory-sync-worker.py (.agent42/ paths)
  </read_first>
  <action>
For each hook file, apply ALL three categories of renames in a single pass (D-13):

**1. memory-recall.py:**
- `AGENT42_SEARCH_URL` -> `FROOD_SEARCH_URL`
- `AGENT42_API_URL` -> `FROOD_API_URL`
- `AGENT42_ROOT` -> `FROOD_ROOT`
- `[agent42-memory]` -> `[frood-memory]`
- `[agent42-session-context]` -> `[frood-session-context]`
- Local variable `agent42_root` -> `frood_root`
- Function `try_agent42_api_search()` -> `try_frood_api_search()`
- All `.agent42/` path strings -> `.frood/` (~5 refs)
- CRITICAL: Do NOT rename Qdrant collection name strings `"agent42_memory"` or `"agent42_history"` -- these are Phase 55 scope

**2. memory-learn.py:**
- `AGENT42_SEARCH_URL` -> `FROOD_SEARCH_URL`
- `[agent42-memory]` -> `[frood-memory]`
- All `.agent42/` -> `.frood/` (if any)

**3. proactive-inject.py:**
- `AGENT42_DASHBOARD_URL` -> `FROOD_DASHBOARD_URL`
- `AGENT42_DATA_DIR` -> `FROOD_DATA_DIR`
- `[agent42-learnings]` -> `[frood-learnings]`
- `[agent42-recommendations]` -> `[frood-recommendations]`
- `.agent42/` -> `.frood/` (~1 ref: `INJECTION_GUARD_DIR` default or path)

**4. cc-memory-sync.py:**
- `[agent42-memory]` -> `[frood-memory]`

**5. context-loader.py:**
- `[agent42]` -> `[frood]` (the simple prefix)

**6. effectiveness-learn.py:**
- `AGENT42_DASHBOARD_URL` -> `FROOD_DASHBOARD_URL`
- `.agent42/` -> `.frood/` (if any)

**7. knowledge-learn-worker.py:**
- `AGENT42_DASHBOARD_URL` -> `FROOD_DASHBOARD_URL`
- `.agent42/` -> `.frood/` (~2 refs)

**8. credential-sync.py:**
- `AGENT42_SSH_ALIAS` -> `FROOD_SSH_ALIAS` (in both os.environ.get AND .env file parsing)
- The .env file parser looks for a line containing `AGENT42_SSH_ALIAS` -- this must now look for `FROOD_SSH_ALIAS` in the .env file

**9. session-handoff.py:**
- `.agent42/` -> `.frood/` (~1 ref)

**10. conversation-accumulator.py:**
- `.agent42/` -> `.frood/` (~1 ref)

**11. knowledge-learn.py:**
- `.agent42/` -> `.frood/` (~1 ref)

**12. cc-memory-sync-worker.py:**
- `.agent42/` -> `.frood/` (~2 refs)

Also update `.claude/hooks/test-validator.py`:
- Add `"frood.py"` to `GLOBAL_IMPACT_FILES` set (line ~34-39)
- Keep `"agent42.py"` in the set too (the shim is still a global impact file since it's an entry point)
  </action>
  <verify>
    <automated>python -c "
import subprocess
# Check no AGENT42_ env var reads remain in hooks
r = subprocess.run(['grep', '-rn', 'AGENT42_', '--include=*.py', '.claude/hooks/'], capture_output=True, text=True)
if r.stdout.strip():
    print(r.stdout[:500])
    raise AssertionError('AGENT42_ refs remain in hooks')

# Check no [agent42- print prefixes remain
r2 = subprocess.run(['grep', '-rn', r'\[agent42-', '--include=*.py', '.claude/hooks/'], capture_output=True, text=True)
if r2.stdout.strip():
    print(r2.stdout[:500])
    raise AssertionError('[agent42- prefixes remain in hooks')

# Check test-validator has frood.py
with open('.claude/hooks/test-validator.py') as f:
    content = f.read()
    assert 'frood.py' in content, 'frood.py not in GLOBAL_IMPACT_FILES'

print('All hooks renamed, test-validator updated')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "AGENT42_" --include="*.py" .claude/hooks/ returns 0 matches
    - grep -rn "\[agent42" --include="*.py" .claude/hooks/ returns 0 matches
    - grep "FROOD_SEARCH_URL" .claude/hooks/memory-recall.py returns a match
    - grep "FROOD_API_URL" .claude/hooks/memory-recall.py returns a match
    - grep "FROOD_ROOT" .claude/hooks/memory-recall.py returns a match
    - grep "\[frood-memory\]" .claude/hooks/memory-recall.py returns a match
    - grep "try_frood_api_search" .claude/hooks/memory-recall.py returns a match
    - grep "\[frood-learnings\]" .claude/hooks/proactive-inject.py returns a match
    - grep "\[frood-recommendations\]" .claude/hooks/proactive-inject.py returns a match
    - grep "FROOD_DASHBOARD_URL" .claude/hooks/proactive-inject.py returns a match
    - grep "\[frood-memory\]" .claude/hooks/cc-memory-sync.py returns a match
    - grep "\[frood\]" .claude/hooks/context-loader.py returns a match
    - grep "FROOD_SSH_ALIAS" .claude/hooks/credential-sync.py returns a match
    - grep "frood.py" .claude/hooks/test-validator.py returns a match
    - grep -rn "\.agent42" --include="*.py" .claude/hooks/ returns 0 matches
    - Qdrant collection names "agent42_memory" and "agent42_history" are STILL PRESENT in memory-recall.py (Phase 55 scope)
  </acceptance_criteria>
  <done>All hook files fully renamed: FROOD_* env vars, [frood-*] print prefixes, .frood/ paths. test-validator.py includes frood.py in GLOBAL_IMPACT_FILES.</done>
</task>

<task type="auto">
  <name>Task 3: Update all test files -- assertions, paths, env var references</name>
  <files>
    tests/test_memory_hooks.py,
    tests/test_proactive_injection.py,
    tests/test_setup.py,
    tests/test_portability.py,
    tests/test_mcp_server.py,
    tests/test_device_auth.py,
    tests/test_consolidation_worker.py,
    tests/test_cc_memory_sync.py,
    tests/test_learning_extraction.py,
    tests/test_migrate.py,
    tests/test_rebrand_phase51.py
  </files>
  <read_first>
    - tests/test_memory_hooks.py (assertions on [agent42-memory] prefix, .agent42/ paths)
    - tests/test_proactive_injection.py (assertions on [agent42-recommendations], [agent42-learnings], AGENT42_DATA_DIR env, .agent42/ paths -- 13 refs)
    - tests/test_setup.py (AGENT42_ refs, agent42_memory tool name assertions)
    - tests/test_portability.py (24 .agent42/ path refs in backup/restore assertions)
    - tests/test_mcp_server.py (AGENT42_ refs)
    - tests/test_device_auth.py (.agent42/devices.jsonl assertion)
    - tests/test_consolidation_worker.py (.agent42/ paths)
    - tests/test_cc_memory_sync.py (.agent42/ paths)
    - tests/test_learning_extraction.py (.agent42/ paths)
    - tests/test_migrate.py (agent42_db references)
    - tests/test_rebrand_phase51.py (.agent42/ exclusion pattern)
  </read_first>
  <action>
Update all test files to match the new frood naming (D-14):

**1. tests/test_memory_hooks.py (8 .agent42/ refs + [agent42-memory] assertions):**
- All `".agent42/memory"` path strings -> `".frood/memory"`  
- All `".agent42/memory/MEMORY.md"` -> `".frood/memory/MEMORY.md"`
- All `".agent42/memory/HISTORY.md"` -> `".frood/memory/HISTORY.md"`
- `assert "[agent42-memory] Recall:" in stderr` -> `assert "[frood-memory] Recall:" in stderr`
- `if "[agent42-memory] Recall:" in stderr` -> `if "[frood-memory] Recall:" in stderr`
- `assert "[agent42-memory] Learn: captured to" in stderr` -> `assert "[frood-memory] Learn: captured to" in stderr`

**2. tests/test_proactive_injection.py (13 .agent42/ refs + prefix assertions + AGENT42_DATA_DIR):**
- All `.agent42` path strings -> `.frood` (INJECTION_GUARD_DIR monkeypatches, guard_file paths)
- `monkeypatch.setenv("AGENT42_DATA_DIR", ".agent42")` -> `monkeypatch.setenv("FROOD_DATA_DIR", ".frood")`
- `assert "[agent42-recommendations]" in output/captured.err` -> `assert "[frood-recommendations]"`
- `assert "[agent42-learnings]" in captured.err` -> `assert "[frood-learnings]"`

**3. tests/test_setup.py (3 AGENT42_ refs):**
- Change `"AGENT42_WORKSPACE"` assertions to `"FROOD_WORKSPACE"`
- Change `"agent42_memory"` tool name assertions to `"frood_memory"`
- Change `"AGENT42 MEMORY"` marker assertions to `"FROOD MEMORY"`

**4. tests/test_portability.py (24 .agent42/ refs + AGENT42_WORKTREE_DIR):**
- ALL `.agent42/` path strings -> `.frood/` across all backup/restore/clone test assertions
- `"AGENT42_WORKTREE_DIR"` env var refs -> `"FROOD_WORKTREE_DIR"`
- Paths like: `.agent42/memory/MEMORY.md`, `.agent42/settings.json`, `.agent42/sessions/`, `.agent42/approvals.jsonl`, `.agent42/outputs/`, `.agent42/templates/`, `.agent42/images/`  -- ALL become `.frood/` equivalents

**5. tests/test_mcp_server.py (1 AGENT42_ ref):**
- Change `AGENT42_` env var assertions to `FROOD_`

**6. tests/test_device_auth.py (1 .agent42/ ref):**
- `assert s.devices_file == ".agent42/devices.jsonl"` -> `assert s.devices_file == ".frood/devices.jsonl"`

**7. tests/test_consolidation_worker.py (2 .agent42/ refs):**
- `.agent42/consolidation-status.json` -> `.frood/consolidation-status.json`

**8. tests/test_cc_memory_sync.py (2 .agent42/ refs):**
- `.agent42/cc-sync-status.json` -> `.frood/cc-sync-status.json`

**9. tests/test_learning_extraction.py (6 .agent42/ refs):**
- All `.agent42/` -> `.frood/` (current-task.json path references)

**10. tests/test_migrate.py (1 ref):**
- `agent42_db` argument reference -> update if it references the old name

**11. tests/test_rebrand_phase51.py (1 ref):**
- `".agent42/" not in line` exclusion pattern -> `".frood/" not in line`

After all test updates, run the relevant test subset to verify:
```bash
python -m pytest tests/test_memory_hooks.py tests/test_proactive_injection.py tests/test_setup.py tests/test_portability.py tests/test_device_auth.py -x -q
```
  </action>
  <verify>
    <automated>python -m pytest tests/test_memory_hooks.py tests/test_proactive_injection.py tests/test_setup.py tests/test_portability.py tests/test_device_auth.py tests/test_consolidation_worker.py tests/test_cc_memory_sync.py tests/test_learning_extraction.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "\[agent42-" --include="*.py" tests/ returns 0 matches
    - grep -rn "AGENT42_" --include="*.py" tests/ returns 0 matches (excluding test_migrate.py CLI arg name if it must match the CLI interface)
    - grep -c "\.agent42" tests/test_portability.py returns 0
    - grep -c "\.agent42" tests/test_proactive_injection.py returns 0
    - grep -c "\.agent42" tests/test_memory_hooks.py returns 0
    - grep "\[frood-memory\]" tests/test_memory_hooks.py returns matches
    - grep "\[frood-recommendations\]" tests/test_proactive_injection.py returns matches
    - grep "\[frood-learnings\]" tests/test_proactive_injection.py returns matches
    - grep "\.frood/" tests/test_portability.py returns at least 20 matches
    - grep ".frood/devices.jsonl" tests/test_device_auth.py returns a match
    - python -m pytest tests/test_memory_hooks.py tests/test_proactive_injection.py -x -q exits 0
  </acceptance_criteria>
  <done>All test files updated to reference frood naming. Tests assert on [frood-*] prefixes, .frood/ paths, and FROOD_* env vars. Key test files pass.</done>
</task>

</tasks>

<verification>
Phase-wide verification after Plans 01, 02, and 03 are all complete:

- `grep -rn 'getLogger("agent42' --include="*.py" .` -- returns 0 matches (PY-01 complete)
- `grep -rn 'AGENT42_' --include="*.py" .` -- returns 0 matches outside Qdrant collection contexts (ENTRY-03 + hooks)
- `grep -rn '\[agent42-' --include="*.py" .` -- returns 0 matches (PY-02 complete)
- `grep -rn '\.agent42' --include="*.py" . | grep -v agent42_memory | grep -v agent42_history` -- returns 0 matches (DATA-03 complete)
- `python -m pytest tests/ -x -q` -- full test suite passes
</verification>

<success_criteria>
1. Zero getLogger("agent42.*") calls in the entire codebase
2. Zero [agent42-*] print prefixes in hooks or source
3. All hook env var reads use FROOD_*
4. All test assertions reference frood naming
5. frood.py is in test-validator.py GLOBAL_IMPACT_FILES
6. Key test files pass (memory_hooks, proactive_injection, setup, portability)
</success_criteria>

<output>
After completion, create `.planning/workstreams/frood-dashboard/phases/52-core-identity-rename/52-03-SUMMARY.md`
</output>
