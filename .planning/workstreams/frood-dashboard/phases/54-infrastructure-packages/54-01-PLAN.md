---
phase: 54-infrastructure-packages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.paperclip.yml
  - Dockerfile
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
---

<objective>
Rename Docker infrastructure from Agent42 to Frood identity — service names, volume names, environment variables, and Dockerfile user/command references.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frood-dashboard/ROADMAP.md
@.planning/workstreams/frood-dashboard/REQUIREMENTS.md
@.planning/workstreams/frood-dashboard/phases/54-infrastructure-packages/54-CONTEXT.md

# Current file state (read before modifying):

## docker-compose.paperclip.yml (lines 53, 69, 89, 106)
```yaml
53:   agent42-sidecar:
55:     command: ["python", "agent42.py", "--sidecar"]
...
69:       - agent42-data:/app/.agent42
...
89:       AGENT42_SIDECAR_URL: "http://agent42-sidecar:8001"
...
106:   agent42-data:
```

## Dockerfile (lines 19-22, 31)
```dockerfile
19: RUN useradd -m -s /bin/bash agent42 && \
20:     mkdir -p /app/.agent42 && \
21:     chown -R agent42:agent42 /app
22: USER agent42
...
31: CMD ["python", "agent42.py"]
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Docker service and volume in compose file</name>
  <files>docker-compose.paperclip.yml</files>
  <read_first>docker-compose.paperclip.yml</read_first>
  <action>
Rename Docker service and volume names in docker-compose.paperclip.yml:

1. Line 53: `agent42-sidecar:` → `frood-sidecar:`
2. Line 55: `command: ["python", "agent42.py", "--sidecar"]` → `command: ["python", "frood.py", "--sidecar"]`
3. Line 69: `agent42-data:/app/.agent42` → `frood-data:/app/.frood`
4. Line 89: `AGENT42_SIDECAR_URL: "http://agent42-sidecar:8001"` → `FROOD_SIDECAR_URL: "http://frood-sidecar:8001"`
5. Line 106: `agent42-data:` → `frood-data:`

Update the comments at the top of the file as well (line 1-7).
  </action>
  <verify>
    <automated>grep -E "frood-sidecar|frood-data|FROOD_SIDECAR" docker-compose.paperclip.yml</automated>
  </verify>
  <done>
- Service name is `frood-sidecar` (not `agent42-sidecar`)
- Volume name is `frood-data` (not `agent42-data`)
- Environment variable is `FROOD_SIDECAR_URL` (not `AGENT42_SIDECAR_URL`)
- Command uses `frood.py` (not `agent42.py`)
- Mount path uses `/app/.frood` (not `/app/.agent42`)
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename user and command in Dockerfile</name>
  <files>Dockerfile</files>
  <read_first>Dockerfile</read_first>
  <action>
Rename user and command references in Dockerfile:

1. Line 19: `useradd -m -s /bin/bash agent42` → `useradd -m -s /bin/bash frood`
2. Line 20: `mkdir -p /app/.agent42` → `mkdir -p /app/.frood`
3. Line 21: `chown -R agent42:agent42 /app` → `chown -R frood:frood /app`
4. Line 22: `USER agent42` → `USER frood`
5. Line 31: `CMD ["python", "agent42.py"]` → `CMD ["python", "frood.py"]`
  </action>
  <verify>
    <automated>grep -E "frood" Dockerfile</automated>
  </verify>
  <done>
- User created as `frood` (not `agent42`)
- Data directory is `/app/.frood` (not `/app/.agent42`)
- CMD uses `frood.py` (not `agent42.py`)
  </done>
</task>

</tasks>

<verification>
## Verification Steps

1. Check docker-compose.paperclip.yml:
   ```bash
   grep -c "agent42" docker-compose.paperclip.yml
   # Should return 0 (no remaining agent42 references)
   grep "frood-sidecar\|frood-data\|FROOD_SIDECAR" docker-compose.paperclip.yml
   # Should return 4 matches
   ```

2. Check Dockerfile:
   ```bash
   grep -c "agent42" Dockerfile
   # Should return 0 (no remaining agent42 references)
   grep "frood" Dockerfile
   # Should return 6 matches
   ```
</verification>

<success_criteria>
- docker-compose.paperclip.yml has zero `agent42` references (case-insensitive)
- Dockerfile has zero `agent42` references (case-insensitive)
- Service name is `frood-sidecar`
- Volume name is `frood-data`
- Environment variable is `FROOD_SIDECAR_URL`
- Default command is `python frood.py`
</success_criteria>

<output>
After completion, create `.planning/workstreams/frood-dashboard/phases/54-infrastructure-packages/54-01-SUMMARY.md`
</output>
