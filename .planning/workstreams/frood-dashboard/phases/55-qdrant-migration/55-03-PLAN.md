---
phase: 55-qdrant-migration
plan: 03
type: execute
wave: 2
depends_on: [55-01, 55-02]
files_modified:
  - tests/test_migrate.py
  - tests/test_cc_memory_sync.py
  - tests/test_mcp_server.py
  - tests/test_task_context.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Test mocks and fixtures use 'frood_' prefix"
    - "Migration tests work with both old and new prefixes"
    - "All test assertions use correct collection names"
  artifacts:
    - path: "tests/test_migrate.py"
      provides: "Collection rename mocks"
      contains: "frood_memory"
    - path: "tests/test_cc_memory_sync.py"
      provides: "Collection name mocks"
      contains: "frood_memory"
    - path: "tests/test_mcp_server.py"
      provides: "Collection assertion"
      contains: "frood_memory"
    - path: "tests/test_task_context.py"
      provides: "Index creation mocks"
      contains: "frood_memory"
  key_links: []
---

<objective>
Update test files to use 'frood_' prefix in mocks and assertions, ensuring tests pass with new collection names.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/workstreams/frood-dashboard/phases/55-qdrant-migration/55-CONTEXT.md
@.planning/workstreams/frood-dashboard/phases/55-qdrant-migration/55-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Update test_migrate.py collection mocks</name>
  <files>tests/test_migrate.py</files>
  <action>
Lines 166, 187 contain:
```
migrate_collection(src, dst, "agent42_memory", "target-comp", 100, False)
```

Replace with "frood_memory" to match new collection names:
```
migrate_collection(src, dst, "frood_memory", "target-comp", 100, False)
```

Also update any docstring references if present.
  </action>
  <verify>
grep -n "frood_memory" tests/test_migrate.py
  </verify>
  <done>test_migrate.py uses frood_memory in migrate calls</done>
</task>

<task type="auto">
  <name>Update test_cc_memory_sync.py mocks</name>
  <files>tests/test_cc_memory_sync.py</files>
  <action>
Lines 216 and 251 contain:
```python
mock_store._collection_name = MagicMock(return_value="agent42_memory")
```

Replace with:
```python
mock_store._collection_name = MagicMock(return_value="frood_memory")
```

This updates the mock to return the new collection name.
  </action>
  <verify>
grep -n "frood_memory" tests/test_cc_memory_sync.py
  </verify>
  <done>test_cc_memory_sync.py mock returns frood_memory</done>
</task>

<task type="auto">
  <name>Update test_mcp_server.py assertion</name>
  <files>tests/test_mcp_server.py</files>
  <action>
Line 257 contains:
```python
assert "agent42_memory" in names
```

Replace with:
```python
assert "frood_memory" in names
```

The test asserts that MCP server correctly detects the collection.
  </action>
  <verify>
grep -n "frood_memory" tests/test_mcp_server.py
  </verify>
  <done>test_mcp_server.py asserts frood_memory presence</done>
</task>

<task type="auto">
  <name>Update test_task_context.py index mocks</name>
  <files>tests/test_task_context.py</files>
  <action>
Lines 334, 335 contain:
```python
store._ensure_task_indexes("agent42_memory")
```

Replace with:
```python
store._ensure_task_indexes("frood_memory")
```

This updates index creation mocks to use new collection name.
  </action>
  <verify>
grep -n "frood_memory" tests/test_task_context.py
  </verify>
  <done>test_task_context.py creates indexes on frood_memory</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {N/A} | Test files — no runtime impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | N/A | Test files | accept | Tests are internal verification only |
</threat_model>

<verification>
- [ ] All 4 test files use frood_memory
- [ ] Tests pass: pytest tests/test_migrate.py tests/test_cc_memory_sync.py tests/test_mcp_server.py tests/test_task_context.py
</verification>

<success_criteria>
All test collection references updated to frood_*
</success_criteria>

<output>
After completion, create `.planning/workstreams/frood-dashboard/phases/55-qdrant-migration/55-03-SUMMARY.md`
</output>