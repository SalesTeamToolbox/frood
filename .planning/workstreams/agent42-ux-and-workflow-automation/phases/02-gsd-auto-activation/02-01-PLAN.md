---
phase: 02-gsd-auto-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/gsd-auto-activate/SKILL.md
  - CLAUDE.md
  - tests/test_skill_loader.py
autonomous: true
requirements: [GSD-01, GSD-02]
must_haves:
  truths:
    - "The gsd-auto-activate skill loads for any task type due to always: true"
    - "CLAUDE.md contains a Development Methodology section establishing GSD as default"
    - "The skill instructs Claude when to use GSD and when to skip it"
  artifacts:
    - path: ".claude/skills/gsd-auto-activate/SKILL.md"
      provides: "Always-on skill with behavioral instructions for GSD auto-detection"
      contains: "always: true"
    - path: "CLAUDE.md"
      provides: "Development Methodology section appended to project instructions"
      contains: "## Development Methodology"
    - path: "tests/test_skill_loader.py"
      provides: "Test confirming gsd-auto-activate skill loads as always-on"
      contains: "test_gsd_auto_activate_skill_always_loads"
  key_links:
    - from: ".claude/skills/gsd-auto-activate/SKILL.md"
      to: "skills/loader.py"
      via: "SkillLoader discovers SKILL.md in .claude/skills/ directories"
      pattern: "always: true"
---

<objective>
Create the always-on GSD skill and append the Development Methodology section to CLAUDE.md.

Purpose: Establishes GSD as the default methodology by injecting behavioral instructions into Claude's context on every prompt (via the skill) and documenting the process in CLAUDE.md for developer reference.
Output: New skill directory with SKILL.md, updated CLAUDE.md, extended skill loader test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/agent42-ux-and-workflow-automation/ROADMAP.md
@.planning/workstreams/agent42-ux-and-workflow-automation/STATE.md
@.planning/workstreams/agent42-ux-and-workflow-automation/phases/02-gsd-auto-activation/02-CONTEXT.md

<interfaces>
<!-- Skill YAML frontmatter format (from .claude/skills/add-tool/SKILL.md): -->

Frontmatter uses triple-dash delimiters with fields: name, description, always, task_types.
When always: true, omit task_types. Body is markdown instructions.

<!-- SkillLoader API (from tests/test_skill_loader.py): -->

    loader = SkillLoader([skill_directories])
    skills = loader.load_all()                    # Returns dict of name -> Skill
    result = loader.get_for_task_type("anything") # Returns list of matching Skills
    # Skills with always: true are returned for ANY task type string

<!-- _write_skill test helper (from tests/test_skill_loader.py): -->

    def _write_skill(skill_dir: Path, name: str, content: str) -> Path:
        """Helper to create a skill directory with SKILL.md."""

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gsd-auto-activate always-on skill</name>
  <files>.claude/skills/gsd-auto-activate/SKILL.md</files>
  <read_first>
    - .claude/skills/add-tool/SKILL.md (example skill YAML frontmatter format)
    - .planning/workstreams/agent42-ux-and-workflow-automation/phases/02-gsd-auto-activation/02-CONTEXT.md (decisions D-01 through D-13)
    - .planning/workstreams/agent42-ux-and-workflow-automation/phases/02-gsd-auto-activation/02-RESEARCH.md (complete SKILL.md example at line 316-362)
  </read_first>
  <action>
Create directory `.claude/skills/gsd-auto-activate/` and file `SKILL.md`.

The file MUST have YAML frontmatter (triple-dash delimited) with these exact fields:
- name: gsd-auto-activate
- description: Instructs Claude to use GSD methodology for multi-step tasks
- always: true
- Do NOT include task_types (always: true makes it irrelevant, per research pitfall 1)

After the frontmatter closing delimiter, the markdown body must contain ALL of the following sections:

SECTION: # GSD Auto-Activation
Content: "When working in this project, use the GSD (Get Shit Done) structured workflow for any multi-step task. This is the default methodology -- not an optional enhancement."

SECTION: ## Activate GSD When You See
Content: Bullet list with these items:
- Multiple files need to be created or modified
- The task has a clear sequence of steps ("first..., then..., finally...")
- Keywords like: build, create, implement, refactor, add feature, set up, migrate, convert, redesign, scaffold, plan, roadmap
- Framework scaffolding: Flask app, Django app, React app, FastAPI service
- The work would naturally break into phases
Then: **Use `/gsd:new-project`** to start a full workstream, or **`/gsd:quick`** for a single self-contained task that still benefits from structured tracking.

SECTION: ## Skip GSD For
Content: Per D-02, bullet list:
- Prompts under ~30 characters
- Questions beginning with "what", "how", "why", "explain", "show me"
- Single-file fixes where the target file is explicitly named ("fix the typo in X")
- Slash commands (/, /help, /gsd:...)
- Debugging a specific error message

SECTION: ## Mid-Task Pivot
Content: Per D-12, include: If a task that started simple reveals unexpected complexity, suggest: "This is getting more involved -- want me to switch to GSD for better tracking?"

SECTION: ## Already In GSD
Content: Per D-13, include: If `.planning/active-workstream` exists and is non-empty, a GSD workstream is already active. Do not suggest starting a new one. Continue within the current workflow.

SECTION: ## Natural Language
Content: Per D-05, include: When GSD activates, mention it naturally in your first response: "I'll use GSD to break this into phases..." -- not a banner or announcement.

SECTION: ## Ambiguous Cases
Content: Per D-03, include: When unsure whether a task needs GSD, default to suggesting it with an opt-out: "This looks like a multi-step task. I'll use GSD to plan and execute. Say 'just do it' to skip."

Key decisions implemented: D-03 (ambiguous opt-out), D-04 (silent system instructions), D-05 (natural language), D-09 (skill is primary mechanism), D-11 (command references by name), D-12 (mid-task pivot), D-13 (active workstream check).
  </action>
  <verify>
    <automated>python -c "import pathlib; p = pathlib.Path('.claude/skills/gsd-auto-activate/SKILL.md'); assert p.exists(); t = p.read_text(); assert 'always: true' in t; assert 'gsd-auto-activate' in t; assert '/gsd:new-project' in t; assert '/gsd:quick' in t; assert 'active-workstream' in t; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/gsd-auto-activate/SKILL.md` exists
    - File contains YAML frontmatter with `name: gsd-auto-activate`
    - File contains `always: true` in frontmatter
    - File does NOT contain `task_types:` in frontmatter
    - File contains `/gsd:new-project` and `/gsd:quick` command references (per D-11)
    - File contains `.planning/active-workstream` check instruction (per D-13)
    - File contains "just do it" opt-out language (per D-03)
    - File contains mid-task pivot suggestion (per D-12)
  </acceptance_criteria>
  <done>gsd-auto-activate SKILL.md exists with always: true frontmatter, references all GSD commands, includes skip criteria, mid-task pivot, active-workstream check, and ambiguous-case opt-out</done>
</task>

<task type="auto">
  <name>Task 2: Append Development Methodology section to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current file -- must read to find insertion point; section goes before ## Common Pitfalls)
    - .planning/workstreams/agent42-ux-and-workflow-automation/phases/02-gsd-auto-activation/02-CONTEXT.md (decisions D-07, D-08)
    - .planning/workstreams/agent42-ux-and-workflow-automation/phases/02-gsd-auto-activation/02-RESEARCH.md (section template at line 200-230)
  </read_first>
  <action>
Insert a new `## Development Methodology` section into CLAUDE.md. Per D-08, this is an append -- do NOT reorganize or rewrite existing content. Per the research recommendation, insert BEFORE `## Common Pitfalls` (line 380 in current file) since methodology belongs with workflow sections, not after the pitfalls table.

Find the line `## Common Pitfalls` and insert the following block BEFORE it, with a horizontal rule separator above:

HORIZONTAL RULE

## Development Methodology

Agent42 uses **GSD (Get Shit Done)** as the default methodology for multi-step work.
The always-on `gsd-auto-activate` skill handles detection automatically.

### When to Use GSD

Use GSD for any task involving multiple files, phases, or coordinated steps:
- New features, tools, skills, or providers
- Refactors touching 3+ files
- Architecture changes or migrations
- Any task where you'd naturally say "first I need to... then..."

### When to Skip GSD

Skip GSD for trivial tasks (Claude handles these directly):
- Quick questions ("what does X do?")
- Single-file typo/style fixes
- Configuration lookups
- Debugging a specific known error

### Key GSD Commands

Table with two columns (Command | When to Use):
- `/gsd:new-project` | Initialize a new multi-phase workstream
- `/gsd:quick` | Ad-hoc task with GSD tracking but no full phase plan
- `/gsd:plan-phase` | Plan a specific phase when already in a workstream
- `/gsd:execute-phase` | Execute a planned phase
- `/gsd:next` | Advance to the next task in the current plan

Per D-07: Section is under 30 lines, covers when to use GSD, when to skip, key commands, and notes the always-on skill. Developer-skim friendly.
Per D-08: Append only -- do not reorganize or rewrite existing CLAUDE.md content.
  </action>
  <verify>
    <automated>python -c "content = open('CLAUDE.md').read(); assert '## Development Methodology' in content; assert '/gsd:new-project' in content; assert '/gsd:quick' in content; assert 'gsd-auto-activate' in content; idx_method = content.index('## Development Methodology'); idx_pitfalls = content.index('## Common Pitfalls'); assert idx_method &lt; idx_pitfalls; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `## Development Methodology` section
    - The section appears BEFORE `## Common Pitfalls` in the file
    - Section contains `gsd-auto-activate` skill name reference
    - Section contains `/gsd:new-project` command
    - Section contains `/gsd:quick` command
    - Section contains `/gsd:plan-phase` command
    - Section contains `/gsd:execute-phase` command
    - Section mentions both "When to Use GSD" and "When to Skip GSD"
    - No existing CLAUDE.md content was reorganized or rewritten (per D-08)
    - Section is under 30 lines of content (per D-07)
  </acceptance_criteria>
  <done>CLAUDE.md has a Development Methodology section before Common Pitfalls, with GSD commands table, when-to-use/skip guidance, and skill name reference</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add gsd-auto-activate skill loader test</name>
  <files>tests/test_skill_loader.py</files>
  <read_first>
    - tests/test_skill_loader.py (current test file -- must see existing patterns, _write_skill helper, TestSkillLoader class)
    - .claude/skills/gsd-auto-activate/SKILL.md (the skill file created in Task 1 -- needed to verify test expectations)
  </read_first>
  <behavior>
    - Test 1: gsd-auto-activate skill with `always: true` loads for any arbitrary task type string
    - Test 2: gsd-auto-activate skill instructions contain `/gsd:new-project` command reference
    - Test 3: gsd-auto-activate skill has no `task_types` field set (always-on skills don't need it)
  </behavior>
  <action>
Add the following test method to the existing `TestSkillLoader` class in `tests/test_skill_loader.py`. Place it after the existing `test_always_load` method (around line 84).

    def test_gsd_auto_activate_skill_always_loads(self, tmp_path):
        """GSD auto-activate skill loads for any task type via always: true."""
        _write_skill(
            tmp_path,
            "gsd-auto-activate",
            "---\nname: gsd-auto-activate\ndescription: Instructs Claude to use GSD methodology for multi-step tasks\nalways: true\n---\n\n# GSD Auto-Activation\n\nUse `/gsd:new-project` for full workstreams.",
        )
        loader = SkillLoader([tmp_path])
        loader.load_all()
        # Should load for any arbitrary task type
        result = loader.get_for_task_type("completely_unknown_type")
        assert len(result) == 1
        assert result[0].name == "gsd-auto-activate"
        # Instructions should contain GSD command reference
        assert "/gsd:new-project" in result[0].instructions
        # always-on skills don't have task_types set
        assert result[0].task_types is None or result[0].task_types == []

This test validates GSD-01: the always-on skill loads for any task type and contains the expected behavioral instructions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_skill_loader.py -x -q -k "gsd"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_skill_loader.py` contains method `test_gsd_auto_activate_skill_always_loads`
    - Method is inside the `TestSkillLoader` class
    - Test creates a skill with `always: true` and name `gsd-auto-activate`
    - Test asserts `get_for_task_type("completely_unknown_type")` returns the skill
    - Test asserts instructions contain `/gsd:new-project`
    - `python -m pytest tests/test_skill_loader.py -x -q -k "gsd"` exits 0
  </acceptance_criteria>
  <done>test_gsd_auto_activate_skill_always_loads passes, confirming the always-on skill mechanism works for GSD</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_skill_loader.py -x -q` -- all existing + new tests pass
- `.claude/skills/gsd-auto-activate/SKILL.md` exists with `always: true` frontmatter
- `grep "## Development Methodology" CLAUDE.md` returns a match
- `## Development Methodology` appears before `## Common Pitfalls` in CLAUDE.md
</verification>

<success_criteria>
1. gsd-auto-activate skill exists with always: true, GSD command references, skip criteria, and active-workstream check
2. CLAUDE.md has Development Methodology section with commands table, positioned before Common Pitfalls
3. Skill loader test confirms the skill loads for arbitrary task types
4. All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/workstreams/agent42-ux-and-workflow-automation/phases/02-gsd-auto-activation/02-01-SUMMARY.md`
</output>
