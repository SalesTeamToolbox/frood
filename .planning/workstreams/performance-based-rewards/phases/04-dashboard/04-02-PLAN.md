---
phase: 04-dashboard
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - dashboard/frontend/dist/app.js
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]
must_haves:
  truths:
    - Every agent card displays a tier badge (Bronze/Silver/Gold/Provisional) next to the status badge
    - Clicking an agent card shows a performance metrics section with score, tier, task count, and success rate
    - The settings page has a Rewards tab with a toggle that shows a confirm dialog before calling POST /api/rewards/toggle
    - The agent detail view has a tier override dropdown calling PATCH /api/agents/{id}/reward-tier
    - A tier_update WebSocket event updates agent card badges in real time without a page refresh
  artifacts:
    - path: dashboard/frontend/dist/app.js
      provides: _renderAgentCards with tier badge, tier_update WS handler, agentShowDetail with performance and override UI, settings Rewards tab
      contains: tier_update
  key_links:
    - from: dashboard/frontend/dist/app.js handleWSMessage
      to: state.agents array
      via: tier_update case updates state.agents[idx].effective_tier
      pattern: tier_update
    - from: dashboard/frontend/dist/app.js agentShowDetail
      to: /api/agents/{id}/performance
      via: fetch call in rewards section
      pattern: api/agents.*performance
    - from: dashboard/frontend/dist/app.js rewards toggle
      to: /api/rewards/toggle
      via: POST fetch with enabled bool
      pattern: api/rewards/toggle
---
<objective>
Add tier visibility and rewards controls to the Agent42 dashboard frontend: tier badges on agent cards, performance metrics panel in agent detail, rewards toggle in settings, admin tier override dropdown, and real-time tier badge updates via the existing WebSocket infrastructure.

Purpose: Operators must see and manage tier assignments at a glance without page refreshes.
Output: In-place edits to dashboard/frontend/dist/app.js only (no build step required).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/performance-based-rewards/ROADMAP.md
@.planning/workstreams/performance-based-rewards/phases/04-dashboard/04-CONTEXT.md
@.planning/workstreams/performance-based-rewards/phases/04-dashboard/04-RESEARCH.md
@.planning/workstreams/performance-based-rewards/phases/04-dashboard/04-01-SUMMARY.md

<interfaces>
Key JS locations extracted from app.js. Read each section via Read tool before editing.

state object (lines 7-110): Does NOT have an agents key. Add agents: [] and rewardsStatus: null.

handleWSMessage (line 492): if/else-if chain. Add tier_update case at the end.

_renderAgentCards (line 2243): Status badge at line 2253.
Agent card HTML is built by assigning a string to el.innerHTML.
All user-supplied values must go through esc() -- 61 existing uses follow this pattern.

agentShowDetail (line 2376-2415): Async. Append rewards section before final closing div strings.

renderSettings (line 6863): tabs array at line 6867. Add Rewards tab entry.

renderSettingsPanel (line 6890): panels object at line 6899. Add rewards function key.

agentDelete (line 2430): Uses synchronous confirm() -- copy for tier override and toggle.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tier badge to agent cards and tier_update WebSocket handler</name>
  <files>dashboard/frontend/dist/app.js</files>
  <read_first>
    - dashboard/frontend/dist/app.js lines 7-110 (state object -- confirm agents key absent)
    - dashboard/frontend/dist/app.js lines 492-540 (handleWSMessage -- find last else-if branch)
    - dashboard/frontend/dist/app.js lines 2232-2285 (_renderAgentCards -- exact status badge line)
  </read_first>
  <action>
    Four targeted edits. Read each section before editing.

    EDIT 1 -- state object (~line 110).
    Before the last key (selectedTeamRun: null), insert:
      agents: [],
      rewardsStatus: null,

    EDIT 2 -- tierColor helper before _renderAgentCards (line 2243).
    Insert immediately before the function definition:
      var TIER_COLORS = { bronze: '#cd7f32', silver: '#94a3b8', gold: '#eab308', provisional: '#6366f1' };
      function tierColor(t) { return TIER_COLORS[(t || '').toLowerCase()] || '#64748b'; }

    EDIT 3 -- tier badge in _renderAgentCards (line 2253).
    Status badge line ends with '</span></div>' + concatenation operator.
    Change '</span></div>' to '</span>' and add two lines:
    Line A: conditional tier badge span using tierColor() and esc(a.effective_tier), only when a.effective_tier is truthy.
    Line B: '</div>' + (closes agent-card-header that previously closed after status badge)

    EDIT 4 -- renderAgents state storage + tier_update WS handler.
    In renderAgents (line 2239), in the .then callback, add state.agents = agents; before _renderAgentCards.

    In handleWSMessage, after the last else-if brace, before the function closing brace, add:
      } else if (msg.type === 'tier_update') {
        (msg.data.agents || []).forEach(function(u) {
          var idx = state.agents ? state.agents.findIndex(function(a) { return a.id === u.agent_id; }) : -1;
          if (idx >= 0) {
            state.agents[idx].effective_tier = u.tier;
            state.agents[idx].performance_score = u.score;
          }
        });
        if (state.page === 'agents') renderAgents();
      }
  </action>
  <verify>
    <automated>node -e "var fs=require(chr(39)+chr(39)+chr(39)fs chr(39));..." </automated>
  </verify>
  <acceptance_criteria>
    - state has agents: [] and rewardsStatus: null
    - tierColor() exists immediately before _renderAgentCards
    - _renderAgentCards renders tier badge when a.effective_tier is truthy
    - handleWSMessage has tier_update case updating state.agents and calling renderAgents()
    - renderAgents() stores fetched agents in state.agents
  </acceptance_criteria>
  <done>See acceptance_criteria above</done>
</task>

<task type="auto">
  <name>Task 2: Add performance metrics panel, override dropdown, and rewards settings tab</name>
  <files>dashboard/frontend/dist/app.js</files>
  <read_first>
    - dashboard/frontend/dist/app.js lines 2376-2416 (agentShowDetail -- find closing div pattern)
    - dashboard/frontend/dist/app.js lines 2430-2434 (agentDelete -- confirm() pattern to copy)
    - dashboard/frontend/dist/app.js lines 6863-6895 (renderSettings tabs array)
    - dashboard/frontend/dist/app.js lines 6890-6960 (renderSettingsPanel panels object)
    - dashboard/frontend/dist/app.js lines 792-840 (loadStorageStatus area -- where to add helpers)
  </read_first>
  <action>
    Five targeted edits to dashboard/frontend/dist/app.js. Read each section before editing.

    EDIT 1 -- rewards section in agentShowDetail (~line 2413).
    The el.innerHTML assignment string ends with two closing div fragments then semicolon.
    Before the final two closing div strings, insert a rewards section div containing:
    (a) Heading: Performance and Tier
    (b) A div with id = agent-perf- + agent.id, initialized to text Loading...
    (c) A select element with id = tier-override- + agent.id, onchange attribute calling
        setTierOverride(agent.id). Options: empty value for auto, bronze, silver, gold, provisional.
        Each option uses a ternary to output " selected" when its value triple-equals agent.tier_override.
    All string values from agent data must go through esc().

    After the assignment semicolon, add async fetch code:
    - Fetch /api/agents/ + id + /performance with Authorization: Bearer + state.token
    - On success: populate element by id agent-perf- + id with a 2-column grid showing:
      Tier (esc(perf.tier)), Score (perf.performance_score.toFixed(3)),
      Tasks (perf.task_count), Success Rate (Math.round(perf.success_rate*100) + %)
      Style: grid-template-columns:1fr 1fr, gap:1rem -- matching the existing detail grid
    - On error: set element text to Performance data unavailable.

    EDIT 2 -- setTierOverride function (add after agentDelete ~line 2434).
    async function setTierOverride(id): get select by id, read .value as tier,
    label = tier || Auto (computed), synchronous confirm() first, then fetch PATCH
    /api/agents/{id}/reward-tier with JSON body {tier: tier}, toast result, call agentShowDetail(id).

    EDIT 3 -- helper functions near loadStorageStatus (~line 798).
    loadRewardsStatus(): calls api(/rewards), stores in state.rewardsStatus, silences errors.
    toggleRewardsSystem(newEnabled): synchronous confirm() first, POST api(/rewards/toggle) with
    {enabled: newEnabled}, then loadRewardsStatus(), toast, renderSettingsPanel().

    EDIT 4 -- Rewards tab in renderSettings tabs array (~line 6874).
    Insert before the closing bracket of the tabs array: { id: rewards, label: Rewards },

    EDIT 5 -- rewards panel function in renderSettingsPanel panels object (~line 6899).
    Add a rewards function key. If state.rewardsStatus null: call loadRewardsStatus().then(renderSettingsPanel)
    and return a loading placeholder. Otherwise: render heading, description, form-group card with flex row:
    label/subtext left, toggle button right calling toggleRewardsSystem(!enabled).
    Button class: btn-primary when currently disabled, btn-outline when currently enabled.
    Button label: Enable Rewards when disabled, Disable Rewards when enabled.
    Tier count summary when state.rewardsStatus.tier_counts exists (Gold, Silver, Bronze, Provisional).
    Follow the style of adjacent panel functions exactly.

    Also add loadRewardsStatus() call alongside loadStorageStatus() in settings page navigation.
  </action>
  <verify>
    <automated>Run: node -e "var s=require(chr39fs chr39).readFileSync(chr39dashboard/frontend/dist/app.js chr39,chr39utf8 chr39);[chr39setTierOverride chr39,chr39toggleRewardsSystem chr39,chr39loadRewardsStatus chr39].forEach(function(p){if(!s.includes(p))throw new Error(p);console.log(chr39OK chr39,p);})"</automated>
  </verify>
  <acceptance_criteria>
    - agentShowDetail renders Performance and Tier section fetching and displaying 4 metrics
    - Tier override select with options, onchange=setTierOverride, pre-selected current override
    - setTierOverride in global scope: synchronous confirm() then PATCH /api/agents/{id}/reward-tier
    - Settings tabs array includes Rewards entry
    - rewards function in panels object with toggle button calling toggleRewardsSystem()
    - toggleRewardsSystem: synchronous confirm() then POST /api/rewards/toggle
    - loadRewardsStatus() fetches GET /api/rewards into state.rewardsStatus
  </acceptance_criteria>
  <done>See acceptance_criteria above</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Phase 4 Dashboard</name>
  <what-built>
    Complete Phase 4 dashboard:
    - Plan 01: 5 rewards REST endpoints with 401 auth guards, effective_tier in agent serialization,
      TierRecalcLoop WebSocket broadcast, test_rewards_api.py
    - Plan 02: Tier badges on agent cards, performance metrics panel in agent detail, Rewards settings
      tab with toggle, tier override dropdown, real-time tier_update WS handler
  </what-built>
  <how-to-verify>
    Start Agent42 with REWARDS_ENABLED=true in .env (python agent42.py), then:

    1. Agents page -- each agent card shows a colored tier badge next to the status badge.
    2. Click agent -- Performance and Tier section loads with score, tier, task count, success rate.
    3. Tier override dropdown -- set Gold, confirm dialog, after confirmation detail refreshes.
    4. Settings > Rewards tab -- toggle button shows current enabled/disabled state.
    5. Click toggle -- confirm dialog, after confirmation button label changes.
    6. DevTools Console, trigger recalculation:
       curl -X POST http://localhost:8000/api/admin/rewards/recalculate-all
       Confirm tier_update WS message arrives and badges update without page refresh.
    7. Unauthenticated: curl http://localhost:8000/api/rewards -- confirm 401.
    8. Full suite: python -m pytest tests/ -x -q -- green.
  </how-to-verify>
  <action>Human verification step -- no automated action. See how-to-verify instructions above.</action>
  <verify><automated>N/A -- human checkpoint</automated></verify>
  <done>Human types approved after confirming all behaviors listed in how-to-verify</done>
  <resume-signal>Type approved to complete phase, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Frontend pattern verification (no runtime required):

  node -e (verify all required patterns exist in app.js including tier_update, tierColor, effective_tier,
  state.agents = agents, setTierOverride, toggleRewardsSystem, loadRewardsStatus, Rewards)

  python -m pytest tests/ -x -q
</verification>

<success_criteria>
- Tier badge on every agent card when effective_tier is truthy
- handleWSMessage tier_update case updates state.agents and calls renderAgents()
- agentShowDetail fetches /api/agents/{id}/performance and renders 4 metrics
- Tier override dropdown calls setTierOverride() via onchange
- Settings Rewards tab toggle calls toggleRewardsSystem()
- toggleRewardsSystem: synchronous confirm() then POST /api/rewards/toggle
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/workstreams/performance-based-rewards/phases/04-dashboard/04-02-SUMMARY.md`
</output>
