---
phase: 02-windows-claude-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - setup.sh
  - scripts/setup_helpers.py
  - .gitattributes
  - tests/test_setup.py
autonomous: true
requirements: [SETUP-06]

must_haves:
  truths:
    - "User on Windows Git Bash runs `bash setup.sh` and venv activates without 'No such file' errors on .venv/bin/activate"
    - "User on Windows Git Bash runs `bash setup.sh` and all python3 calls succeed (no 'command not found' for python3)"
    - "Shell and Python files checked out from git always have LF line endings on any platform"
    - "setup_helpers.py generates MCP config with .venv/Scripts/python.exe on Windows and .venv/bin/python on Linux"
    - "setup_helpers.py health check uses correct venv python path for the current platform"
  artifacts:
    - path: "setup.sh"
      provides: "Platform detection block setting VENV_ACTIVATE and PYTHON_CMD"
      contains: "MINGW*|MSYS*|CYGWIN*"
    - path: "scripts/setup_helpers.py"
      provides: "_venv_python() helper returning platform-correct path"
      exports: ["_venv_python"]
    - path: ".gitattributes"
      provides: "LF line ending enforcement for .sh and .py files"
      contains: "eol=lf"
    - path: "tests/test_setup.py"
      provides: "TestWindowsCompat test class"
      contains: "class TestWindowsCompat"
  key_links:
    - from: "setup.sh"
      to: "scripts/setup_helpers.py"
      via: "$PYTHON_CMD scripts/setup_helpers.py"
      pattern: "\\$PYTHON_CMD scripts/setup_helpers"
    - from: "scripts/setup_helpers.py:generate_mcp_config"
      to: "_venv_python()"
      via: "function call replacing hardcoded path"
      pattern: "_venv_python\\(project_dir\\)"
    - from: "scripts/setup_helpers.py:check_health"
      to: "_venv_python()"
      via: "function call replacing hardcoded path"
      pattern: "_venv_python\\(project_dir\\)"
---

<objective>
Make setup.sh and setup_helpers.py work on Windows Git Bash by adding platform detection,
platform-aware venv paths, and CRLF prevention via .gitattributes.

Purpose: SETUP-06 requires Windows Git Bash users to run `bash setup.sh` without path errors
or CRLF failures. Currently setup.sh hardcodes `.venv/bin/activate` and `python3`, both of
which fail on Windows (venv is at `.venv/Scripts/`, and Git Bash has `python` not `python3`).

Output: Platform-aware setup.sh, platform-aware setup_helpers.py, .gitattributes, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gsd-and-jcodemunch-integration/ROADMAP.md
@.planning/workstreams/gsd-and-jcodemunch-integration/STATE.md
@.planning/workstreams/gsd-and-jcodemunch-integration/phases/02-windows-claude-md/02-CONTEXT.md
@.planning/workstreams/gsd-and-jcodemunch-integration/phases/02-windows-claude-md/02-RESEARCH.md

<interfaces>
<!-- Current setup.sh structure (key lines): -->
<!-- Line 14: PROJECT_DIR="$(cd "$(dirname "$0")" && pwd)" -->
<!-- Line 20-22: info()/warn()/error() logging functions -->
<!-- Lines 24-55: sync-auth subcommand block -->
<!-- Lines 57-221: create-shortcut subcommand block (has MINGW detection at line 65-66) -->
<!-- Line 223: QUIET=false -->
<!-- Line 240: python3 check -->
<!-- Line 274: python3 -m venv .venv -->
<!-- Line 275: source .venv/bin/activate (BREAKS on Windows) -->
<!-- Lines 313,315,321,326,331,339: python3 calls (ALL must become $PYTHON_CMD) -->
<!-- Line 347: echo "source .venv/bin/activate && python agent42.py" (final instruction) -->

<!-- Current setup_helpers.py structure (key lines): -->
<!-- Line 216: os.path.join(project_dir, ".venv", "bin", "python") in generate_mcp_config() -->
<!-- Line 391: os.path.join(project_dir, ".venv", "bin", "python") in check_health() -->
<!-- Lines 608-649: CLI __main__ block with subcommands: mcp-config, register-hooks, health, claude-md -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _venv_python() helper and platform-aware paths in setup_helpers.py + tests</name>
  <files>scripts/setup_helpers.py, tests/test_setup.py</files>
  <read_first>
    - scripts/setup_helpers.py (current state — find hardcoded .venv/bin/python at lines 216 and 391)
    - tests/test_setup.py (current test classes — add TestWindowsCompat without breaking existing tests)
  </read_first>
  <behavior>
    - Test 1 (test_venv_python_returns_scripts_on_win32): When sys.platform == "win32", _venv_python("/project") returns "/project/.venv/Scripts/python.exe"
    - Test 2 (test_venv_python_returns_bin_on_linux): When sys.platform == "linux", _venv_python("/project") returns "/project/.venv/bin/python"
    - Test 3 (test_mcp_config_uses_venv_python_win32): On win32, generate_mcp_config() writes .mcp.json with command containing "Scripts/python.exe"
    - Test 4 (test_mcp_config_uses_venv_python_linux): On linux, generate_mcp_config() writes .mcp.json with command containing ".venv/bin/python"
    - Test 5 (test_health_check_uses_platform_venv_path): check_health() calls subprocess with path from _venv_python(), not hardcoded .venv/bin/python
  </behavior>
  <action>
    1. In `scripts/setup_helpers.py`, add a new function after the existing imports (around line 20, after `import urllib.request`):
       ```python
       import sys

       def _venv_python(project_dir: str) -> str:
           """Return the correct venv python executable path for the current platform."""
           if sys.platform == "win32":
               return os.path.join(project_dir, ".venv", "Scripts", "python.exe")
           return os.path.join(project_dir, ".venv", "bin", "python")
       ```
       Note: `sys` is already imported at the bottom in the `__main__` block — move the `import sys` to the top-level imports.

    2. Replace line 216 in `generate_mcp_config()`:
       OLD: `venv_python = os.path.join(project_dir, ".venv", "bin", "python")`
       NEW: `venv_python = _venv_python(project_dir)`

    3. Replace line 391 in `check_health()`:
       OLD: `venv_python = os.path.join(project_dir, ".venv", "bin", "python")`
       NEW: `venv_python = _venv_python(project_dir)`

    4. In `tests/test_setup.py`, add new import for `_venv_python`:
       Update the import block to include `_venv_python` from `scripts.setup_helpers`.

    5. Add `TestWindowsCompat` class to `tests/test_setup.py` with the 5 tests described in behavior.
       Use `mock.patch("scripts.setup_helpers.sys")` to mock `sys.platform` for platform-specific tests.
       For test_mcp_config_uses_venv_python_win32: create `.venv/Scripts/python.exe` in tmp_path instead of `.venv/bin/python`, mock sys.platform to "win32", then call generate_mcp_config and check the command path.
       For test_health_check_uses_platform_venv_path: mock subprocess.run and urllib.request.urlopen, mock sys.platform, call check_health, verify subprocess.run was called with a path containing "Scripts/python.exe" (win32) or ".venv/bin/python" (linux).

    Per D-02: Use `sys.platform == "win32"` (not `os.name == "nt"`). Keep stdlib-only.
  </action>
  <verify>
    <automated>python -m pytest tests/test_setup.py -x -q -k "TestWindowsCompat or TestMcpConfigGeneration or TestHealthReport"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/setup_helpers.py contains `def _venv_python(project_dir: str) -> str:`
    - scripts/setup_helpers.py contains `if sys.platform == "win32":`
    - scripts/setup_helpers.py contains `Scripts/python.exe` (in the _venv_python function)
    - scripts/setup_helpers.py line in generate_mcp_config uses `_venv_python(project_dir)` not hardcoded `.venv/bin/python`
    - scripts/setup_helpers.py line in check_health uses `_venv_python(project_dir)` not hardcoded `.venv/bin/python`
    - tests/test_setup.py contains `class TestWindowsCompat`
    - tests/test_setup.py contains `test_venv_python_returns_scripts_on_win32`
    - tests/test_setup.py contains `test_mcp_config_uses_venv_python_win32`
    - `python -m pytest tests/test_setup.py -x -q` exits 0
  </acceptance_criteria>
  <done>_venv_python() helper exists; both generate_mcp_config() and check_health() use it; 5 new tests pass; all existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add platform detection to setup.sh + create .gitattributes</name>
  <files>setup.sh, .gitattributes</files>
  <read_first>
    - setup.sh (full file — understand the subcommand blocks, the python3 check block at line 240, venv activation at line 275, and all python3 calls at lines 313/315/321/326/331/339)
    - .gitattributes (confirm it does not exist yet)
  </read_first>
  <action>
    1. Create `.gitattributes` at the repo root with this exact content:
       ```
       # Normalize line endings: enforce LF for shell and Python files
       * text=auto
       *.sh    text eol=lf
       *.py    text eol=lf
       *.bash  text eol=lf
       ```
       Per D-04: enforce LF at the git level. Per D-05: do NOT add runtime CRLF stripping.

    2. In `setup.sh`, insert a platform detection block IMMEDIATELY AFTER the logging function definitions (after line 22, before the `# -- Subcommand: sync-auth` block at line 24). This ensures $PYTHON_CMD and $VENV_ACTIVATE are set before ANY subcommand can use them. The block must be:
       ```bash
       # ── Platform detection ────────────────────────────────────────────────────
       OS_TYPE="$(uname -s)"
       case "$OS_TYPE" in
           MINGW*|MSYS*|CYGWIN*)
               VENV_ACTIVATE=".venv/Scripts/activate"
               PYTHON_CMD="python"
               ;;
           *)
               VENV_ACTIVATE=".venv/bin/activate"
               PYTHON_CMD="python3"
               ;;
       esac
       ```
       Use `OS_TYPE` as the variable name (the `create-shortcut` subcommand already uses `OS` locally — avoid collision).
       Per D-01: use `MINGW*|MSYS*|CYGWIN*` case pattern.
       Per D-03: Windows gets `PYTHON_CMD=python`, Linux/macOS gets `PYTHON_CMD=python3`.

    3. Replace the Python 3 check block (currently lines 240-252). The current code:
       ```bash
       if ! command -v python3 &>/dev/null; then
           error "Python 3 not found. Install Python 3.11+."
       fi
       PY_VERSION=$(python3 -c "import sys; ...")
       PY_MAJOR=$(python3 -c "import sys; ...")
       PY_MINOR=$(python3 -c "import sys; ...")
       ```
       Replace all `python3` in this block with `$PYTHON_CMD`:
       ```bash
       if ! command -v $PYTHON_CMD &>/dev/null; then
           error "Python 3 not found. Install Python 3.11+."
       fi
       PY_VERSION=$($PYTHON_CMD -c "import sys; print(f'{sys.version_info.major}.{sys.version_info.minor}')")
       PY_MAJOR=$($PYTHON_CMD -c "import sys; print(sys.version_info.major)")
       PY_MINOR=$($PYTHON_CMD -c "import sys; print(sys.version_info.minor)")
       ```

    4. Replace the venv creation and activation block (currently lines 273-275):
       OLD:
       ```bash
       python3 -m venv .venv
       source .venv/bin/activate
       ```
       NEW:
       ```bash
       $PYTHON_CMD -m venv .venv
       source "$VENV_ACTIVATE"
       ```

    5. Replace ALL remaining `python3` calls in setup.sh with `$PYTHON_CMD`. Specifically:
       - Line ~313: `python3 scripts/setup_helpers.py mcp-config` -> `$PYTHON_CMD scripts/setup_helpers.py mcp-config`
       - Line ~315: `python3 scripts/setup_helpers.py mcp-config` -> `$PYTHON_CMD scripts/setup_helpers.py mcp-config`
       - Line ~321: `python3 scripts/setup_helpers.py register-hooks` -> `$PYTHON_CMD scripts/setup_helpers.py register-hooks`
       - Line ~326: `python3 scripts/setup_helpers.py claude-md` -> `$PYTHON_CMD scripts/setup_helpers.py claude-md`
       - Line ~331: `python3 scripts/jcodemunch_index.py` -> `$PYTHON_CMD scripts/jcodemunch_index.py`
       - Line ~339: `python3 scripts/setup_helpers.py health` -> `$PYTHON_CMD scripts/setup_helpers.py health`
       - Line ~44: `python3 -c "import sys, json..."` in sync-auth -> `$PYTHON_CMD -c "import sys, json..."`
       - Line ~87-88: `python3` calls in create-shortcut icon generation -> `$PYTHON_CMD`

    6. Update the final "Done" message (currently line 347):
       OLD: `echo "  1. Start Agent42:  source .venv/bin/activate && python agent42.py"`
       NEW: `echo "  1. Start Agent42:  source $VENV_ACTIVATE && python agent42.py"`

    IMPORTANT: Do NOT touch the `create-shortcut` subcommand's own `OS="$(uname -s)"` and platform detection (lines 59-218). That is a separate scope for browser detection and will continue to work as-is. Only add the top-level `OS_TYPE` detection block.
  </action>
  <verify>
    <automated>bash -n setup.sh && echo "setup.sh syntax OK" && test -f .gitattributes && grep -q "eol=lf" .gitattributes && echo ".gitattributes OK" && grep -c "python3" setup.sh | xargs -I{} test {} -le 2 && echo "python3 references minimized"</automated>
  </verify>
  <acceptance_criteria>
    - .gitattributes exists at repo root
    - .gitattributes contains `* text=auto`
    - .gitattributes contains `*.sh    text eol=lf`
    - .gitattributes contains `*.py    text eol=lf`
    - setup.sh contains `OS_TYPE="$(uname -s)"`
    - setup.sh contains `MINGW*|MSYS*|CYGWIN*)`
    - setup.sh contains `VENV_ACTIVATE=".venv/Scripts/activate"`
    - setup.sh contains `PYTHON_CMD="python"`
    - setup.sh contains `source "$VENV_ACTIVATE"`
    - setup.sh contains `$PYTHON_CMD scripts/setup_helpers.py mcp-config`
    - setup.sh contains `$PYTHON_CMD scripts/setup_helpers.py register-hooks`
    - setup.sh contains `$PYTHON_CMD scripts/setup_helpers.py health`
    - setup.sh does NOT contain `source .venv/bin/activate` outside of comments or echo strings
    - `bash -n setup.sh` exits 0 (valid syntax)
    - grep for `python3` in setup.sh returns at most 2 matches (only in the docstring header comments, not in executable lines — the sync-auth SSH inline python can remain as python3 since that runs on the REMOTE Linux server)
  </acceptance_criteria>
  <done>setup.sh has platform detection at top, all local python3 calls use $PYTHON_CMD, venv activation uses $VENV_ACTIVATE, .gitattributes enforces LF endings. bash -n passes.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_setup.py -x -q` — all tests pass (existing + new TestWindowsCompat)
2. `bash -n setup.sh` — no syntax errors
3. `test -f .gitattributes` — file exists
4. `grep -c "python3" setup.sh` — at most 2 occurrences (header comments or remote SSH only)
5. `grep "_venv_python" scripts/setup_helpers.py` — function defined and called in both generate_mcp_config and check_health
</verification>

<success_criteria>
- All existing tests in test_setup.py still pass (no regressions)
- 5 new TestWindowsCompat tests pass
- setup.sh passes bash -n syntax check
- .gitattributes exists with eol=lf rules
- No hardcoded .venv/bin/python remains in setup_helpers.py
- No hardcoded python3 in executable lines of setup.sh (except remote SSH commands)
</success_criteria>

<output>
After completion, create `.planning/workstreams/gsd-and-jcodemunch-integration/phases/02-windows-claude-md/02-01-SUMMARY.md`
</output>
