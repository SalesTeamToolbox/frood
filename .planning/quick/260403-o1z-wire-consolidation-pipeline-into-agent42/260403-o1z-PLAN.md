# Quick Task 260403-o1z: Wire consolidation pipeline into agent42.py

**Created:** 2026-04-04
**Status:** Ready

## Plan 01: Wire consolidation pipeline end-to-end

### Task 1: Add ConsolidationRouter to memory/consolidation.py

**Files:** `memory/consolidation.py`
**Action:** Add a `ConsolidationRouter` class before `ConsolidationPipeline` that implements `async complete(model, messages) -> (text, provider_name)`. Provider chain: OpenRouter free -> Synthetic.new -> Anthropic -> OpenAI. Uses httpx async. Returns `("", "none")` if all fail.
**Verify:** Class exists, has `.complete()` method, uses async httpx
**Done:** ConsolidationRouter can be instantiated and passed to ConsolidationPipeline

### Task 2: Instantiate pipeline and fix SessionManager wiring in agent42.py

**Files:** `agent42.py`
**Action:** After memory backends are initialized (~line 158), create ConsolidationRouter + ConsolidationPipeline. Move SessionManager creation from line 123 to after pipeline is ready, passing redis_backend and consolidation_pipeline.
**Verify:** SessionManager receives both backends, ConsolidationPipeline is created with router + embeddings + qdrant
**Done:** `self.session_manager` has consolidation pipeline, `self.consolidation_pipeline` is accessible

### Task 3: Add tests for ConsolidationRouter

**Files:** `tests/test_memory_backends.py`
**Action:** Add test class for ConsolidationRouter: test instantiation, test complete returns tuple, test graceful failure with no API keys.
**Verify:** `python -m pytest tests/test_memory_backends.py -x -q` passes
**Done:** Router adapter has test coverage
