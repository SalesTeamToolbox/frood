---
phase: 31-advanced-migration-docker
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.paperclip.yml
  - .env.paperclip.example
autonomous: true
requirements:
  - ADV-05
must_haves:
  truths:
    - "docker-compose.paperclip.yml defines 5 services: postgresql, redis, qdrant, agent42-sidecar, paperclip"
    - "Health check dependency chain enforces startup order: postgresql+qdrant+redis healthy before agent42-sidecar; agent42-sidecar healthy before paperclip"
    - "agent42-sidecar runs in sidecar mode with CMD python agent42.py --sidecar"
    - "Internal Docker network URLs are set via environment overrides, not in .env"
    - ".env.paperclip.example contains all required variables for the Paperclip stack"
  artifacts:
    - path: "docker-compose.paperclip.yml"
      provides: "5-service Docker Compose topology for Paperclip + Agent42 stack"
      contains: "agent42-sidecar"
    - path: ".env.paperclip.example"
      provides: "Environment variable template for Paperclip deployment"
      contains: "PAPERCLIP_COMPANY_ID"
  key_links:
    - from: "docker-compose.paperclip.yml"
      to: "Dockerfile"
      via: "build context for agent42-sidecar service"
      pattern: "build: \\."
    - from: "docker-compose.paperclip.yml"
      to: "agent42.py"
      via: "command override for sidecar mode"
      pattern: "python.*agent42\\.py.*--sidecar"
---

<objective>
Docker Compose topology that runs the full Paperclip + Agent42 stack with a single `docker compose up` command, including health checks and correct startup ordering.

Purpose: ADV-05 requires a docker-compose.paperclip.yml that orchestrates Paperclip, Agent42 sidecar, Qdrant, PostgreSQL, and Redis with health-check-gated dependencies so all services reach healthy status without manual intervention.

Output: `docker-compose.paperclip.yml` and `.env.paperclip.example`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-advanced-migration-docker/31-CONTEXT.md
@.planning/phases/31-advanced-migration-docker/31-RESEARCH.md

@docker-compose.yml
@Dockerfile
@.env.example

<interfaces>
<!-- Key patterns from existing compose and Dockerfile the executor needs. -->

From docker-compose.yml (existing standalone compose):
```yaml
# Redis health check pattern:
healthcheck:
  test: ["CMD", "redis-cli", "ping"]
  interval: 10s
  timeout: 3s
  retries: 3

# Qdrant health check pattern:
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:6333/healthz"]
  interval: 10s
  timeout: 3s
  retries: 3

# Environment override pattern for internal URLs:
environment:
  - DASHBOARD_HOST=0.0.0.0
  - REDIS_URL=redis://redis:6379/0
  - QDRANT_URL=http://qdrant:6333
  - QDRANT_ENABLED=true
```

From Dockerfile:
```dockerfile
FROM python:3.12-slim
# curl is installed (apt-get install curl)
# Non-root user: agent42
# Default CMD: python agent42.py
# HEALTHCHECK: curl -f http://localhost:8000/api/health
EXPOSE 8000
```

From dashboard/sidecar.py:
```python
# Sidecar health endpoint: GET /sidecar/health (returns 200 when ready)
# Sidecar default port: 8001 (from PAPERCLIP_SIDECAR_PORT)
```

From .env.example (relevant sidecar vars):
```
# SIDECAR_ENABLED=false
# PAPERCLIP_SIDECAR_PORT=8001
# PAPERCLIP_API_URL=
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker-compose.paperclip.yml with 5-service topology</name>
  <files>docker-compose.paperclip.yml</files>
  <read_first>
    - docker-compose.yml (full file — copy redis/qdrant service patterns, volume naming, health checks)
    - Dockerfile (full file — confirm curl availability, EXPOSE port, CMD, non-root user)
    - .env.example (lines 414-421 — sidecar config vars: SIDECAR_ENABLED, PAPERCLIP_SIDECAR_PORT, PAPERCLIP_API_URL)
    - .planning/phases/31-advanced-migration-docker/31-RESEARCH.md (Pattern 4: Docker Compose service_healthy dependency chain; Pitfall 2: DASHBOARD_HOST override; Pitfall 3: PostgreSQL health check; Pitfall 5: curl availability)
  </read_first>
  <action>
    Create docker-compose.paperclip.yml at project root. Per D-04, this is a NEW file — do NOT modify existing docker-compose.yml.

    The file defines 5 services per D-05 with health-check dependency chain per D-13:

    ```yaml
    # Paperclip + Agent42 deployment stack
    # Usage: cp .env.paperclip.example .env.paperclip && nano .env.paperclip && docker compose -f docker-compose.paperclip.yml up -d
    #
    # Startup order (health-check gated):
    #   1. postgresql, redis, qdrant (parallel, no dependencies)
    #   2. agent42-sidecar (waits for postgresql + redis + qdrant healthy)
    #   3. paperclip (waits for agent42-sidecar healthy)

    services:
      postgresql:
        image: postgres:16
        env_file:
          - .env.paperclip
        environment:
          POSTGRES_DB: paperclip
          POSTGRES_USER: ${POSTGRES_USER:-paperclip}
          POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in .env.paperclip}
        ports:
          - "${POSTGRES_PORT:-5432}:5432"
        volumes:
          - postgresql-data:/var/lib/postgresql/data
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-paperclip} -d $${POSTGRES_DB:-paperclip}"]
          interval: 10s
          timeout: 5s
          retries: 5
          start_period: 30s
        restart: unless-stopped

      redis:
        image: redis:7-alpine
        ports:
          - "${REDIS_PORT:-6379}:6379"
        volumes:
          - redis-data:/data
        healthcheck:
          test: ["CMD", "redis-cli", "ping"]
          interval: 10s
          timeout: 3s
          retries: 3
        restart: unless-stopped

      qdrant:
        image: qdrant/qdrant:latest
        ports:
          - "${QDRANT_PORT:-6333}:6333"
        volumes:
          - qdrant-data:/qdrant/storage
        healthcheck:
          test: ["CMD", "curl", "-f", "http://localhost:6333/healthz"]
          interval: 10s
          timeout: 3s
          retries: 3
        restart: unless-stopped

      agent42-sidecar:
        build: .
        command: ["python", "agent42.py", "--sidecar"]
        env_file:
          - .env.paperclip
        environment:
          SIDECAR_ENABLED: "true"
          DASHBOARD_HOST: "0.0.0.0"
          PAPERCLIP_SIDECAR_PORT: "8001"
          REDIS_URL: "redis://redis:6379/0"
          QDRANT_URL: "http://qdrant:6333"
          QDRANT_ENABLED: "true"
          PAPERCLIP_API_URL: "http://paperclip:3000"
        ports:
          - "${SIDECAR_PORT:-8001}:8001"
        volumes:
          - agent42-data:/app/.agent42
        depends_on:
          postgresql:
            condition: service_healthy
          qdrant:
            condition: service_healthy
          redis:
            condition: service_healthy
        healthcheck:
          test: ["CMD", "curl", "-f", "http://localhost:8001/sidecar/health"]
          interval: 15s
          timeout: 5s
          retries: 3
          start_period: 20s
        restart: unless-stopped

      paperclip:
        image: paperclip/app:latest
        env_file:
          - .env.paperclip
        environment:
          DATABASE_URL: "postgresql://${POSTGRES_USER:-paperclip}:${POSTGRES_PASSWORD}@postgresql:5432/${POSTGRES_DB:-paperclip}"
          AGENT42_SIDECAR_URL: "http://agent42-sidecar:8001"
          REDIS_URL: "redis://redis:6379/0"
        ports:
          - "${PAPERCLIP_PORT:-3000}:3000"
        depends_on:
          agent42-sidecar:
            condition: service_healthy
        healthcheck:
          test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
          interval: 15s
          timeout: 5s
          retries: 3
          start_period: 30s
        restart: unless-stopped

    volumes:
      postgresql-data:
      redis-data:
      qdrant-data:
      agent42-data:
    ```

    Key decisions implemented:
    - D-04: New file, existing docker-compose.yml untouched
    - D-05: All 5 services present (postgresql, redis, qdrant, agent42-sidecar, paperclip)
    - D-06: agent42-sidecar uses `command: ["python", "agent42.py", "--sidecar"]`
    - D-12: Internal Docker URLs (REDIS_URL, QDRANT_URL, PAPERCLIP_API_URL, DATABASE_URL, AGENT42_SIDECAR_URL) set in environment: blocks, NOT in .env
    - D-13: Health check chain — postgresql/qdrant/redis -> agent42-sidecar -> paperclip (all using service_healthy condition)
    - Pitfall 2: DASHBOARD_HOST set to "0.0.0.0" in environment override for inter-container reach
    - Pitfall 3: pg_isready uses -U and -d flags with start_period: 30s
    - Pitfall 5: curl is available in agent42-sidecar (confirmed in Dockerfile)
    - Paperclip image: `paperclip/app:latest` placeholder with comment to replace (Claude's Discretion)
    - PostgreSQL 16 (Claude's Discretion)
    - No nginx reverse proxy (Claude's Discretion — keep minimal)
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('docker-compose.paperclip.yml')); print('YAML valid')"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.paperclip.yml exists at project root
    - File contains `services:` key with exactly 5 services: `postgresql:`, `redis:`, `qdrant:`, `agent42-sidecar:`, `paperclip:`
    - postgresql service contains `image: postgres:16` and `pg_isready` in healthcheck test
    - postgresql service contains `start_period: 30s` in healthcheck
    - redis service contains `image: redis:7-alpine` and `redis-cli` in healthcheck test
    - qdrant service contains `image: qdrant/qdrant:latest` and `http://localhost:6333/healthz` in healthcheck test
    - agent42-sidecar service contains `build: .` and `command:` with `python` `agent42.py` `--sidecar`
    - agent42-sidecar service contains `DASHBOARD_HOST: "0.0.0.0"` in environment
    - agent42-sidecar service contains `REDIS_URL: "redis://redis:6379/0"` in environment
    - agent42-sidecar service contains `QDRANT_URL: "http://qdrant:6333"` in environment
    - agent42-sidecar service contains `PAPERCLIP_API_URL: "http://paperclip:3000"` in environment
    - agent42-sidecar depends_on has postgresql, qdrant, redis all with `condition: service_healthy`
    - agent42-sidecar healthcheck test contains `http://localhost:8001/sidecar/health`
    - paperclip service contains `AGENT42_SIDECAR_URL: "http://agent42-sidecar:8001"` in environment
    - paperclip service contains `DATABASE_URL:` in environment with `postgresql://` prefix
    - paperclip service depends_on has agent42-sidecar with `condition: service_healthy`
    - File contains `volumes:` key with postgresql-data, redis-data, qdrant-data, agent42-data
    - File is valid YAML (parseable without errors)
    - Existing docker-compose.yml is NOT modified
  </acceptance_criteria>
  <done>
    docker-compose.paperclip.yml at project root defines the full 5-service Paperclip + Agent42 topology with health-check dependency chains and internal Docker network URL overrides.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create .env.paperclip.example template</name>
  <files>.env.paperclip.example</files>
  <read_first>
    - .env.example (full file — reference for Agent42 env var patterns, sidecar section at lines 414-421)
    - docker-compose.paperclip.yml (the file created in Task 1 — understand which vars are referenced via ${VAR} syntax)
    - .planning/phases/31-advanced-migration-docker/31-CONTEXT.md (D-11: shared .env.paperclip.example template; D-12: internal URLs NOT in .env)
  </read_first>
  <action>
    Create .env.paperclip.example at project root. Per D-11, this template contains ALL required variables for the Paperclip stack. Per D-12, internal Docker network URLs (REDIS_URL, QDRANT_URL, AGENT42_SIDECAR_URL, DATABASE_URL, PAPERCLIP_API_URL) are NOT in this file because they are set via compose environment overrides.

    File contents (exact):

    ```
    # -- Paperclip + Agent42 Stack Configuration ----------------------------------
    # Copy to .env.paperclip and fill in values before running:
    #   docker compose -f docker-compose.paperclip.yml up -d
    #
    # IMPORTANT: Internal Docker network URLs (REDIS_URL, QDRANT_URL, DATABASE_URL,
    # AGENT42_SIDECAR_URL, PAPERCLIP_API_URL) are set in docker-compose.paperclip.yml.
    # Do NOT override them here -- doing so will break container networking.

    # -- PostgreSQL ----------------------------------------------------------------
    POSTGRES_USER=paperclip
    POSTGRES_PASSWORD=changeme-use-a-strong-password
    POSTGRES_DB=paperclip
    POSTGRES_PORT=5432

    # -- Redis ---------------------------------------------------------------------
    REDIS_PORT=6379

    # -- Qdrant --------------------------------------------------------------------
    QDRANT_PORT=6333

    # -- Agent42 Sidecar -----------------------------------------------------------
    # Port exposed on host for agent42-sidecar (container always listens on 8001)
    SIDECAR_PORT=8001

    # LLM Provider API Keys (at least one required for agent execution)
    # See .env.example for full list of supported providers
    OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
    # OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
    # ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
    # DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
    # CEREBRAS_API_KEY=csk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
    # GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

    # Dashboard auth (used by sidecar for JWT token validation)
    JWT_SECRET=generate-with-python-c-import-secrets-print-secrets-token_hex-32
    DASHBOARD_PASSWORD_HASH=$2b$12$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

    # Sandbox (always enabled in production)
    SANDBOX_ENABLED=true

    # -- Paperclip -----------------------------------------------------------------
    PAPERCLIP_PORT=3000
    # Paperclip-specific configuration (consult Paperclip docs)
    # PAPERCLIP_SECRET_KEY=
    # PAPERCLIP_COMPANY_ID=
    ```

    Key design decisions:
    - D-11: All required variables present with placeholder values
    - D-12: Header warns against overriding internal Docker URLs
    - Only external-facing ports are configurable (POSTGRES_PORT, REDIS_PORT, QDRANT_PORT, SIDECAR_PORT, PAPERCLIP_PORT)
    - LLM API keys section references .env.example for full provider list
    - JWT_SECRET and DASHBOARD_PASSWORD_HASH included per security requirements
    - SANDBOX_ENABLED=true per CLAUDE.md security requirements
  </action>
  <verify>
    <automated>python -c "
lines = open('.env.paperclip.example').readlines()
keys = [l.split('=')[0].strip() for l in lines if '=' in l and not l.strip().startswith('#')]
required = ['POSTGRES_USER', 'POSTGRES_PASSWORD', 'POSTGRES_DB', 'SIDECAR_PORT', 'JWT_SECRET', 'SANDBOX_ENABLED', 'OPENROUTER_API_KEY']
missing = [k for k in required if k not in keys]
assert not missing, f'Missing keys: {missing}'
assert any('Do NOT override' in l for l in lines), 'Missing D-12 warning about internal URLs'
print(f'Valid: {len(keys)} keys, D-12 warning present')
"</automated>
  </verify>
  <acceptance_criteria>
    - .env.paperclip.example exists at project root
    - File contains `POSTGRES_USER=paperclip`
    - File contains `POSTGRES_PASSWORD=changeme-use-a-strong-password`
    - File contains `POSTGRES_DB=paperclip`
    - File contains `SIDECAR_PORT=8001`
    - File contains `OPENROUTER_API_KEY=sk-or-`
    - File contains `JWT_SECRET=`
    - File contains `DASHBOARD_PASSWORD_HASH=`
    - File contains `SANDBOX_ENABLED=true`
    - File contains `PAPERCLIP_PORT=3000`
    - File contains a warning comment about NOT overriding internal Docker URLs (D-12)
    - File does NOT contain `REDIS_URL=` as an active (non-commented) variable
    - File does NOT contain `QDRANT_URL=` as an active (non-commented) variable
    - File does NOT contain `DATABASE_URL=` as an active (non-commented) variable
    - File does NOT contain `AGENT42_SIDECAR_URL=` as an active (non-commented) variable
    - Existing .env.example is NOT modified
  </acceptance_criteria>
  <done>
    .env.paperclip.example template contains all required variables for the Paperclip stack, with clear D-12 warning about internal URLs, and placeholder values for all secrets.
  </done>
</task>

</tasks>

<verification>
- docker-compose.paperclip.yml is valid YAML with 5 services and correct health check dependency chain
- .env.paperclip.example contains all required variables without internal Docker URLs
- Existing docker-compose.yml and .env.example are NOT modified
- `python -m pytest tests/ -x -q` — full test suite passes (no regressions)
</verification>

<success_criteria>
- docker-compose.paperclip.yml defines postgresql, redis, qdrant, agent42-sidecar, paperclip with service_healthy dependency chain
- agent42-sidecar builds from Dockerfile, runs --sidecar mode, overrides DASHBOARD_HOST to 0.0.0.0
- All internal Docker network URLs are in compose environment blocks, not in .env
- .env.paperclip.example is a complete template with all configurable variables
- No existing files modified (docker-compose.yml, .env.example untouched)
</success_criteria>

<output>
After completion, create `.planning/phases/31-advanced-migration-docker/31-02-SUMMARY.md`
</output>
