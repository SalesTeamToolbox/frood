# Deferred Items — Phase 31

## Pre-existing Test Failures

- **test_app_git.py::TestAppManagerGit::test_persistence_with_git_fields** — PathLib ValueError on Windows with temp path not being subpath of project root. Unrelated to migration work.
